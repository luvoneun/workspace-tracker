# 제품 결정 기록

미래의 에이전트가 "개선"한다며 뒤집기 쉬운 결정만, 한 줄씩 적는다. 기능을 고치기 전에 관련 줄을 확인하고, 뒤집어야 하면 먼저 사용자에게 묻는다. 새 결정은 같은 형식으로 덧붙인다(과거 줄을 고쳐 쓰지 않는다).
확신: **확인** = 사용자가 직접 확인함 · **코드** = 코드·주석·테스트에 근거가 있음 · **추측** = 근거를 못 찾음(사용자 확인 필요)

## 데이터·저장
- [초기] AI는 `tracker/`의 업무 목록을 직접 편집하지 않고 앱 API로만 넣는다. 서버가 꺼져 있으면 우회하지 않고 실패를 알린다 — 검증·ID 생성·인박스 분류·저장 복구를 우회하지 않으려고 · 확인
- [초기] `meeting_drafts.json`은 끝에 추가만 하고 기존 항목은 고치지 않는다 — 앱이 노트·종류·문구 해시로 검토 기록을 식별해서, 고치면 "이미 검토함"이 끊긴다(`workflow-store.js`) · 확인
- [초기] 업무 데이터는 코드 저장소에 넣지 않는다 — 다른 사람이 빈 상태로 시작할 수 있게. 백업은 프로젝트 밖 별도 비공개 저장소에 하루 한 번(19:30)이고, 그 뒤 저녁 작업이 다음 날까지 백업되지 않는 것은 감수한다(2026-09-20) · 확인
- [2026-09-20] "복구 필요" 상태에서는 서버를 띄운 채 저장만 막고, 저널·잠금·파일을 자동으로 지우거나 되돌리지 않는다 — 사람이 확인하기 전에 다음 저장이 덮어쓰지 못하게 / 버린 대안: 자동 복구 · 코드
- [초기] 조회(GET)는 어떤 파일도 쓰지 않는다 — 화면을 여는 것만으로 기록이 바뀌지 않게(테스트로 고정) · 코드
- [2026-09-21] 회의 초안의 할 일은 기본 "나중에 할 일"로 담고, 사람이 카드에서 "오늘 할 일"을 고른 것만 오늘 목록에 올린다. 회의 하나가 오늘 목록을 채우지 않게 한 결정은 그대로 두고 예외만 연다(할 일이 아닌 항목의 "오늘"은 서버가 거절) · 확인
- [2026-09-21] 확인 대기에도 마감일(`due`)을 둔다 — 상대에게 회신을 받아야 하는 기한이다(화면 이름은 "회신 기한"). 회의 정리의 초안 카드와 직접 담기에서 사람이 지정하고, AI는 채우지 않는다. 결정·아이디어에는 여전히 마감일이 없다("마감일은 할 일에만" 결정을 확인 대기까지 넓힘) · 확인
- [2026-09-21] 초안 담기는 결과 카드의 "실행 취소"로 되돌린다: 만든 항목은 삭제 휴지통으로 옮기고(원문 보존) 초안은 다시 검토 대기로 올린다. `meeting_drafts.json`은 이때도 고치지 않고, 이 회의에서 초안으로 만든 항목이 아니면 서버가 거절한다(테스트로 고정) · 확인

## 날짜
- [초기] 마감일(`due`)은 사용자가 명시한 날짜만 넣고 절대 추측하지 않는다. 애매하면 비운다 — 틀린 마감이 기록처럼 남으면 목록을 못 믿게 된다 · 확인
- [초기] 마감일이 있어도 "오늘 할 일"로 올리지 않는다 — 실행 예정일(`scheduled`)과 마감일은 별개다 · 확인
- [초기] 옛 항목의 날짜를 일괄 변환하거나, 덮어써진 과거 마감일을 추정해 복원하지 않는다 — 추정한 날짜가 기록처럼 보이지 않게 · 코드
- [초기] 회의 초안에서 담은 할 일은 "나중에 할 일"로 들어간다 — 회의 하나가 오늘 목록을 채우지 않게 · 코드
- [초기] 마감일은 할 일(task)에만 있다. 확인 대기·결정·아이디어에는 넣지 않는다 — 의도한 제한이다 · 확인

## 자동 수집
- [초기] Slack 채널에 공유된 것은 사람이 이미 고른 것이므로 AI가 포함 여부를 다시 판단하지 않는다 — 문구 다듬기·맥락 파악·중복 제거만 / 버린 대안: AI의 중요도 필터 · 확인
- [초기] 하나라도 실패하면 수집 커서를 전진시키지 않는다 — 재시도의 중복은 서버가 원본 링크로 걸러 주므로 "빠뜨림"보다 "다시 시도"가 안전하다 · 코드
- [초기] Slack 수집은 주말에도 돌고 9~19시에만 돈다 — 요일 제한은 사용자 요청으로 뺐다. 시간대 제한은 꼭 필요해서 둔 게 아니다: 풀어도 부담은 미미하고, 시간 밖에 공유한 메시지가 다음 날로 밀려도 상관없다(2026-09-20 사용자) · 확인
- [2026-09-20] Slack 수집 실행은 파일 수정 권한 없이 돈다(등록·조회 두 명령만). 실수 방지 장치이지 OS 격리가 아니다. 캘린더·지라 실행은 파일을 써야 해서 그대로 둔다 · 확인
- [2026-09-20] 프로세스 종료 신호는 직접 띄운 프로세스와 그 그룹에만 보낸다. 프로세스 목록을 훑어 대상을 고르지 않는다 — 관계없는 프로그램을 강제 종료한 사고가 있었다. "종료 미확인 시 잠금 유지"는 스크립트를 단순하게 두려고 보류했다(겹쳐 돌아도 데이터는 안전) · 확인
- [2026-09-20] 수집 잠금은 경과 시간이 아니라 "잠금 주인이 살아 있는 이 스크립트인가"로 판단한다 — 시간 기준은 정상 실행을 뺏거나 죽은 잠금을 방치했다 · 코드
- [2026-09-22] 슬랙 수집은 공유가 아닌 모든 메시지를 직접 적은 메모로 본다(링크 미리보기·파일이 붙은 글이 어느 갈래에도 안 걸려 빠졌다). 본 메시지의 처리 결과를 빠짐없이 센다. 공유하며 같이 적은 메모는 맥락으로 참고한다 · 확인
- [2026-09-22] 지라 캐시는 업무에 연결된 키 중 기본 조회(내 담당·미완료)에서 빠진 것을 `## 업무에 연결된 그 밖의 이슈` 구역에 따로 적는다 — 이슈가 완료되면 요약이 사라져 번호만 남았다. 그 구역의 이슈는 요약 표시에만 쓰고 프로젝트 고르기 목록에는 넣지 않으며, 완료 범주는 상태 칸을 `완료` 한 글자로 통일해 앱이 판정한다 · 확인
- [2026-09-22] 내 담당 지라 티켓 목록도 앱 서버가 지라에서 직접 읽는다(메모리에만, 10분마다·새로고침 때). AI가 쓰는 스냅샷 파일은 대비책으로 남긴다 — 직접 읽지 못할 때만 그 파일과 낡음 경고가 쓰인다 · 확인
- [초기] 자동화 스크립트는 설정·토큰을 node로 읽는다 — launchd 밑에서 python3·bash의 파일 접근이 조용히 막혀 수집이 몇 시간 멈춘 적이 있다 · 코드
- [2026-09-24] 지라 동기화 자동화(jira-sync, Claude가 jira_issues.md를 쓰던 것)를 없앤다 — 앱이 지라를 직접 읽게 된 뒤로 대비책일 뿐이었고, Claude 호출만 소모하며 실패가 쌓였다 · 확인

## 주간보고
- [2026-09-20] 주간보고는 업무 원본에서 만드는 초안이고, 외부 AI 호출이나 결과 추측을 하지 않는다 · 확인
- [2026-09-20] 직접 고친 보고 문장은 덮어쓰지 않고 수정 제안으로만 보여 준다. 옛 `weekly_reports.md`는 그대로 두고 새 편집은 `.report-drafts.json`에 저장한다 · 확인
- [2026-09-20] 다음 주 계획은 사람이 직접 쓴다. 자동으로 채우지 않는다 · 코드
- [2026-09-22] 다음 주 계획 문장에 프로젝트를 지정할 수 있다(`/api/report/change`의 add가 선택적 `group`을 받는다). 사람이 직접 쓴다는 결정은 그대로다 — 묶어 보여 줄 뿐 문장을 만들어 주지 않는다. 저장 경로는 여전히 `/api/report/change` 하나다 · 확인
- [2026-09-22] 슬랙 복사는 구역 → 프로젝트 → 글머리 항목 형식이고, 서식 있는 복사(`text/html`)와 일반 글자를 함께 넣는다. 슬랙에 넣을 구역은 사람이 칩으로 고른다(기본 완료·진행 중·예정) — 사용자가 실제로 올리는 글의 구조를 그대로 살리려고 · 확인 → [2026-09-23] 구역 제목은 `[완료]` `[진행중]` `[예정]`처럼 대괄호로 감싼다(사용자가 올리는 글의 모양) → [2026-09-24] 슬랙으로 나가는 프로젝트 줄에는 지라 키를 싣지 않는다(요약만)
- [2026-09-22] 앱이 모르는 소제목도 그 이름 그대로 한 구역이 되고, 처음 보는 구역은 켠 채로 시작한다 — 복사할 때 문장이 조용히 빠지지 않게 · 코드
- [2026-09-22] 묶은 문장은 나중에도 풀 수 있다(묶을 때 원래 문장을 함께 저장, `/api/report/change`의 split). 직전 변경 되돌리기는 알림의 버튼과 ⌘Z로도 된다 — 서버 메모리의 되돌리기 토큰은 새로고침·재시작 뒤에는 쓸 수 없어서, 묶기처럼 되돌릴 일이 잦은 변경은 저장된 값으로 풀 수 있어야 했다 · 확인
- [2026-09-22] 주간요약의 묶기를 "글자 합치기"에서 "문장을 다른 문장 아래로 넣기"로 바꿨다(`nest`/`unnest`). 합치면 어느 줄이 대표가 되는지 보이지 않았고, 합친 문장은 굳어서 원본을 따라오지 못했다. 아래로 넣은 문장은 독립된 문장으로 남아 따로 고치고 빼고 자동 갱신된다. 여러 업무를 한 문장으로 요약할 때는 수정 + 제외를 쓴다. 옛 합치기 행과 `merge`/`split`은 호환을 위해 남긴다 · 확인
- [2026-09-20] 옛 주간요약 카드 화면과 서버의 옛 저장 경로(`/api/weekly-report/save`·`refresh`)를 지웠다. 주간보고를 저장하는 길은 보고 초안(`/api/report/change`) 하나다. 옛 `weekly_reports.md`는 읽기만 한다 — 저장 경로가 둘이면 에이전트가 틀린 쪽을 확장한다 · 확인

## 화면
- [초기] 기존 앱의 색·간격·컴포넌트를 우선하고 새 시각 방향은 합의했을 때만 적용한다 — 차분한 생산성 도구, 파란색은 선택·행동에만 · 확인 → [2026-09-21] 확정 룩 항목으로 대체(파란색 규칙은 그대로)
- [2026-09-21] 확정 룩은 토스/flex 느낌이다: 연회색 바탕 위 테두리 없는 둥근 흰 카드, 넉넉한 간격, 뜻 있는 색. **밀도를 목표로 삼지 않는다 — 매일 종일 띄워 두는 화면이라 기분이 먼저다.** 값 기준은 `DESIGN.md` 하나이고, 색·간격·둥글기는 `ui.css`의 토큰으로만 적는다 · 확인
- [2026-09-21] 계획·정리 모드를 없앴다. 줄에 손이 닿으면 `내일`·`나중에`가 그 자리에 뜨고, 나중에 보낸 업무는 머리줄의 `나중에 할 일` 서랍에 모인다 — 모드를 오가지 않고 보던 자리에서 처리하려고 / 버린 대안: 두 목록을 나란히 펼치는 계획 모드 · 확인
- [2026-09-21] 프로젝트 모아보기는 팝업이 아니라 탭이다 — 자주 들어가는 면이라 돌아올 자리가 있어야 한다 · 확인
- [2026-09-21] 상세는 한 벌뿐이고 **누른 줄 옆에 뜨는 카드**로 연다(업무·확인 대기 360px, 회의 정리 460px, 520px 이하에서는 아래 시트) — 오른쪽 끝에 고정한 패널은 누른 곳과 멀어 눈이 줄에서 떨어졌다. 상세의 값은 보이는 그 자리에서 고치고 저장 버튼을 두지 않는다 / 버린 대안: 오른쪽 고정 패널, 줄 높이에 맞춰 여는 `anchorTop` · 확인
- [2026-09-21] 회의 정리도 같은 카드를 쓰되 **빈 곳을 눌러도 닫히지 않는다** — 한참 붙들고 검토하는 면이라 자리를 잃으면 안 된다(✕·Esc·같은 회의 줄 다시 누르기로 닫는다) · 확인
- [2026-09-21] 검색·회의 모아보기·오늘 신규를 ⌘K 팔레트 하나로 모았다. 여러 뷰를 담던 큰 모달 창은 없앴다 — 들어가는 길이 여러 개면 지금 어디에 있는지 헷갈렸다 · 확인 → [2026-09-22] 회의 모아보기는 회의 탭으로
- [2026-09-22] 회의 모아보기를 ⌘K 팔레트에서 떼어 `회의` 탭으로 옮겼다 — 검색은 찾는 창, 회의 모아보기는 훑어보며 몰아서 정리하는 면이라 한 창에 두니 크기도 쓰임도 어긋났다. 팔레트에는 회의를 단어로 찾는 것만 남긴다. 오늘 탭의 미팅 줄은 그대로 줄 옆 카드(빠른 확인), 탭은 몰아서 정리 · 확인
- [2026-09-22] 확인 대기의 날짜 이름을 `회신 기한` → `답변 받을 날`로 바꿨다(줄의 배지는 짧게 `N일 늦음`·`오늘 답변 예정`, 무슨 날인지는 툴팁이 푼다) — "회신 기한 1일 지남"이 내가 회신해야 하는 것처럼 읽혔다. 데이터 필드(`due`)·API 이름은 그대로다 · 확인
- [2026-09-21] 여러 개 선택에 `완료로 표시`와 `삭제`를 더했다. 완료는 서버의 `task-batch`(`status:'done'`, 완료일은 서버가 오늘로 적고 완료 취소는 줄마다 체크로 되돌린다), 삭제는 한 건씩 휴지통으로 보내고 알림 하나로 되돌린다 — 저장 경로를 새로 만들지 않으려고 · 코드
- [2026-09-21] `새로 들어온 것` 줄에는 `오늘`·`나중에`·`완료`를 늘 보인다(나머지는 ⋯ 안) — 자주 누르는 셋이고, 다 읽고 바로 끝내는 줄이 많다 · 확인
- [2026-09-21] 완료 표시는 체크박스다(모서리 둥근 네모 한 가족, 누르는 영역 28px) — 사용자가 "완료 표시는 체크박스가 맞다"고 골랐다 · 확인
- [2026-09-21] 배경을 채운 상태 배지는 급한 말에만 쓴다(`긴급`·`기한 N일 지남`·`오늘까지`·회의의 `초안 N`). 나머지는 아이콘 + 글자다 — 배지가 많으면 급한 것이 안 보인다 · 확인
- [2026-09-21] 말을 거는 문장은 해요체, 버튼·메뉴·열 이름은 짧은 명사형으로 둔다 · 확인
- [2026-09-21] 본문 글꼴 Pretendard Variable을 앱에 넣고 서버가 직접 준다(`/fonts/*.woff2`) — 외부에서 받아오지 않는다. 인증 예외에는 넣지 않는다(아이콘·manifest만 예외라는 결정 그대로) · 확인
- [2026-09-21] `NEW`가 사라질 때 알림을 띄우지 않는다(`/api/track/seen`은 조용히 보낸다) — "NEW는 표시다" 결정의 연장, 사라진 것을 알려 주면 확인 동작이 된다 · 코드
- [초기] `NEW`는 누르는 버튼이 아니라 표시다. 카드가 화면에 잠깐 머물면 조용히 사라진다 — 확인 동작을 강요하지 않으려고 · 코드
- [2026-09-17] 자동화 경고 점은 "지금 실패 중"일 때만 켠다. 해결된 과거 실패는 접어 둔다 — 지금 문제인지 옛 문제인지 헷갈렸다 · 코드
- [초기] 회의 상세에는 프로젝트·반복 회의 입력을 두지 않는다 — 두 곳에서 저장하면 한쪽만 바뀌어 어긋났다 · 코드
- [2026-09-22] 회의에서 담은 항목은 회의 카드의 그 줄에서 바로 고치고 지운다(다른 목록과 같은 ⋯ 메뉴 + 문구 고치기). 종류 바꾸기는 저장 파일이 달라 서버 옮기기가 필요해 미뤘다 · 확인
- [2026-09-22] 오늘 목록의 줄 태그는 우선순위 → 상태(밀림·진행·답변) → 기한 순으로 자리를 고정한다 — 같은 종류가 세로로 서야 훑을 수 있다. 기한이 가장 급한 정보라 맨 오른쪽 · 확인 → [2026-09-23] 항목으로 대체
- [2026-09-22] 우선순위 표시에서 깃발 아이콘을 뺐다(글자와 색이면 충분하고, 같은 아이콘이 열을 따라 반복되면 산만했다). 기한·밀림·답변 아이콘은 뜻을 알려 주는 역할이라 남긴다 · 확인
- [2026-09-22] 더보기(⋯)는 그 줄에서 할 일이 그것뿐인 곳에서는 늘 보인다 — 회의 카드의 줄과 머리, 프로젝트 탭의 확인 대기·결정·아이디어, 전체 업무 기록 · 확인
- [2026-09-22] 우선순위는 체크박스 안의 위 화살표로 말한다(중요 한 겹·긴급 두 겹, 지라의 우선순위 아이콘과 같은 말). 글자 태그를 세 칸으로 고정했더니 빈자리에 글자가 흩어져 보였다 — 줄 오른쪽에는 날짜 성격(밀림·진행·답변·기한)만 남기고 오른쪽 끝에 붙인다 / 버린 대안: 세 칸 고정, 체크박스 색만, 제목 뒤 글자, 느낌표·별·막대·색 점 · 확인
- [2026-09-22] 다음 주 계획은 후보(오늘 할 일의 미완료 업무, 나중에 할 일에서 고르기)에서 눌러 담거나 직접 쓴다. 자동으로 채우지 않는다는 결정은 그대로 — 앱은 후보만 보여 준다. 직접 쓴 문장은 기본으로 나중에 할 일에도 업무로 만든다(만든 뒤에는 서로 따로 논다). 계획 행의 연결은 planOf에 두고 sourceIds에는 넣지 않는다(이번 주 진행중 문장이 사라지지 않게) · 확인
- [2026-09-22] 회의 탭의 기본 목록은 오늘 + 최근 2주의 기록 있는 회의 + 손댈 일이 남은 회의다(캘린더 회의가 매일 쌓여 목록이 끝없이 길어지므로). 미완료는 할 일·확인 대기만 센다 — 결정은 끝내는 대상이 아니다 · 확인
- [2026-09-22] 아이디어·결정 탭은 결정이 본문(넓게, 전문을 줄바꿈으로)이고 아이디어는 왼쪽 좁은 칸이다 — 결정에는 정책 문장이 쌓이고 나중에 찾아보는 자리다. 좁은 창에서는 쌓지 않고 세그먼트로 한쪽만 본다(한쪽이 길어져도 다른 쪽이 밀려나지 않게). 미반영만 보기 토글은 두지 않는다 — 체크(=PRD 반영함)하면 반영 완료로 내려가므로 위 목록이 곧 미반영 목록이다 · 확인
- [2026-09-22] 미팅 노트는 자동으로 가져오지 않고 앱의 버튼을 눌렀을 때만 가져온다(티로에서 사람이 먼저 검수한다). 앱 서버는 요청 표시 파일만 남기고 실행은 launchd가 기존 실행기로 한다 — 서버가 프로세스를 띄우지 않는다 · 확인
- [2026-09-22] 회의에서 나온 줄과 프로젝트 탭의 확인 대기·결정 줄에서도 바로 체크한다(업무=완료, 확인 대기=확인 완료, 결정=PRD 반영함). 저장 길은 목록과 같은 toggle 하나 — 값이 보이는 곳에서 끝낼 수 있어야 한다 · 확인
- [2026-09-22] 지라 프로젝트는 줄 안의 짧은 표기에서는 요약만, 그룹 제목·프로젝트 탭에서는 `KEY · 요약`으로 보인다(번호만으로는 무엇인지 알 수 없었다). 확인 대기는 기본으로 프로젝트별로 묶어 보이고 `급한 순`으로 바꿀 수 있다. 프로젝트 탭은 열린 항목이 없는 프로젝트를 기본으로 숨긴다 · 확인
- [2026-09-22] 확인 대기를 체크하면 그 자리에서 "다음은?"을 묻는다(후속 할 일 · 결정으로 남기기 · 답변 한 줄 남기기 · 기다리던 업무). 알림은 몇 초 만에 사라져 다음 행동으로 이어지지 못했다. 체크는 즉시 저장되고 제안은 무시해도 된다. 답변 한 줄은 업무의 결과 한 줄과 같은 outcome에 담겨 주간요약의 확인 완료 문장이 된다 · 확인
- [2026-09-22] 좁은 레일에서 확인 대기를 체크하면 다음 행동을 한 줄로만 제안하고(기다리던 업무를 오늘로 / 후속 할 일 만들기), 전체 선택지는 상세 카드에서 본다 — 레일은 훑는 자리라 입력 작업을 펼치면 카드가 화면을 다 먹었다 · 확인
- [2026-09-23] 프로젝트 목록은 사람이 분류하지 않고 앱이 아는 값으로 묶는다 — 지라 상태(진행 중 / 시작 전 / 지난)와 배포 버전. 보관·폴더·태그처럼 사람이 붙이는 장치는 두지 않는다(방치되는 걸 확인함) · 확인

## 개발·운영
- [초기] 프레임워크·빌드 단계·외부 패키지 없이 Node 기본 모듈만 쓴다 — 의도한 선택이다. 재작성·프레임워크 도입을 "개선"으로 제안하지 않는다 · 확인
- [초기] 서버는 `127.0.0.1`과 지정한 주소(Tailscale) 하나에만 연다. `0.0.0.0`으로 열지 않는다 — 같은 와이파이의 다른 사람에게 노출되지 않게 · 코드
- [2026-09-20] 다른 기기 접속 인증에서 앱 아이콘(`/icons/*.png`)과 `manifest.webmanifest`만 예외로 연다 — 아이폰은 홈 화면에 추가할 때 이 파일들을 로그인 정보 없이 가져가서, 막아 두면 아이콘이 안 나온다. 그 밖의 어떤 경로도 예외에 넣지 않는다 · 확인
- [2026-09-20] 브랜치 상시 운영·인수인계 문서·변경 이력 문서를 두지 않는다(커밋 메시지와 `git status`가 기준점). 다만 화면 파일을 여러 곳 고칠 때는 격리된 복사본에서 작업한다 — 화면 파일은 저장 즉시 열린 앱에 반영된다 · 확인
- [2026-09-20] 스크립트는 단순하게 유지한다. 드문 상황을 막는 장치보다 읽기 쉬운 구조를 우선한다 — 복잡해진 자리에서 사고가 났다 · 확인
- [2026-09-20] 로그인할 때 앱 창을 자동으로 띄우지 않는다(Dock에서 직접 연다) · 확인
- [2026-09-22] 새로고침에 성공했을 때는 알림을 띄우지 않는다 — 도는 아이콘이 이미 말해 준다. 실패는 지금처럼 알린다 · 확인
- [2026-09-22] 지라 키는 상세(항목 상세 카드의 프로젝트 값, 프로젝트 탭 제목 아래, 툴팁)와 프로젝트를 고르는 목록(요약 · 키)에서만 보이고, 그 밖의 화면은 요약만 보여 준다. 번호는 읽는 사람에게 뜻이 없고 자리만 차지했다. 같은 요약이 한 목록에 둘이면 그때만 키로 구분한다 · 확인
- [2026-09-22] 프로젝트 탭은 연결된 지라 티켓의 실시간 상태·배포 버전/배포일·기한·담당·하위 진행률을 띠 카드로 보여 준다(앱 서버가 API 토큰으로 직접 읽는다, AI를 거치지 않는다). 지라 것임을 늘 밝혀 내 업무의 상태와 헷갈리지 않게 한다. 바꾸기는 다음 단계에서 확인 절차와 함께 · 확인
- [2026-09-22] 마지막으로 본 탭 복원은 사람이 이미 탭을 골랐으면 하지 않는다 — 페이지를 열자마자 누른 탭이 복원에 덮여 "탭 이름만 바뀌고 내용은 이전 탭"이 되던 경쟁이 있었다. 앱이 켜지기 전에 누른 탭은 버튼에 남은 초점으로 알아본다 · 코드
- [2026-09-22] 프로젝트 탭의 지라 띠 카드에서 지라 상태·배포 버전(티켓 이동, 버전 이름·배포일 수정)·기한을 바꿀 수 있다. 지라에 쓰는 동작은 전부 전→후를 보여 주는 확인을 거치고 앱의 ⌘Z 대상이 아니다. 지라가 허용하는 전환만 내놓고 추가 입력이 필요한 전환·버전이 여러 개인 티켓은 지라에서 직접 하게 안내한다 · 확인
- [2026-09-22] 프로젝트 탭의 `지라에서 완료됨` 글자는 없앤다 — 띠 카드의 지라 상태가 같은 말을 한다 · 확인
- [2026-09-22] 직접 만든 프로젝트에 지라 티켓 하나를 수동으로 연결할 수 있다(번호나 주소로 찾아 미리 본 뒤 연결). 프로젝트 이름과 항목은 그대로 두고 연결 표시만 붙인다 — 항목을 지라 프로젝트로 옮겨 쓰면 이름이 바뀌고 되돌리기 어렵다. 하나로 시작하고 여러 개는 필요해지면 · 확인
- [2026-09-22] 지라 띠 카드에서 하위 티켓을 펼쳐 티켓별 지라 상태와 담당자를 본다(접힌 줄에는 미완료의 담당별 개수). 읽기 전용이고 줄을 누르면 지라가 열린다 — 하위 티켓까지 앱에서 바꾸기 시작하면 지라를 다시 만드는 일이 된다 · 확인
- [2026-09-22] 배포가 3일 안(또는 지났는데 미배포)인 프로젝트에 열린 항목이 있으면 리마인드 카드가 알린다. 프로젝트를 열어야만 배포일이 보여서 놓칠 수 있었다. 주간요약의 슬랙 글에는 원할 때만 지라 상태·배포 버전을 붙인다(기본 끔) · 확인
- [2026-09-22] 직접 만든 프로젝트에 연결할 티켓은 완료한 내 티켓(최근 90일)도 고를 수 있다 — 끝난 티켓을 프로젝트 기록으로 걸어 두는 경우가 있다 · 확인
- [2026-09-22] 열린 항목이 없고 14일 넘게 조용한 프로젝트와 직접 보관한 프로젝트는 왼쪽 목록의 '지난 프로젝트'로 내린다. 지라 프로젝트는 티켓이 끝나면 저절로 빠지지만 직접 만든 프로젝트는 쌓이기만 했다. 보관은 화면 배치만 바꾸고 기록·주간요약·검색은 그대로다 · 확인
- [2026-09-22] 끝난 회의의 미팅 노트를 아직 안 가져왔으면 리마인드 카드가 알린다(오늘 것만). 슬랙 수집의 처리 대장은 설정 > 상태에서 바로 읽는다 — 로그를 열지 않고도 무엇이 왜 안 들어왔는지 보이게 · 확인
- [2026-09-22] 회의에서 담은 항목의 종류는 같은 id로 파일을 옮겨 바꾼다 — 회의 연결과 검토 기록이 끊기지 않게 · 확인
- [2026-09-22] 결정에는 본문(내용) 칸이 있다 — 정책 결정은 한 줄로 끝나지 않는다. 앱 파일(.workflow.json)에 두고 decisions.md는 건드리지 않는다 · 확인
- [2026-09-22] 회의 목록은 날짜순과 프로젝트별 두 보기로 훑는다(드롭다운 거르기 대신 접는 소제목). 회의에서 나온 것은 종류 소제목으로, 여러 프로젝트가 섞였을 때만 프로젝트로 한 번 더 나눈다 — 한 회의에서 여러 프로젝트 일이 나오는 부서 간 회의가 있다 · 확인
- [2026-09-22] 삭제한 항목은 설정에서 보고 되살린다 — 확인 없는 삭제를 시간이 지나 알아차리면 되돌릴 길이 없었다. 자동 영구 삭제는 여전히 하지 않는다 · 확인
- [2026-09-22] 직접 만든 프로젝트의 이름은 그 프로젝트에 속한 모든 기록(항목·회의·연결·보관·주간요약)을 한 트랜잭션으로 함께 바꾼다 — 반쯤 바뀐 상태를 남기지 않는다. 지라 프로젝트의 이름은 지라 요약을 따른다 · 확인
- [2026-09-22] 새 프로젝트를 만들 때 직군을 고르면 앱이 지라에 에픽과 직군별 하위 티켓을 만든다(있는 에픽에 붙일 수도). 담당자는 비워 두고 사람이 정한다. 만들 목록을 전부 보여 주고 확인을 받으며 ⌘Z 대상이 아니다 — 지라에 여러 이슈를 만드는 일은 되돌릴 수 없다 · 확인
- [2026-09-22] 새로 만든 지라 에픽은 만든 사람(나)에게 자동 배정한다 — 그래야 프로젝트 목록에도 바로 뜨고, 담당자를 고르는 절차(2차)가 필요 없어진다. 하위 티켓은 배정하지 않는다(직군별로 다른 사람에게 갈 수 있어 사람이 나중에 따로 지정, 지금처럼 담당 없음). 있는 에픽에 붙일 때는 그 에픽도 재배정하지 않는다(남의 에픽일 수 있다) — 위 줄의 "담당자는 비워 두고 사람이 정한다"를 새 에픽에 한해 뒤집음 · 확인
- [2026-09-22] 하루 마감은 '오늘 정리' 창에서 남은 오늘 할 일에 내일/나중에/완료를 한 번에 찍고 기존 일괄 저장 길로 저장한다. 기본은 전부 '그대로'다 — 앱이 미룰 것을 추측하지 않는다 · 확인
- [2026-09-22] 안 가져온 미팅 노트는 리마인드 카드에 올리지 않고 회의 탭에서만 알린다 — 노트를 쓰지 않는 회의도 많아 매번 알리면 소음이었다(앞의 '리마인드 카드가 알린다' 결정을 뒤집음) · 확인
- [2026-09-22] 내 반응이 필요한 것(1차 지라 댓글)은 오늘 탭 맨 위 '반응 필요' 구역에 모으고, 내가 답하면 스스로 사라진다 — 메일 알림 대신 직접 연동을 택한 이유가 이것이다. 0개면 구역이 없다 · 확인
- [2026-09-22] 검색 결과에서 할 일·확인 대기를 그 자리에서 완료하거나(체크박스) 할 일은 오늘로 옮길 수 있다(오늘로 글자 버튼) — 결과를 닫고 다시 찾아가지 않아도 된다 · 확인
- [2026-09-22] 프로젝트 "보관"(수동, 저장됨)을 없애고 "조용함"(자동, 매번 재계산) 판정 하나로 합친다 — 화면에서 둘이 똑같이 보여 사용자가 구분하지 못했다. 이제 열린 항목이 없고 14일 넘게 조용하면 자동으로 지난 프로젝트로, 업무가 생기면 자동으로 되돌아온다. 사람이 영구히 숨기는 길은 없다(2026-09-22의 "직접 보관한 프로젝트는..." 결정을 뒤집음) · 확인
- [2026-09-23] 주간요약에서 여러 문장을 골라 사람이 지은 요약 한 줄 아래로 모으고 접을 수 있다 — 접힌 묶음은 슬랙·문서에 한 줄만 나가고 원문은 근거로 남는다. 앱이 "짜잘함"을 판단해 자동으로 접지는 않는다 · 확인
- [2026-09-23] 지라 프로젝트는 앱 안 별칭을 가질 수 있다 — 기본은 지라 요약, 별칭이 있으면 앱 안 어디서나(슬랙 복사 포함) 그 이름이고 지라 원래 이름은 제목 아래 줄에 늘 보인다. 지라 요약을 앱에서 고쳐 쓰지는 않는다 · 확인
- [2026-09-23] 앱은 이 저장소 그대로 배포한다(clone + setup.sh, 데이터는 gitignore) — 새 저장소·빌드 없음. 동료는 태그(stable)만 받고 나는 main을 받는다. 업데이트는 백업 → 받기 → 변환 → 재시작 → 점검 순서이고 실패하면 이전 코드와 백업 데이터로 되돌린다 · 확인
- [2026-09-23] 연동 설정은 앱의 설정 > 연동에서 하나씩 켠다(설치 때 묻지 않는다). 이 저장만 `workspace.config.json`을 쓴다(아는 키만, 토큰은 파일로) — 동료는 JSON을 열지 않는다 · 확인
- [2026-09-23] 새 버전 안내는 하되 앱이 스스로 업데이트하지 않는다(업데이트.command) — 스크립트가 검증된 뒤 앱 안 버튼을 검토한다 · 확인
- [2026-09-23] 직접 만든 프로젝트는 지라 에픽으로 통째로 옮길 수 있다(항목·회의·주간요약 소속을 한 트랜잭션으로). 옮기면 옛 프로젝트는 저절로 사라지고, 알림의 되돌리기로만 되돌린다(화면에 따로 표시하지 않음). 앱이 짝을 추측해 자동으로 옮기지 않으며, 같은 이름일 때만 만들 때·빈 에픽을 열 때 한 번 묻는다 · 확인
- [2026-09-24] 슬랙 연결은 "쓸 곳을 먼저 고르고 채널을 앱이 한 번에 만들어 주는" 길 하나로 한다 — 이미 있는 채널 링크를 붙이는 갈래는 없앤다(사용자 결정). 채널은 ID로 추적해 슬랙에서 이름을 바꿔도 이어지고, 표시 이름은 앱이 따라간다 · 확인
- [2026-09-24] 캘린더는 Claude 없이 **비밀 주소(iCal)를 앱 서버가 직접 읽는 갈래**를 먼저 둔다(Claude Code 갈래는 그대로 남김). 비밀 주소는 토큰과 같은 급이다 — 0600 파일에만 두고 응답·로그·화면에 싣지 않는다. 직접 읽은 값이 있으면 `calendar_today.md`는 보지 않고(지라와 같은 우선순위) Claude로 읽는 `calendar-sync`는 등록하지 않는다. 종일 일정은 오늘 미팅에 넣지 않는다(회의가 아니다) · 확인
- [2026-09-24] 내 앱 꾸미기(Dock 아이콘·Dock 이름·워크스페이스 제목)는 **개인별**이다 — 그림은 `local/icon.png`, 이름은 설정의 `server.dockName`·`title` 두 칸에만 쓰고(연동 저장과 같은 방식으로 아는 키만) 프리셋은 두지 않는다. 앱 서버는 Dock 앱을 직접 만들지 않고 요청 표시 파일만 쓴다(launchd `app-refresh`가 만든다). **Dock은 다시 시작하지 않는다** — 바뀐 모습은 앱을 닫고 다시 열면 보인다 · 확인
- [2026-09-24] 첫 실행 `시작하기` 카드(기록이 0일 때만)를 `사용설명서` 카드로 바꾼다 — 닫기 전까지는 기록과 무관하게 늘 서고, 닫으면 설정 파일이 아니라 그 브라우저에만 기억하며, 같은 네 줄이 도움말 맨 위에 남는다 · 확인
- [2026-09-24] 연동 카드의 `지금 가져오기`에서 지라·캘린더(비밀 주소)는 서버가 메모리 보관함을 곧바로 다시 읽지만, 슬랙·캘린더(Claude)·티로는 **요청 표시 파일 + launchd**(`slack-capture-now`·`calendar-sync-now`·`tiro-sync`)로만 돈다 — 서버는 프로세스를 띄우지 않는다(미팅 노트 가져오기와 같은 원칙). launchd 파일이 없으면 쓰지 않고 `업데이트.command`를 안내하며, 같은 연동은 1분에 한 번이다 · 확인
- [2026-09-24] 설치 폴더가 회사(playio) 폴더 안이면 setup·update가 **묻지 않고** `~/workspace`로 옮긴다(선택지 없음 — 사용자 결정). 옮길 수 없으면(대상이 이미 있음·다른 디스크·mv 실패) 아무것도 바꾸지 않고 멈춘다. playio가 아니면 어디든(이름을 바꾼 폴더도) 허용하고, 바탕화면·문서·iCloud 아래면 경고만 한다 · 확인
