---
name: 워크스페이스
description: 종일 옆에 띄워 두는 1인용 업무 트래커 — 연회색 바탕 위 테두리 없는 큰 둥근 흰 카드, 파랑은 선택과 행동에만, 완료는 체크박스
colors:
  bg: "#f2f4f6"
  surface: "#ffffff"
  surface-line: "transparent"
  field: "#f2f4f6"
  hover: "#f4f6f8"
  rail-hover: "#f4f6f8"
  border: "#e3e7eb"
  hair: "#eef1f4"
  check-line: "#8b95a1"
  text: "#191f28"
  muted: "#4e5968"
  dim: "#636c78"
  accent: "#3182f6"
  accent-strong: "#1b64da"
  accent-text: "#1b64da"
  accent-soft: "#e8f3ff"
  on-fill: "#ffffff"
  sel: "#eef5ff"
  sel-hover: "#e6f1ff"
  urgent: "#c9252d"
  urgent-bg: "#ffeceb"
  warn: "#b0560a"
  warn-bg: "#fff4e5"
  success: "#0d7a4a"
  success-bg: "#e6f6ee"
  neutral-bg: "#eef1f4"
  mark: "#ffe7a3"
  warm: "#fffaf2"
  warm-hover: "#fff4e4"
  prog-track: "#e9edf2"
  pj-0: "#4f83e8"
  pj-1: "#2f9b87"
  pj-2: "#c0842b"
  pj-3: "#8a6ed2"
  pj-4: "#cd6579"
  pj-5: "#5d9b47"
  scrim: "rgba(25,31,40,.16)"
  bg-dark: "#15181e"
  surface-dark: "#1e222a"
  surface-line-dark: "#2b313b"
  field-dark: "#262b34"
  hover-dark: "#272c35"
  border-dark: "#333a45"
  hair-dark: "#2b313b"
  check-line-dark: "#636e7d"
  text-dark: "#eaedf1"
  muted-dark: "#a8b1bd"
  dim-dark: "#8b95a1"
  accent-dark: "#4b91f1"
  accent-strong-dark: "#4b91f1"
  accent-text-dark: "#7aa9ff"
  accent-soft-dark: "#1b2a44"
  on-fill-dark: "#0b1020"
  sel-dark: "#1c2533"
  sel-hover-dark: "#212c3d"
  urgent-dark: "#ff8f8f"
  urgent-bg-dark: "#3a2022"
  warn-dark: "#ffbe6b"
  warn-bg-dark: "#3a2e1b"
  success-dark: "#6ddba0"
  success-bg-dark: "#1c3729"
  neutral-bg-dark: "#2a2f38"
  mark-dark: "rgba(255,190,107,.3)"
  warm-dark: "#26221b"
  warm-hover-dark: "#2e2820"
  prog-track-dark: "#2b313b"
  pj-0-dark: "#7aa2ff"
  pj-1-dark: "#57c6ad"
  pj-2-dark: "#dfa85c"
  pj-3-dark: "#b198ff"
  pj-4-dark: "#ef92a0"
  pj-5-dark: "#8ccb72"
  scrim-dark: "rgba(0,0,0,.5)"
typography:
  font:
    fontFamily: "'Pretendard Variable', Pretendard, -apple-system, BlinkMacSystemFont, 'Apple SD Gothic Neo', 'Noto Sans KR', 'Malgun Gothic', 'Segoe UI', sans-serif"
  project-title:
    fontSize: "24px"
    fontWeight: 700
    letterSpacing: "-0.025em"
  doc-title:
    fontSize: "22px"
    fontWeight: 700
    letterSpacing: "-0.02em"
  brand:
    fontSize: "20px"
    fontWeight: 700
    letterSpacing: "-0.02em"
  detail-title:
    fontSize: "20px"
    fontWeight: 700
    lineHeight: "29px"
    letterSpacing: "-0.02em"
  list-title:
    fontSize: "19px"
    fontWeight: 700
    letterSpacing: "-0.015em"
  section-title:
    fontSize: "18px"
    fontWeight: 700
    letterSpacing: "-0.015em"
  card-title:
    fontSize: "16px"
    fontWeight: 700
    letterSpacing: "-0.015em"
  row-title:
    fontSize: "16px"
    fontWeight: 500
    lineHeight: "25px"
    letterSpacing: "-0.012em"
  doc-body:
    fontSize: "15.5px"
    fontWeight: 400
    lineHeight: 1.65
  tab:
    fontSize: "15px"
    fontWeight: 500
  group-title:
    fontSize: "14.5px"
    fontWeight: 600
  body:
    fontSize: "14px"
    fontWeight: 400
    lineHeight: 1.55
  button:
    fontSize: "13.5px"
    fontWeight: 600
  meta:
    fontSize: "13px"
    fontWeight: 500
    fontFeature: "tabular-nums"
  chip:
    fontSize: "12.5px"
    fontWeight: 600
rounded:
  r: "20px"
  r-sm: "12px"
  r-xs: "10px"
  row: "14px"
  rail-row: "12px"
  menu: "18px"
  toast: "16px"
  badge: "6px"
  check: "6px"
  pill: "999px"
spacing:
  "2": "2px"
  "4": "4px"
  "6": "6px"
  "8": "8px"
  "10": "10px"
  "12": "12px"
  "14": "14px"
  "16": "16px"
  pad: "20px"
  "22": "22px"
  doc-gutter: "32px"
sizes:
  hdr: "60px"
  row: "54px"
  grp: "36px"
  rail-w: "296px"
  rail-bleed: "20px"
  wp: "104px"
  wm: "184px"
shadows:
  soft: "0 1px 3px rgba(25,31,40,.04), 0 8px 24px rgba(25,31,40,.06)"
  shadow: "0 1px 2px rgba(25,31,40,.06)"
  pop: "0 12px 36px rgba(25,31,40,.14), 0 2px 8px rgba(25,31,40,.06)"
  drawer-sh: "-10px 0 30px rgba(25,31,40,.07)"
  soft-dark: "0 1px 3px rgba(0,0,0,.4), 0 8px 24px rgba(0,0,0,.3)"
  pop-dark: "0 12px 36px rgba(0,0,0,.55), 0 2px 8px rgba(0,0,0,.35)"
motion:
  ease: "cubic-bezier(.22,.8,.3,1)"
components:
  card:
    backgroundColor: "{colors.surface}"
    borderColor: "{colors.surface-line}"
    rounded: "{rounded.r}"
    shadow: "{shadows.soft}"
    padding: "10px"
  button-primary:
    backgroundColor: "{colors.accent-strong}"
    textColor: "{colors.on-fill}"
    rounded: "{rounded.r-sm}"
    height: "42px"
    padding: "0 20px"
  button-secondary:
    backgroundColor: "{colors.accent-soft}"
    textColor: "{colors.accent-text}"
    rounded: "{rounded.r-xs}"
    height: "33px"
    padding: "0 13px"
  button-tertiary:
    backgroundColor: "{colors.neutral-bg}"
    textColor: "{colors.muted}"
    rounded: "{rounded.r-xs}"
    height: "34px"
    padding: "0 13px"
  button-danger:
    backgroundColor: "transparent"
    textColor: "{colors.urgent}"
    rounded: "{rounded.r-xs}"
    height: "34px"
  field:
    backgroundColor: "{colors.field}"
    textColor: "{colors.text}"
    rounded: "{rounded.r-sm}"
    height: "30px"
    padding: "0 10px"
  quick-add:
    backgroundColor: "transparent"
    rounded: "{rounded.pill}"
    height: "48px"
    padding: "0 20px"
  checkbox:
    backgroundColor: "{colors.surface}"
    borderColor: "{colors.check-line}"
    rounded: "{rounded.check}"
    width: "22px"
    height: "22px"
  checkbox-checked:
    backgroundColor: "{colors.accent}"
    textColor: "{colors.on-fill}"
  badge:
    rounded: "{rounded.badge}"
    height: "23px"
    padding: "0 8px"
  count-chip:
    backgroundColor: "{colors.neutral-bg}"
    textColor: "{colors.muted}"
    rounded: "{rounded.pill}"
    height: "22px"
    padding: "0 9px"
  task-row:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.row}"
    height: "{sizes.row}"
    padding: "0 {spacing.pad}"
  task-row-hover:
    backgroundColor: "{colors.hover}"
  task-row-selected:
    backgroundColor: "{colors.sel}"
    textColor: "{colors.accent-text}"
  header:
    backgroundColor: "{colors.surface}"
    height: "{sizes.hdr}"
    padding: "0 18px 0 24px"
  rail:
    backgroundColor: "transparent"
    width: "{sizes.rail-w}"
  detail-card:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.r}"
    width: "360px"
    widthWide: "460px"
    shadow: "{shadows.pop}"
  later-drawer:
    backgroundColor: "{colors.bg}"
    width: "404px"
  overflow-menu:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.menu}"
    width: "304px"
    padding: "6px"
    shadow: "{shadows.pop}"
  search-palette:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.r}"
    width: "620px"
    shadow: "{shadows.pop}"
  toast:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.toast}"
    padding: "12px 12px 12px 18px"
    shadow: "{shadows.pop}"
  select-bar:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.r}"
    maxWidth: "1120px"
    shadow: "{shadows.pop}"
  settings-dialog:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.r}"
    width: "620px"
    shadow: "{shadows.pop}"
  report-doc:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.r}"
    padding: "28px 32px 48px"
    maxWidth: "720px"
  slack-preview:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.r}"
    padding: "18px 20px 20px"
---

# Design System: 워크스페이스

이 문서의 값은 전부 지금 앱의 코드(`tracker/inbox-app/ui.css`, `report-ui.css`, `app.js`)에서 읽었다. 앞선 D안 초안은 폐기했다 — 사용자가 시안 여섯 벌을 비교한 뒤 고른 **확정 룩**이 기준이다. 토큰 값은 맨 위 frontmatter가 기준이고, 아래 표는 같은 값을 역할과 함께 다시 적은 것이다. 제품 차원의 결정(무엇을 보여 줄지)은 `DECISIONS.md`에 있고, 이 문서는 **어떻게 보이게 할지**만 다룬다.

## Overview

**북극성 — 사용자의 말 그대로:** "동글동글하고, 색이 좀 있고, 글씨 간의 여유가 있는 것이 좋다" · "토스/flex 느낌이 나면 좋겠다" · "전체적으로 괜찮은데 약간 복잡스럽다" · "완료 표시는 체크박스가 맞다".

이 네 마디가 모든 판단의 기준이다. **동글동글**은 20px 카드와 14px 줄 모서리로, **색**은 연회색 바탕 위에 뜬 흰 면과 프로젝트 색 점으로, **여유**는 54px 줄과 16px 카드 간격으로, **복잡하지 않게**는 "급한 것만 배지, 나머지는 회색 글자"로 지킨다.

한 문장으로: **연회색 바탕 위에 테두리 없는 큰 둥근 흰 카드가 떠 있고, 그 안에 여백으로 나뉜 줄이 쌓인다.** 실선으로 자르지 않고, 카드 안에 카드를 넣지 않는다.

**Key Characteristics**
- 60px 흰 헤더 + 296px 왼쪽 레일(바탕은 투명, 그 위에 흰 카드 셋) + 본문 카드
- 카드: `--surface` + 20px 모서리 + 테두리 없음 + `--soft` 그림자(다크에서만 hairline)
- 줄 54px, 줄 사이 실선 없음 — 여백과 14px 둥근 회색 hover로 나눈다
- 버튼 3단(파란 채움 / 연파랑 / 연회색), 정보는 버튼처럼 그리지 않는다
- 완료는 **둥근 네모 체크박스**(22px·6px), 켜지면 파란 채움 + 흰 체크
- 상태 배지는 **급한 것만** 6px 모서리로 세우고, 나머지는 아이콘 + 회색 글자
- 말투는 해요체(`내일로 미뤘어요`), 버튼·메뉴 이름은 짧은 명사형(`내일`)

## Colors

연회색 바탕(`--bg`)과 흰 면(`--surface`)의 두 단계, 그 위에 파랑 하나와 의미 색 셋. 색이 한 화면에 네 계통(파랑·주황·빨강·초록)을 넘지 않는다.

### Primary — 파랑은 채움과 글자를 나눈다

| 토큰 | 라이트 | 다크 | 허용 역할 |
|---|---|---|---|
| `--accent` | `#3182f6` | `#4b91f1` | **글자를 얹지 않는** 파랑: 체크박스 채움, 입력 초점 테, `:focus-visible` 외곽선, 탭 밑줄, 오늘 카드 윗변 진행 선 |
| `--accent-strong` | `#1b64da` | `#4b91f1` | **흰 글자를 얹는** 채움 버튼(primary, 2차 버튼의 hover) |
| `--accent-text` | `#1b64da` | `#7aa9ff` | 파란 글자: 선택된 줄 제목, 글자 링크, 2차 버튼 글자, 진행 중인 회의의 시각·제목 |
| `--accent-soft` | `#e8f3ff` | `#1b2a44` | 2차 버튼 바탕, 오늘 할 일 개수 칩, 주차 목록의 `새 기록 N` 칩, 팔레트에서 고른 종류 칩 |
| `--sel` / `--sel-hover` | `#eef5ff` / `#e6f1ff` | `#1c2533` / `#212c3d` | 고른 줄의 바탕(불투명해야 한다 — 겹쳐 뜨는 동작 묶음이 같은 값을 쓴다) |
| `--on-fill` | `#ffffff` | `#0b1020` | 채움 위 글자와 체크 표시 |

> `#3182f6` 위의 흰 글자는 3.71:1로 미달이다. 그래서 **채움(`--accent`)과 흰 글자를 얹는 채움(`--accent-strong`)을 나눴다.** 이 둘을 다시 합치지 않는다.

### Secondary — 뜻 있는 색(글자값 / 틴트 바탕이 한 쌍)

| 토큰 | 라이트 | 다크 | 허용 역할 |
|---|---|---|---|
| `--urgent` / `--urgent-bg` | `#c9252d` / `#ffeceb` | `#ff8f8f` / `#3a2022` | `긴급` · `기한 N일 지남` · 확인 대기의 `N일 늦음` 배지, 삭제 버튼 글자, 자동화 실패 배지 |
| `--warn` / `--warn-bg` | `#b0560a` / `#fff4e5` | `#ffbe6b` / `#3a2e1b` | `오늘까지` 배지, `중요`(배경 없는 글자), 새로 들어온 것·확인 대기의 개수 칩 |
| `--success` / `--success-bg` | `#0d7a4a` / `#e6f6ee` | `#6ddba0` / `#1c3729` | `답변 왔어요` 글자, 회의 담기 결과 카드의 면 |
| `--neutral-bg` | `#eef1f4` | `#2a2f38` | 중립 개수 칩, 3차 버튼, 세그먼트 트랙, 날짜 칩 |
| `--mark` | `#ffe7a3` | `rgba(255,190,107,.3)` | 검색어 일치 부분(글자색은 `--text`) |

### Neutral

| 토큰 | 라이트 | 다크 | 허용 역할 |
|---|---|---|---|
| `--bg` | `#f2f4f6` | `#15181e` | 페이지 바탕, 레일 바탕, 서랍 판 |
| `--surface` | `#ffffff` | `#1e222a` | 모든 카드·패널·메뉴·팔레트·모달·알림 |
| `--surface-line` | `transparent` | `#2b313b` | 카드 테두리 — **라이트에서는 없다**(다크에서만 hairline) |
| `--field` | `#f2f4f6` | `#262b34` | 입력칸 채움 |
| `--hover` / `--rail-hover` | `#f4f6f8` | `#272c35` | 줄 hover |
| `--border` | `#e3e7eb` | `#333a45` | 컨트롤 테두리, 근거 업무 들여쓰기 선, 취소선 색 |
| `--hair` | `#eef1f4` | `#2b313b` | 헤더 밑선, 패널 구역선, 문서 구분선 |
| `--check-line` | `#8b95a1` | `#636e7d` | 꺼진 체크박스 테두리 |
| `--text` | `#191f28` | `#eaedf1` | 제목·본문 |
| `--muted` | `#4e5968` | `#a8b1bd` | 보조 글자, 3차 버튼 글자, 배지 안 중립 글자 |
| `--dim` | `#636c78` | `#8b95a1` | 가장 조용한 글자 — 상태말, 개수 숫자, placeholder, 0건 프로젝트 |
| `--warm` / `--warm-hover` | `#fffaf2` / `#fff4e4` | `#26221b` / `#2e2820` | `새로 들어온 것` 카드(아주 옅은 따뜻한 면) |
| `--prog-track` | `#e9edf2` | `#2b313b` | 오늘 카드 윗변 진행 선의 트랙 |
| `--scrim` | `rgba(25,31,40,.16)` | `rgba(0,0,0,.5)` | 팔레트·설정 뒤 가림막 |

테마 전환: 기본은 `prefers-color-scheme`, `:root[data-theme="dark|light"]`가 있으면 그것이 이긴다. 컴포넌트 규칙에는 색 값을 직접 적지 않는다. 토큰 밖 색을 쓰는 곳은 스크롤바 thumb(`#cbd0d8` / 다크 `#3a4150`) 하나뿐이다.

### 프로젝트 색 (여섯 개)

`--pj-0…5`(`#4f83e8 #2f9b87 #c0842b #8a6ed2 #cd6579 #5d9b47`, 다크는 한 단계 밝게)를 **이름 해시 % 6**으로 고른다(`uiProjectHue`). 같은 프로젝트는 그룹 열쇠(`group:가입_개선`)로 오든 이름(`가입 개선`)으로 오든 같은 색이다 — `uiProjectKey`가 접두사·대소문자·공백/`_`/`-`를 먼저 지운다. **겹침을 피하지 않는다**: 프로젝트가 하나 늘었다고 다른 프로젝트의 색이 밀리면 안 된다.

프로젝트 표기는 `uiProjectName(item, opts)` 하나가 만든다(BKEY 결정, 2026-09-24). 기본(옵션 없음)은 **요약만**(모르면 키) — 목록·그룹 제목·프로젝트 탭·주간요약·팔레트 등 바깥 화면은 전부 이 기본을 쓴다. `{ withKey: true }`는 상세 카드의 값·프로젝트 탭 오른쪽의 `열린 항목 N` 줄(`열린 항목 2 · IO-48394`)·title 툴팁의 `키 · 요약`, `{ picker: true }`는 프로젝트 고르는 목록의 `요약 · 키`(정렬은 요약 기준). 그룹 열쇠(`jira:…`/`group:…`) 문자열에서 같은 규칙을 쓰려면 `uiGroupLabel(key, opts)`를 쓴다. 같은 요약을 가진 지라 이름이 한 목록에 둘 이상 그룹 제목으로 나오면 그때만 키로 구분하며, 이 판단은 `uiGroupLabels(keys)`(app.js) / `reportGroupTitles(names)`(report-ui.js, 저장 문자열 `키 · 요약`에서 판단) 한 곳에서만 한다. 색 점의 키는 표기와 분리돼 있다(`uiProjectColorKey`가 늘 `item.jira`/`group`/`project` 원값을 쓴다) — 표기가 무엇을 보여 주든 색은 바뀌지 않는다.

색 점(7px 원)을 찍는 자리는 넷뿐이다 — ① 오늘 목록의 프로젝트 그룹 제목 ② 제목 뒤 `· ● 이름` 표기 ③ 프로젝트 탭의 왼쪽 목록 ④ ⌘K 결과 줄의 제목 뒤.

### Named Rules

**The Blue Means Go Rule.** 파랑은 "지금 선택된 것"과 "누르면 실행되는 주 행동"에만 쓴다. 정보·장식·아이콘 기본색에 파랑을 쓰지 않는다. 화면에서 파란 것을 보면 선택돼 있거나 누를 수 있어야 한다.

**The Badge Is For Urgency Rule.** 틴트 바탕을 깐 배지는 **급한 말**에만 쓴다: `긴급` · `기한 N일 지남` · `오늘까지` · 확인 대기의 `N일 늦음`·`오늘 답변 예정` · 회의의 `초안 N` · 자동화의 `N분 전 실패`. 나머지 상태말(`중요`, `어제에서 밀림`, `진행 중`, `답변 기다리는 중`, 먼 기한)은 아이콘 + 회색 글자다.

**The Four Hues Rule.** 한 화면에 동시에 보이는 색 계통은 파랑·주황·빨강·초록 넷을 넘지 않는다. 다섯 번째 색이 필요하면 그건 정보를 줄여야 한다는 신호다.

**The Jira Card Says Jira Rule.** 지라에서 읽어 온 값은 내 업무의 값과 섞이지 않게 **한 카드 안에 모으고 이름에 `지라`를 붙인다**(`지라` 표시 · `지라 상태` · `지라의 배포 버전` · `지라에서 열기`). 그 카드 안에서 색이 붙는 자리는 둘뿐이다 — 상태는 **범주로만**(완료 `--success` 글자 · 할 일 `--dim` · 진행 `--accent-text` 파랑, 배지가 아니다), 배포일은 **3일 안이면 `--warn`, 지났는데 미배포면 `--urgent`**(그 밖에는 색이 없다). 상태를 바꾸는 드롭다운의 선택지 글씨도 같은 범주 색이다(회색은 hover·키보드 초점에서 `--text`로 진해져 비활성처럼 보이지 않는다). 색만으로 말하지 않게 글자도 함께 달라진다(`· 2일 남음` / `· 3일 지남` / `· 배포함`). 하위 티켓도 이 카드 **안**에서 펼친다 — 지라 줄이 내 업무 목록과 섞여 서지 않게, 그리고 그 목록은 읽기만 하는 자리라는 것이 카드 경계로 드러나게.

## Typography

**Body Font:** `"Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo", "Noto Sans KR", "Malgun Gothic", "Segoe UI", sans-serif`

Pretendard Variable(v1.3.9, SIL OFL 1.1)은 **앱에 넣어 두고 서버가 직접 준다**(`/fonts/PretendardVariable.woff2`, `font-display: swap`). 외부에서 받아오지 않는다. 라이선스는 `tracker/inbox-app/fonts/Pretendard-LICENSE.txt`.

기본은 14px / 1.55, `word-break: keep-all`(한글은 띄어쓰기에서 줄을 바꾼다), `-webkit-font-smoothing: antialiased`. 숫자가 세로로 맞아야 하는 곳(개수·기한·시각·날짜)은 `.num` = `font-variant-numeric: tabular-nums`.

### Hierarchy

| 역할 | 크기 / 굵기 / 줄높이 / 자간 | 쓰는 곳 |
|---|---|---|
| project-title | 24 / 700 / -0.025em | 프로젝트 탭의 프로젝트 이름 |
| doc-title | 22 / 700 / -0.02em | 주간요약 문서 제목(`이번 주`) |
| brand · detail-title | 20 / 700 / -0.02em (줄높이 29px) | 앱 이름. 상세 카드의 제목은 한 단계 작은 17 / 700 / 25px |
| list-title | 19 / 700 / -0.015em | `오늘 할 일`, `아이디어`, `결정` 머리줄 |
| section-title | 18 / 700 / -0.015em | `새로 들어온 것` |
| card-title | 16 / 700 / -0.015em | 레일 카드 제목(`오늘 미팅`), 문서의 프로젝트 소제목, 프로젝트 탭 구역 제목(17px) |
| row-title | 16 / 500 / 25px / -0.012em | 업무 줄 제목, 새로 들어온 줄 제목, 프로젝트 탭 줄 제목 |
| doc-body | 15.5 / 400 / 1.65 | 주간보고 문장 — 이 화면에서 가장 큰 본문 |
| tab | 15 / 500 (선택 700) | 헤더 탭 |
| group-title | 14.5 / 600 | 목록 안 그룹 제목(`가입 개선`) |
| body | 14 / 400 / 1.55 | 본문, 메뉴 항목, 알림, 레일 줄 제목(14.5) |
| button | 13.5 / 600 | 버튼·세그먼트 글자(작은 버튼 13) |
| meta | 13 / 500 / tabular | 상태말, 개수 숫자, 조용한 글자 버튼 |
| chip | 12.5 / 600 | 개수 칩, 상태 배지, `원문` 링크(500) |

입력칸 글자: 빠른 추가 15px, 팔레트 입력 17px, 다음 주 계획 15px, 상세 입력 14px, 초안 문구 14px. 520px 이하의 입력은 iOS 확대를 막으려고 16px로 올린다.

### Named Rules

**The 700 Is A Heading Rule.** 굵기 700은 제목에만 쓴다(위 표의 project/doc/brand/detail/list/section/card-title, 레일 미팅 시각, 슬랙 미리보기의 제목·구역·프로젝트 줄). 선택된 것·진행 중인 줄은 600까지, 부가 정보는 최대 500이다.

**The Title Is The Loudest Rule.** 한 줄에서 가장 크고 진한 것은 제목(16px `--text`)이다. 옆의 정보는 전부 13px `--dim` 이하이고, 의미가 있을 때만 색이 붙는다. 완료한 줄은 제목이 `--muted` 400 + 취소선(선 색 `--border`)으로 내려간다.

**The Numbers Line Up Rule.** 개수·날짜·시각·기한에는 `.num`을 붙인다. 숫자가 흔들리면 목록이 지저분해 보인다.

## Spacing · Shapes · Elevation

**간격.** 2px 눈금에서 2·4·6·8·10·12·14·16·20·22·28·32만 쓴다. 고정 값: 카드 안쪽 여백 10px, 줄 좌우 여백 `--pad` 20px, 줄 열 간격 12px, 카드 사이 16px, 문서 안쪽 여백 28/32px, 레일 카드 안쪽 10px.

**치수.** `--hdr` 60px · `--row` 54px · `--grp` 36px · `--rail-w` 296px · `--rail-bleed` 20px · `--wp` 104px(프로젝트 열) · `--wm` 184px(상태 열). 자리를 고정한 줄(`.d-row.is-cols` — 오늘 목록·서랍·마감·중요도순)은 비어 있는 프로젝트 열을 접고(`--wp: 0`) 상태 열을 `--wm` 306px로 쓴다(≤1280은 268px).

**모서리.** 네 단계다: 카드·모달·팔레트·막대 `--r` 20px, 입력칸·세그먼트·메뉴 항목 `--r-sm` 12px, 버튼 `--r-xs` 10px, 줄 hover 14px(레일 줄 12px). 알약(999px)은 **개수 칩·날짜 칩·필터 칩·빠른 추가 칸**에만 허락된다. 상태 배지는 알약이 아니라 **6px**이다 — 버튼과 확실히 달라야 한다. 체크박스도 6px.

**그림자.** 테두리 대신 그림자가 카드를 띄운다.
- `--soft` (`0 1px 3px rgba(25,31,40,.04), 0 8px 24px rgba(25,31,40,.06)`): 모든 카드·패널·문서.
- `--shadow` (`0 1px 2px …`): 세그먼트의 흰 손잡이 하나.
- `--pop` (`0 12px 36px …, 0 2px 8px …`): 떠 있는 것 — 줄 옆 카드(상세), 더보기 메뉴, ⌘K 팔레트, 설정 모달, 알림, 여러 개 선택 막대.
- `--drawer-sh` (`-10px 0 30px …`): 나중에 할 일 서랍.

**아이콘.** 14px 인라인 SVG 한 벌(`uiIcon`)만 쓴다 — `viewBox="0 0 16 16"`, `stroke: currentColor`, 굵기 1.5, 끝 둥글게, 채움 없음. 이모지·글리프 문자는 쓰지 않는다. 있는 아이콘: `search more close plus check chevron link refresh gear calendar clock chat priHigh priTop`. `priHigh`·`priTop`은 업무 체크박스 안에 겹치는 위 꺾쇠(굵기 2.2)다 — 줄에 붙는 상태말이 아니다.

### Named Rules

**The One Card Rule.** 카드(`--surface` + 20px + `--soft`) 안에 또 카드를 넣지 않는다. 카드 안의 구분은 여백과 그룹 제목, 아주 가끔 `--hair` 한 줄로만 한다. 서랍과 프로젝트 목록도 "바탕색 판 + 그 안의 흰 카드"이지 카드 속 카드가 아니다.

**The No Line Between Rows Rule.** 목록 줄을 실선으로 자르지 않는다. 줄은 여백과 둥근 회색 hover로 나뉜다.

**The Only Floating Things Cast Pop Rule.** 붙어 있는 카드는 `--soft`, 다른 것 위에 떠서 잠깐 나타나는 것만 `--pop`을 쓴다.

## Layout

| 영역 | 값 |
|---|---|
| 헤더 | `--hdr`(기본 60px) `--surface`, 테두리 대신 `box-shadow: 0 1px 0 var(--hair)`, 여백 `0 18px 0 24px`, 간격 16px. 왼쪽부터 앱 이름(20/700) → 날짜 회색 칩(28px, 999px) → 자동화 지연 표시(지금 실패 중일 때만) → 탭 → (오른쪽) 새로고침 · 검색(`⌘K`) · 설정(톱니바퀴). 520px 이하에서는 탭이 둘째 줄로 내려가고 `--hdr`가 100px이 된다 |
| 탭 | `오늘 \| 프로젝트 \| 회의 \| 아이디어·결정 \| 주간요약`. 15/500, 선택은 700 + 2.5px 파란 밑줄. 탭 이름 옆 작은 숫자(`d-tabnum`, 12.5px `--dim`)는 0이면 비운다 — `회의`는 검토할 초안이 있는 회의 수, `아이디어·결정`은 PRD에 아직 반영하지 않은 결정 수(결정 칸의 개수 칩과 같은 값·같은 title 문구, 한 곳에서만 말한다) |
| 페이지 | `.page` 여백 `16px 20px 48px`. 아이디어·결정 탭만 최대 1240px 가운데 정렬 |
| 오늘 탭 | grid `calc(296px - 20px) minmax(0,1fr)`. 레일은 `--rail-bleed` 20px만큼 페이지 왼쪽 여백을 되물어 창 끝까지 닿는다. 본문 열은 왼쪽 20px 안쪽 여백 + 최대 1120px |
| 왼쪽 레일 | 296px, 바탕 투명, 오른쪽 선 없음, `position: sticky; top: 60px`, 레일만 따로 스크롤. 그 위에 흰 카드 셋 — **오늘 미팅 / 리마인드 / 확인 대기**(카드 사이 16px). 요약 줄·큰 숫자 카드 없음 |
| 본문 | `새로 들어온 것`(따뜻한 카드) → 오늘 할 일 머리줄 → 미루기 제안 → 목록 카드(첫 줄이 빠른 추가) |
| 상세 | **누른 줄 옆에 뜨는 카드**(`position: fixed`, z 50). 목록은 상세가 열려도 전체 폭을 그대로 쓴다 — 상세를 위해 비워 두는 열은 없다. 폭 360px, 회의 정리는 460px. 누른 줄은 `--sel`로 표시된다. 자세한 규칙은 아래 `상세 (줄 옆 카드)` |
| 나중에 할 일 서랍 | 폭 404px, 헤더 아래 오른쪽에 붙어 열린다. 900px 초과에서는 본문을 덮지 않고 `padding-right: 424px`로 **밀어낸다**(160ms). 바탕은 `--bg` 판이고 그 안에 흰 카드 목록이 들어간다 |
| 프로젝트 탭 | grid `264px minmax(0,1fr)`. 왼쪽은 흰 카드 목록(줄 40px, 색 점 + 이름 + 오른쪽 묶음 `.rt` = (배포가 2주 안이면) 조용한 날짜 `.dp` + 회색 숫자, 0건은 `--dim`). `.dp`는 12.5/500 `tabular-nums`에 `min-width: 34px` 오른쪽 맞춤이라 **이름 칸이 먼저 줄고**(`9/30`), 색은 글자색뿐이다 — 3일 안 `--warn`, 지남 `--urgent`, 그 밖은 `--dim`(고른 줄에서는 숫자와 같이 `--accent-text`). title에 `v2.70.0 · 9월 30일 배포 예정`을 풀어 두고, ≤640px에서는 숨는다(띠 카드가 같은 말을 한다). **머리 바로 아래에 보기 전환 `상태별 | 배포별`**(`.d-seg.d-pfseg`, 아래 `세그먼트` 부품과 같음, 28px 칸)이 서고, 전체 프로젝트(지난 프로젝트 포함)가 **8개 이상**이면 그 아래 조용한 입력칸(`.d-pfind`, `.d-din` 그대로, 32px, 자리 전체) `프로젝트 찾기`가 붙는다 — 입력하는 동안은 목록의 그 뒤(소제목·줄)만 다시 그리고 칸 자체는 다시 만들지 않는다(한글 조합이 끊기지 않게). **상태별**(기본)은 소제목 없이 진행 중 줄이 먼저 오고, 시작 전이 하나라도 있을 때만 그 아래 접힌 소제목(`.d-plink`, 지난 프로젝트와 같은 부품) `시작 전 N`이 붙는다(지금 고른 프로젝트가 그 안에 있으면 자동으로 펼침). **배포별**은 버전마다 접이식 소제목(`.d-grp.tog`, 그룹 제목과 같은 모양)이 서고 `이름` 뒤에 색 글자 `.d-pdepdate`(주의 `--warn`·급함 `--urgent`, 배포일 없으면 글자 자체가 없음) + 개수가 붙으며, 배포일 이른 순 → 날짜 없는 버전 → 맨 끝 `배포 미정` 순이다(기본 모두 펼침, 세션 동안 접고 펼 수 있음). 머리의 `프로젝트 N`은 두 보기에서 같은 **지난 프로젝트를 뺀 전체 수**다(세그먼트를 바꿔도 숫자가 달라지지 않는다). 찾기 중에는 접힘 상태를 무시하고 소제목까지 전부 펼치며(`.d-plink`는 맞는 개수만 적은 글자로 `disabled` — `숨기기`가 뜻이 없다), 맞는 것이 없는 소제목은 그리지 않고 하나도 없으면 `.d-empty` 한 줄이다(Esc는 칸을 비우고 초점은 칸에 남긴다). 목록 끝에는 접힌 구역 `지난 프로젝트 N`이 온다: 조용한 글자 버튼(`.d-plink`) `지난 프로젝트 N`/`지난 프로젝트 숨기기`(하나도 없으면 버튼 없음, 세션 동안만 기억), 펼치면 같은 `.d-prow` 줄이 `.is-past`로 `--dim`·색 점 0.45 흐리게 이어 붙는다(`.is-zero`와 같은 흐림이다). 분류는 열린 항목이 없고 14일 넘게 조용한지 자동 판정뿐이라 사람이 접거나 꺼내는 버튼·권유 줄은 없다. 지금 고른 프로젝트는 구역이 바뀌어도 보는 동안 위 목록에 남는다. 오른쪽 제목 줄(`.d-ptitle`)은 그룹·지라 프로젝트 모두 이름 옆에 28px ⋯(`.d-more`, `--dim`)이 붙는다(BJALIAS로 지라에도 돌아왔다) — 그룹은 `이름 바꾸기` 하나, 지라는 별칭이 없으면 `이름 바꾸기`(별칭을 청하는 것 — 값·입력칸·Enter/Esc는 그룹과 같은 `projectRenameStart` 부품이고 현재 표시 이름이 전체 선택된 채로 연다) 하나이고 별칭이 있으면 그 아래 `지라 이름으로 되돌리기`(`alias:null`)가 더 붙는다. 지라 제목의 title 툴팁 `이름은 지라 요약을 따라요`는 별칭이 없을 때만 선다. 그 아래 조용한 줄은 늘 `열린 항목 N`으로 시작하고(지라면 `· KEY`), 지라 프로젝트에 별칭이 있으면 그 뒤에 `· 지라: 원래 요약`이 늘 붙는다(어긋남을 숨기지 않는다 — 보관/조용함 교훈, 길면 `.d-ptquiet`로 말줄임 + title에 전문). 목록 차례는 탭에 들어올 때·새로고침 때만 다시 정렬하고(`projectOrderKeys`), 그 사이 다시 그리는 것(체크 등)은 차례를 고정한다. 오른쪽은 제목(24/700) + 구역마다 흰 카드 + 열 이름 줄 + 54px 줄, 내용 최대 1000px. `진행할 업무` 줄은 `30px 60px minmax(0,1fr) 116px` = 체크 \| `언제 할지` \| 업무 \| `기한`이다 — **`우선순위` 열은 없다**(체크박스 안의 꺾쇠가 같은 말을 한다). 정렬은 그대로 마감·중요도순이다. 확인 대기·결정·아이디어·회의 구역의 줄(`.d-rec`)은 할 수 있는 일이 더보기뿐이라 늘 보이는 ⋯을 달고, 그 종류의 목록이 쓰는 메뉴를 그대로 연다. 확인 대기·결정 줄만 맨 앞에 체크 칸이 하나 더 붙는다(`.d-rec.has-ck`, `30px minmax(0,1fr) auto`) — 목록이 쓰는 체크박스(확인 대기 `.d-wcb`, 결정 `.d-check`+`.d-cb`) 그대로다. 확인 대기 구역은 미완료만 보여 주므로 체크하면 줄이 빠지고, 결정 구역은 반영 완료도 함께 보여 주므로 체크해도 줄은 남고 오른쪽 글자만 바뀐다. 아이디어 줄에는 원문이 있으면 제목 뒤에 조용한 `원문` 링크가 붙는다. 아이디어·회의 줄에는 체크 칸이 없다. **빈 에픽 옮기기 제안 줄**(BMOVE ②) — 지라 띠 카드 바로 아래, 그 지라 프로젝트에 항목이 0개이고 같은 이름의 그룹 프로젝트가 있을 때만 서는 조용한 한 줄로 지라 연결 줄과 같은 `.d-jline` 부품이다: `결제 리뉴얼 프로젝트의 항목 12개를 여기로 옮길까요?` + `.sep` + `옮기기`(`.d-link`) + `.sep` + `아니요`(`.d-link`). `옮기기`를 누르면 같은 자리가 위 옮기기 확인 줄(`.d-jconfirm`)로 바뀐다 |
| 지라 띠 카드 | 프로젝트 탭 제목·`열린 항목 N` 줄 아래, 첫 구역 위의 흰 카드 하나(`.d-jira`, `--r` 20px + `--soft`, 여백 `12px 16px`, 13.5px `--muted`). 첫 줄(`.top`, 간격 8px): 중립 표시 `지라`(20px, 6px 모서리, `--neutral-bg` + `--muted` 11.5/700 — 헤더 날짜 칩과 같은 계열의 읽기 전용 칩이다) + 요약(600 `--text`, 말줄임) + `종류 · 담당 ○○`(12.5 `--dim`) + `.sp` + `지라에서 열기 ↗`(13/600 `--accent-text`, 새 탭 + `rel="noopener noreferrer"`, **키는 이 링크의 title에만**) + 28px 새로고침 아이콘 버튼. 둘째 줄(`.cells`, 간격 `8px 28px`): 칸마다 라벨 위(`.lb` 12/600 `--dim`) · 값 아래(`.v` 14/600 `--text`) — `지라 상태` · `배포 버전` · `기한`. 셋째 줄(`.foot`, 위 1px `--hair`): `하위 티켓` + 84px 진행 막대(`--prog-track` 위 `--success`) + `12개 중 7개 완료`(12.5 `--dim`) + 미완료의 담당별 개수(`.who`) + `.sp` + 28px 꺾쇠 버튼(`.d-jexp`, `aria-expanded`, 펼치면 위로 돈다), 하위가 없으면 줄 자체가 없다. ≤640px에서는 첫 줄이 두 줄로 나뉜다(요약 먼저, 종류·담당과 링크가 아래). 부르는 동안은 같은 높이의 조용한 뼈대(`.is-loading`, `--neutral-bg` 막대, **움직이지 않는다**), 연결 안 됨·오류는 카드 대신 조용한 한 줄(`.d-jline` 13px `--dim` + `.d-link` `설정 방법`/`다시 시도`). 세 칸의 값은 **상세 카드와 같은 값 고르개**(`.d-dpick` 그대로 — 값 + 꺾쇠, hover `--hover`, 28px)이고 누르면 줄의 ⋯와 같은 고르개(`uiMenu`)가 붙는다. 제목 옆에 지라 완료를 알리는 글자는 두지 않는다 — `지라 상태` 칸이 이미 그 말을 한다 |
| 지라 하위 티켓 | 띠 카드 셋째 줄에서 펼친다(카드 밖으로 나가지 않는다). **접힌 줄**: 진행률 뒤에 미완료의 담당별 개수(`.who`) — 이름마다 28px 글자 버튼(`.d-jwho`, 12.5/500 `--dim`, hover `--hover`, `aria-pressed="true"`면 700 `--text`)이고 사이는 `·`, 넷을 넘으면 `외 N명`(`.qt` `--dim`), 다 끝났으면 `모두 완료`. 거르는 중에는 끝에 `전체`(`.d-jwho.clear`, `--accent-text` 600). 이름이 곧 거르개다 — 별도 토글·드롭다운을 만들지 않는다. **펼친 목록**(`.d-jkids`, 행 간격 1px): 한 줄(`.d-jkid`) = 74px 지라 상태(`.st` 12.5/600, 범주로만 색 — 완료 `--success` · 할 일 `--dim` · 진행 `--accent-text` 파랑, 배지가 아니다) · 요약(`.sm` 13px `--text`, 줄 높이 28px, 새 탭 + `rel="noopener noreferrer"`, **키는 title에만**, hover에서 `--accent-text` 밑줄) · 96px 담당자(`.wh` 12.5 `--muted` 오른쪽 맞춤, 없으면 `--dim` `담당 없음`) · 72px 배포 버전(`.ver` 12 `--dim`, 쓰는 줄이 있을 때만 칸을 잡는다). 완료한 줄(`.is-done`)은 맨 아래에서 글자만 `--dim`이 된다(취소선·다른 면을 쓰지 않는다). 끝에 `완료 N개 더 보기`(`.d-link.more`)와, 100개를 다 채웠을 때만 `지라에서 전체 보기 ↗`(`.all` `--accent-text`). ≤640px에서는 담당자가 요약 아래 줄로 가고 배포 버전은 숨는다. 읽기 전용이라 이 목록에는 값 고르개도 ⋯도 없다 |
| 지라 확인 줄 | 지라에 쓰는 동작은 전부 이 줄을 거친다(모달을 새로 만들지 않는다). 띠 카드 안 값 칸 바로 아래(`.d-jconfirm`, 고른 줄과 같은 `--sel` + `--r-sm` 12px, 여백 `12px 14px`, 13px `--muted`): `지라의 이 티켓을 바꿀까요?`(14/700 `--text`) + 티켓 **요약**(600 `--text`)과 그 옆 조용한 키(12 `--dim`) + 전→후 줄(`지라 상태:` `--dim` · 전 `--muted` 취소선 · `→` · 후 700 `--text`) + 버전 고치기일 때만 `이 버전을 쓰는 모든 티켓에 적용돼요`(`--warn`) + 오른쪽 아래 `취소`(3차, 연파란 판 위라 바탕만 `--surface`)·`바꾸기`(2차 `.acc`). Esc · 바깥 클릭 · `취소`로 닫히고 아무것도 보내지 않는다. 보내는 동안 `바꾸기`는 `보내는 중…`이 되고 세 고르개와 새로고침이 잠긴다. 열리면 줄로 초점이 간다(`tabindex=-1`) |
| 지라 연결 줄 | 아직 지라를 걸지 않은 **직접 만든(그룹) 프로젝트**에서 띠 카드가 서는 그 자리. 평소는 조용한 한 줄(`.d-jline`)의 글자 버튼 하나 — `지라 티켓 연결`(`.d-link.d-jlinkgo`, 누르는 자리 28px). 지라 설정이 없으면 이 줄 대신 띠 카드와 같은 `지라 연결이 필요해요 · 설정 방법`이 선다. 누르면 그 자리가 판(`.d-jlink`, 확인 줄과 같은 `--sel` + `--r-sm` 12px, 여백 `12px 14px`, 13px `--muted`, 행 간격 8px)으로 열린다: 입력칸(`.in` 32px, `--field` 채움 + 초점에서 `--surface` + `0 0 0 2px --accent`, placeholder `지라 번호나 주소 — 예: IO-12345`) + 3차 `찾기`·`취소`(연파란 판 위라 바탕만 `--surface`) → 못 찾으면 그 아래 `.er`(`--urgent`) 한 줄 → 그 아래 조용한 선택지(`.opts`, 비면 자리 자체가 없다): `내 담당 티켓`(`.note` `--dim`) + 28px 줄 버튼(`.pk`, `요약 · 키`, hover `--hover`), 그 끝에 같은 줄 부품의 조용한 글자 버튼 `.pk.qt`(`--dim`) `완료한 티켓도 보기`(부르는 동안 `불러오는 중…` + disabled, 실패하면 그 아래 `.note` 한 줄). 누르면 `완료한 티켓`(`.note`) 소제목 아래로 같은 `.pk.qt` 줄(`요약 · 키`, 회색이 곧 완료 표시다 — 배지를 새로 만들지 않는다)이 덧붙고, 두 목록은 한 입력으로 함께 걸러진다. 찾으면 같은 판이 미리 보기로 바뀐다(`.pv`: 요약 600 `--text` + `상태 · 담당 ○○` + 조용한 키 12 `--dim`, 필요하면 `.note` 한 줄) + 오른쪽 아래 `취소`(3차)·`연결`(2차 `.acc`, 고른 티켓이 에픽이면 `연결만`으로 불리고 옆에 3차 `이 에픽으로 옮기기`가 하나 더 선다). Esc · `취소`로 닫히고 아무것도 보내지 않는다. 연결된 그룹 프로젝트의 띠 카드에는 새로고침 옆에 28px ⋯(`.d-more`)가 붙고 `지라 연결 해제`(+ 에픽이면 `IO-123으로 옮기기…`)를 연다(`jira:KEY` 프로젝트의 카드에는 없다). **옮기기 확인 줄**(BMOVE)은 새 판을 만들지 않는다 — 지라 값 바꾸기와 같은 `.d-jconfirm`을 그대로 써서 `결제 리뉴얼의 항목 12 · 회의 3 · 주간요약 문장 5를 IO-48501로 옮길까요? …` + `취소`/`옮기기`가 값 칸(또는 미리 보기) 바로 아래에 선다 |
| 회의 탭 | grid `340px minmax(0,1fr)` — 프로젝트 탭과 같은 뼈대이고 왼쪽 카드만 조금 넓다(`시각 · 제목 · 프로젝트 · 상태`가 한 줄에 들어와야 한다). 왼쪽은 흰 카드 목록(토글 칩 `초안 있음`·`미완료만`·`빈 회의 포함`(툴팁이 뜻을 푼다) → 그 아래 제 줄로 두 칸 세그먼트 `날짜순 | 프로젝트별`(`.d-seg`, 보기 전환이라 `aria-pressed`, 28px 칸) → 소제목 → 줄 `42px minmax(0,1fr) auto` → 있으면 `이전 회의 더 보기`). **보기가 둘이다.** `날짜순`(기본, localStorage에 기억)은 날짜 소제목(`.d-mtday`) 아래로 줄이 서고, `프로젝트별`은 접히는 프로젝트 그룹 제목(오늘 목록과 같은 `uiGroupHeading` — 색 점 + 이름 + 회의 수, 목록 카드라 여백만 줄인다) 아래로 묶는다. 묶음 차례는 미완료가 남은 프로젝트가 위, 그 안에서 가장 최근 회의 날짜순, `프로젝트 없음`은 맨 끝이다. 접은 상태는 세션 동안만 산다. 소제목 옆에는 지라 상태 이름이 범주 색 글자로 조용히 붙을 수 있다(앱이 그 티켓을 직접 읽고 있을 때만, BJCOLOR 규칙, 색 점 없음). 프로젝트별의 줄은 날짜 소제목이 없으므로 첫 칸이 74px로 넓어지고 `9/22 10:00`을 적으며, 프로젝트 표기는 소제목이 이미 말하므로 뺀다. `이전 회의 더 보기`는 프로젝트별에서 소제목 아래 `더 보기 N`(왼쪽 정렬, 글자선 52px)으로 붙는다. 프로젝트로 거르는 드롭다운(`.d-msel`)은 없앴다 — 접는 소제목이 그 자리다, 오른쪽은 **줄 옆 회의 카드와 같은 내용**을 페이지 안의 흰 카드(`.d-detail.is-tab`, 최대 940px)로 그린다 — ✕ 없이, 담기 바는 카드 맨 아래에 `position: sticky`. 머리줄(`회의 N`) 오른쪽 끝은 오늘 가져올 미팅 노트가 있는지로 둘 중 하나다: 있으면 3차 `.d-btn.sm` `오늘 것 모두 가져오기`(라벨 뒤에 조용한 숫자 — `d-headnum`의 `오늘 신규 N`과 같은 표현, `.sp`로 밀어낸다), 없으면 그 자리에 조용한 한 줄(`.d-mtlast`, 12.5px `--dim`) `오늘 미팅 노트는 다 가져왔어요`(오늘 시작한 회의가 아직 없으면 `아직 가져올 미팅 노트가 없어요`) + 옆에 글자 버튼(`.d-link`) `다시 확인`(같은 요청 길). 오늘 가져온 기록이 있으면 그 아래 또 한 줄(`.d-mtlast`)이 붙는다. 회의 내용 머리 아래(`.d-dget`)는 그 회의의 노트 유무로 둘 중 하나다: 아직 안 가져왔으면 3차 `.d-btn.sm` `이 회의의 미팅 노트 가져오기`(도는 동안 그 버튼만 `가져오는 중…`이고 다른 가져오기 버튼은 `:disabled`), 이미 가져왔으면 조용한 글자(`.d-mtlast`) `미팅 노트 가져옴` + (링크가 있으면) `.d-link` `티로에서 열기`(노트가 여럿이면 `1`·`2`…) — 초안을 다 검토해 0개가 되어도 남는다. 시작 전 회의에는 이 자리 자체가 없다 |
| 아이디어·결정 탭 | 왼쪽 아이디어 좁은 칸(320px) + 오른쪽 결정 넓은 본문(간격 28px, `#gridRecords` 최대 1240px 가운데 정렬은 오늘·프로젝트·회의 탭과 같은 "왼쪽 좁은 목록 + 오른쪽 넓은 본문" 짜임). 각 칸이 흰 카드 하나. 머리줄 = 제목(19/700) + 회색 개수 칩(title에 `PRD에 아직 반영하지 않은 결정 N개` — 탭 숫자와 같은 문구) + (결정만) 오른쪽에 작은 `결정 찾기` 입력(200px, placeholder만). 두 칸 모두 **프로젝트 그룹 제목**으로 묶는다(오늘 목록과 같은 부품, 위 18px 여백, 줄의 왼쪽 글자선 12px에 맞춤, 눌러서 프로젝트 탭). 아이디어 줄 = 문구 + `원문`(좁은 칸에서 2줄 클램프) \| `가능성 높음` \| ⋯. 결정 줄 = `PRD 반영` 체크 \| 문구(말줄임 없이 전문을 줄바꿈, 15px/1.55, `word-break: keep-all`, 글줄 폭 최대 760px) \| (반영 완료만) `· ● 프로젝트` \| ⋯, 그 아래 조용한 정보 줄(12.5px `--dim`, ` · `로 잇는다) = `9월 18일 결정`(있으면) · 나온 회의(있으면, 눌러서 회의 탭 — `openMeetingsTab`) · `원문`(있으면, 제목 옆이 아니라 여기로 옮겼다). `반영 완료`는 카드 밖 아래에 접어 두고, 그 줄에도 같은 정보 줄이 붙는다(+ 반영 날짜는 기존대로 오른쪽에). 넓은 창(901px~)에서는 아이디어 칸이 `position: sticky`로 헤더 아래에 자리를 지키고, 칸 높이가 화면을 넘으면 `.d-list` 안에서만 스크롤한다(머리줄은 고정) |
| 주간요약 탭 | grid `236px minmax(0,1fr) 356px`, 간격 16px = 주차 목록 카드 \| 문서 카드 \| 슬랙 미리보기 카드. 세 카드 모두 흰 면이고 문서 본문은 720px까지만 넓어진다 |
| 아래 고정 막대 | 여러 개 선택·문장 모으기 막대는 화면 아래에 떠 있는 흰 둥근 막대(`--pop`, 최대 1120px). 본문은 `padding-bottom: 104px`, 알림은 `bottom: 76px`로 비켜선다 |

**반응형 경계.**

| 폭 | 바뀌는 것 |
|---|---|
| ≤1280 | 상태 열 152px, 프로젝트 열 84px. 주간요약 `208px 1fr 312px`, 문서 여백 24/26px |
| ≤1120 | 주간요약은 `196px 1fr`이 되고 슬랙 미리보기가 문서 아래 전체 폭 카드로 내려온다. **주차 목록 카드는 제 높이만큼만 차지한다**(늘려 두면 문서 옆에 빈 자리가 길게 남는다). 상세는 그대로 줄 옆 카드다 |
| ≤900 | 레일·프로젝트 목록·주차 목록이 본문 위로 쌓인다(가로 스크롤 한 줄). 프로젝트 탭 세그먼트(`.d-pfseg`)는 원래부터 `inline-flex`라 손대지 않아도 머리 `+` 칩 줄 끝에 그대로 이어 선다. 찾기 칸(`.d-pfind`)은 `display:block; width:100%`라 그 자리에서 줄바꿈해 아래 한 줄 전체를 차지하고, 그 뒤 칩 줄이 새 줄에서 이어진다. 시작 전·지난 프로젝트 소제목(`.d-plink`)과 배포별 버전 소제목(`.d-grp`)도 원래 `display:block`/`flex; width:100%`라 칩 줄 사이에서 자연히 줄바꿈된다 — 좁은 폭 전용 규칙을 새로 두지 않았다. 회의 목록도 본문 위로 쌓이되 칩 줄·날짜 소제목이 있어 가로로 흘리지 않고 세로 그대로 두고 높이만 40vh로 줄인다. 서랍은 아래에서 올라오는 전체 폭 시트(최대 86vh, 위쪽 모서리 20px). 아이디어·결정은 쌓지 않는다 — 머리에 세그먼트(기존 `.d-seg` 부품) `아이디어 N \| 결정 N`이 나타나고 누른 쪽 칸만 보인다(CSS로 감추고, 그 쪽 상태는 localStorage에 기억 — 기본은 결정). 팔레트·검색에서 결정/아이디어 줄로 옮겨 가 밝힐 때도 그 항목이 있는 쪽을 자동으로 연다 |
| ≤640 | 헤더 여백 `0 12px 0 16px`, 앱 이름 숨김, 검색은 아이콘만, 날짜 칩 26px. 탭은 아직 한 줄이고 넘치면 가로로 스크롤한다 |
| ≤520 | **헤더가 두 줄이 된다**(`--hdr: 100px`): 첫 줄은 날짜 칩 + 새로고침·검색·설정, 둘째 줄은 탭 다섯이 전체 폭으로(48px, 넘치면 가로 스크롤, 스크롤 막대는 숨긴다). 레일·서랍·시트가 모두 `--hdr`를 보고 자리를 잡으므로 이 한 값만 바꾼다(서랍 머리는 60px로 되돌린다). 상세도 아래에서 올라오는 전체 폭 시트가 된다(최대 86vh, 위쪽 모서리 20px, z 47) — 손가락이 닿는 자리. 줄이 두 줄 격자로 쌓이고(최소 60px) hover 동작은 항상 보인다. 팔레트는 전체 폭, 결과는 `56px 1fr` 두 열 최소 46px. 프로젝트 열 이름 줄은 숨긴다 |

어느 폭에서도 좌우 여백 16px 이상, 가로 페이지 스크롤 없음.

### Named Rules

**The Detail Opens Beside Its Row Rule.** 상세는 누른 줄 옆에 뜬다 — 눈이 줄에서 떨어지지 않게. 자리 때문에 목록이 좁아지지 않고, 카드가 길어져도 화면을 넘지 않는다(머리·발은 붙박이, 가운데만 스크롤).

**The Drawer Pushes Rule.** `나중에 할 일`은 본문을 덮지 않고 밀어낸다(900px 초과). 레일과 오늘 목록은 제자리에 남고 목록 폭만 줄어든다.

## Components

### 버튼 세 등급 (이게 전부다)

| 등급 | 모양 | 쓰는 곳 |
|---|---|---|
| **1차 (파란 채움)** | 42px, 여백 `0 20px`, 12px 모서리, `--accent-strong` + `--on-fill`, 14.5/600. hover `brightness(1.08)` | **한 패널에 하나**: 상세의 `완료로 표시`, 회의의 `N개 담기`, 주간요약의 `슬랙용으로 복사`, 문장 모으기 막대의 `완료` |
| **2차 (연파랑)** | 33px, 여백 `0 13px`, 10px 모서리, `--accent-soft` + `--accent-text`, 13.5/600. hover는 파란 채움 | 줄 위에 떠오르는 동작(`내일` `나중에` `오늘로`), 새로 들어온 줄의 `오늘`·`나중에`·`완료`, 선택 막대의 `완료로 표시`(`.d-btn.acc`) |
| **3차 (연회색)** | 34px(작은 형 32px), 여백 `0 13px`, 10px 모서리, `--neutral-bg` + `--muted`, 13.5/600. hover `--hover` + `--text` | 그 밖의 모든 조용한 동작 |
| **삭제 (빨간 글자)** | 3차와 같은 크기, 배경 없음, `--urgent` 글자. hover에서 `--urgent-bg` | `삭제`. 확인창 없이 실행하고 알림의 `실행 취소`(⌘Z)로 되돌린다 |

전부 `:active { transform: scale(.98) }`, 전환 170ms. 곁가지: **글자 링크**(`.d-link`, `--accent-text` 13/600, hover 밑줄), **아이콘 버튼**(34px 원, `--muted` → hover `--hover`, `aria-label` 필수), **조용한 글자 버튼**(`.d-headnum`, 28px, `--dim` 13/500 — `오늘 신규 14`처럼 숫자를 누를 수 있게 한 자리).

**새로 들어온 줄의 동작**은 평소 3차(연회색)이고, 줄에 손이 닿으면(`:hover`/`:focus-within`) 그제야 2차(연파랑)로 물든다.

### 세그먼트

회색 트랙(`--neutral-bg`, 12px, 안쪽 3px) + 흰 손잡이(9px, `--shadow`). 칸은 32px / `0 14px` / 13.5px 600 `--muted`(꺼진 칸이 `--neutral-bg` 위에서 4.5:1을 넘어야 해서 `--dim`이 아니다), 눌린 칸만 `--surface` + `--text`. 보기 전환은 `aria-pressed`, 값을 고르는 것(초안의 종류·`나중에/오늘`)은 `aria-checked`를 쓰고 **모양은 같다**. 동작 버튼과 반드시 달라 보여야 한다.

### 입력칸

테두리 0, `--field` 채움, 12px 모서리, 초점에서 `background: --surface` + `box-shadow: 0 0 0 2px var(--accent)`. 적용 대상: 메뉴의 글자·선택 칸, 날짜 칸, 상세의 여러 줄 입력, 회의 초안 문구(`.d-dtxt`, 최소 38px), 주간요약 문장 수정(`.rp-ta`), 프로젝트 이름 바꾸기(`.d-pren .d-din`, 최소 36px).

**제목 자리에서 고치는 칸**(프로젝트 이름 바꾸기 `.d-pren`)은 제목이 있던 그 줄을 그대로 쓴다 — 묶음 최대 520px, `입력칸 + 2차 저장 + 2차 취소`(gap 6px)이고 글자는 제목을 이어받아 18/700이다. 현재 이름이 전체 선택된 채로 열리고, Enter/`저장`·Esc/`취소`이며 한글을 조합하는 중의 Enter는 넘긴다(회의 줄의 `문구 고치기`와 같은 규칙).

**빠른 추가는 다르다.** 목록 첫 줄과 다음 주 계획은 평소 **그냥 맨 줄**(48px, 배경 없음, `+` 아이콘 + 안내글 15px)이고, **초점에서만** `--field` 채움 + `outline: 2px solid var(--accent); outline-offset: -1px`이 생긴다. 레일·서랍의 빠른 추가는 40px 회색 알약이다.

### 체크박스 한 가족

화면의 체크박스는 한 가족뿐이다 — **모서리 둥근 네모**.

| 자리 | 크기 / 모서리 | 누르는 영역 |
|---|---|---|
| 업무 줄 · 서랍 · 결정의 `PRD 반영` (`.d-cb`) | 22px / 6px | `::after { inset: -3px }` = 28px |
| 레일 확인 대기 (`.d-wcb`) | 20px / 6px | `::after { inset: -4px }` = 28px |
| 여러 개 선택 칸 (`.d-selcb`) | 17px / 4px | `::after { inset: -6px }` = 29px |

꺼짐: 1.5px `--check-line` 테두리 + `--surface` 안쪽. hover: 테두리 `--accent`. 켜짐: `--accent` 채움 + `--on-fill` 체크. 선택 칸만 안쪽 2px 흰 테를 넣어 완료 체크와 구분한다(둘은 같은 화면에 동시에 뜨지 않는다). 잠긴 동안(회의 줄의 문구 고치기 중): 흐리게(`opacity: 0.5`), 누를 수 없다.

같은 체크박스를 같은 항목이 나오는 자리마다 그대로 재사용한다 — 회의 카드/탭의 `이 회의에서 나온 것`·`이전 회차의 미해결 항목` 줄과 프로젝트 탭의 확인 대기·결정 줄도 목록·아이디어·결정 탭과 같은 부품(`taskCompletionCheckbox`·`waitingCheckbox`·`decisionCheckbox`)을 쓴다. 저장 길도 하나다: 체크 → `toggleTask` → 목록과 같은 `toggle`.

**우선순위는 이 체크박스 안에서 말한다**(미완료 업무 줄에만 — 확인 대기 `.d-wcb`·결정의 `PRD 반영`·완료한 줄은 대상이 아니다):

| 값 | 테두리 / 바탕 | 안에 겹치는 것 |
|---|---|---|
| `중요`(high) | `--warn` / `--warn-bg` | 위 꺾쇠 **한 겹**(`priHigh`, `--warn`) |
| `긴급`(critical) | `--urgent` / `--urgent-bg` | 위 꺾쇠 **두 겹**(`priTop`, `--urgent`) |
| 보통 · 낮음 | 그대로 | 없음 |

꺾쇠(`.d-pri`, 13px)는 `pointer-events: none`으로 체크박스 위에 겹치므로 누르는 자리는 그대로 28px이다. **hover·focus-visible이면 꺾쇠를 감추고 평소의 체크박스(파란 테 + 흰 바탕)로 돌아간다** — 누르면 무엇이 되는지가 먼저다. 체크되면(완료 순간 애니메이션 포함) 꺾쇠 없이 평소의 파란 채움 + 흰 체크다. 색만으로 말하지 않는다: 체크박스의 `title`이 `중요한 업무예요`/`가장 먼저 해야 하는 업무예요`(`UI_PRIORITY_META.hint`)이고 `aria-label`은 `가입 문구 검토하기 — 중요 · 완료로 표시`처럼 우선순위 말을 품는다.

**완료하는 순간**(320ms + 520ms): 체크가 그려지며 살짝 커졌다 돌아오고(`d-pop-check`) → 제목에 줄이 왼쪽에서 그어지고 → 옅어진다(`d-strike`). `prefers-reduced-motion`에서는 둘 다 끈다.

### 배지 · 칩

- **상태 배지** — 23px, **6px 모서리**(알약 아님), 12.5/600, 틴트 바탕 + 같은 계열 진한 글자, `pointer-events: none`(눌러 보고 싶어지지 않게). 세우는 말은 위 **The Badge Is For Urgency Rule**의 목록뿐이다.
- **개수 칩** — 22px, 999px, `--neutral-bg` + `--muted`, 12.5/600. 구역 제목과 카드 제목에만 붙인다. 예외 둘: 오늘 할 일의 남은 개수는 `--accent-soft` 파란 칩, 새로 들어온 것·확인 대기는 `--warn-bg` 주황 칩.
- **그룹 제목의 개수는 칩이 아니다** — 조용한 회색 숫자(13px / 500 / `--dim`)다.
- **필터 칩**(⌘K) — 30px, 999px. 종류는 회색 트랙 위에 나란히 서고 고른 하나만 `--accent-soft`. 켜고 끄는 칩(`완료 제외`)은 트랙 밖에서 `--neutral-bg`(꺼짐) / `--accent-soft`(켜짐).

### 업무 줄 (Signature)

- **격자** `30px minmax(0,1fr) var(--wp) var(--wm)`(기본 `--wp: 0` · `--wm: 168px`), 열 간격 12px, 높이 54px, 좌우 여백 20px, 14px 모서리. 프로젝트는 제목 뒤에 적으므로 가운데 열은 폭 0이고 그만큼 **제목이 가장 넓다**(≤520px의 두 줄 행에서만 이름 붙인 칸으로 쓴다).
- **열 순서** 체크박스(**= 우선순위**) | 제목(+ `· ● 프로젝트` + `원문`) | (폭 0인 프로젝트 열) | 상태 열.
- **줄 오른쪽에는 날짜 성격의 말만 선다** — `상태(밀림 · N일째 진행 중 · 답변 기다리는 중/답변 왔어요) → 기한` 차례로 **있는 것만** 오른쪽 끝에 붙는다(기한이 맨 오른쪽). 우선순위 글자는 여기 오지 않는다(체크박스가 말한다 — DECISIONS 2026-09-23). 빈 칸을 자리 표시로 남기지 않는다: 넓은 빈자리에 글자가 흩어져 보였다. 체크박스가 없는 줄(미루기 제안 줄)에서는 예전처럼 우선순위 글자를 맨 앞에 쓴다.
- **배경은 `--rowbg` 하나로 정한다** — 기본 `--surface`, hover/focus-within `--hover`, 선택 `--sel`(+ 제목 600 `--accent-text`), 선택 + hover `--sel-hover`. 줄 위에 겹쳐 뜨는 동작 묶음이 같은 값을 쓰므로 반드시 불투명해야 한다.
- **hover/focus 동작** 오른쪽 끝에 상태 열 **위로 겹쳐** 나타난다(제목 폭을 뺏지 않는다). 왼쪽 24px은 `transparent → var(--rowbg)` 그라디언트로 아래 글자를 가린다. 130ms.
- **완료한 줄** 제목 `--muted` 400 + 취소선, 상태 열이 280px로 넓어져 왼쪽 정렬로 결과 한 줄을 보여 준다. 결과가 없으면 글자 링크 `결과 한 줄 적기` → 그 자리에서 입력칸(Enter 저장 / Esc 취소).
- **상세를 열면** 프로젝트 열은 접히고 상태 열은 내용만큼만 잡는다 — 제목이 먼저 자리를 갖는다.
- **≤520px** 두 줄 격자 `"ck ti ac" / "ck mt ac"`, 최소 60px. 제목 최대 2줄, 프로젝트는 정보 줄 맨 앞으로, 동작은 항상 보이고 세로로 쌓인다. 상태말은 같은 순서(상태 → 기한)로 왼쪽부터 이어 붙는다. 체크박스 크기·꺾쇠는 넓은 화면과 같다.

### 상태말 — 말과 아이콘 표

기한·밀림·진행·답변에는 14px 아이콘 + 풀어 쓴 `title` 툴팁이 붙는다. **배지** 칸이 ✓인 것만 틴트 바탕으로 선다.
**우선순위 줄 둘은 체크박스가 없는 자리(미루기 제안 줄·상세·메뉴)에서만 글자로 쓴다** — 업무 줄에서는 체크박스 안의 꺾쇠가 말한다(위 `체크박스 한 가족`).

| 종류 | 아이콘 | 말 | 색 | 배지 | 툴팁 |
|---|---|---|---|---|---|
| 기한 | `calendar` | `오늘까지` | `--warn` | ✓ | `기한은 9월 22일 (화)이에요` |
| 기한 | `calendar` | `기한 2일 지남` | `--urgent` | ✓ | 〃 |
| 기한 | `calendar` | `내일까지` / `9월 27일까지` | `--dim` | — | 〃 |
| 밀림 | `clock` | `어제에서 밀림` / `5일째 밀림` | `--dim` | — | `오늘 하려다 넘어온 업무예요` |
| 진행 | `clock` | `진행 중` / `3일째 진행 중` | `--dim` | — | `이미 손을 댄 업무예요` |
| 우선순위(체크박스 밖) | — | `긴급` | `--urgent` | ✓ | `가장 먼저 해야 하는 업무예요` |
| 우선순위(체크박스 밖) | — | `중요` | `--warn` (600, 배경 없음) | — | `중요한 업무예요` |
| 우선순위 | — | `보통` · `낮음` | 찍지 않는다 | — | — |
| 답변 | `chat` | `답변 기다리는 중` | `--dim` | — | `다른 사람의 답변을 기다리는 중이에요` |
| 답변 | `chat` | `답변 왔어요` | `--success` (600) | — | `기다리던 답변이 왔어요` |
| 답변 받을 날 | `calendar` | `1일 늦음` | `--urgent` | ✓ | `9월 20일 (일)까지 답변을 받기로 했어요` |
| 답변 받을 날 | `calendar` | `오늘 답변 예정` | `--warn` | ✓ | 〃 |
| 원문 | — | `원문` | `--dim` 12.5/500 | — | `슬랙 원문을 새 탭에서 열어요` |

데이터 값(`critical`/`high`)은 그대로 두고 **표시하는 말만** 이 표를 따른다. 값을 고르는 자리(상세·메뉴)의 선택지는 `긴급 / 중요 / 보통 / 낮음`.

### 그룹 제목

높이 `--grp` + 위 14px 여백, 좌우 20px, 14.5/600 `--muted`. 프로젝트 그룹이면 앞에 색 점, 뒤에 회색 숫자. `진행 중` 그룹만 `--text`. 목록 카드 안에서는 `position: sticky; top: 0` + `--surface` 배경. 접히는 그룹(`완료 5`, `반영 완료 4`, `완료한 업무 N`)은 줄 전체가 버튼이고 꺾쇠가 90° 돈다(130ms). 마우스를 올리면 오른쪽에 `+`가 나타나 그 자리에서 추가한다.
줄의 좌우 여백이 20px가 아닌 목록(아이디어·결정 열, 주간요약의 전체 업무 기록 — 줄 여백 12px)에서는 그룹 제목도 **그 줄의 왼쪽 글자선에 맞춰** 12px로 서고, 위 여백은 18px다. 묶음이 보이지 않으면 그룹 제목이 아니다.

### 반응 필요 (오늘 탭 맨 위)

내가 답해야 하는 것(1차 지라 댓글)을 모으는 구역. 자리는 오늘 탭 **본문 맨 위**, `새로 들어온 것` 위이고 리듬은 같다(머리줄 + 줄 목록). **0개이거나 연결이 없으면 구역 자체가 없다** — 매일 뜨는 `없어요`는 소음이다(작동 여부는 설정 › 연동 지라 카드 둘째 줄의 `반응 필요 댓글 N개 · N분 전 확인`). 머리줄에 ⋯은 없다.

- **머리줄** `반응 필요` + 주황 개수 칩(`.d-sechd .n` 그대로, 안 보이는 줄까지 센다) — **평소엔 그 뒤에 아무 글자도 없다**(`새로 들어온 것 4`처럼 제목+개수만이어야 다른 구역과 리듬이 맞는다 — `지라 댓글 · 3분 전`은 흰-노랑-흰으로 위계가 튀어서 뺐다. 작동 여부는 이미 설정 › 연동의 지라 카드에서 본다). 마지막 갱신이 실패해 이전 값을 쓰는 동안에만 조용한 글자 `3시간 전 기준`이 붙고 그때만 `--warn` 글자다.
- **줄 앞 출처 이름표** 첫 줄 맨 앞, 프로젝트 색 점보다 앞(`.sc`, 12/600 `--dim`) — `지라`. 지금은 출처가 하나뿐이라도 나중에 늘어날 걸 대비해 줄 안에서 구분한다(머리줄에는 안 적는다). 이름표+색 점은 **제 칸(`.ld`)** 이라(grid `"ld tl mt" ". sb sb"`), 둘째 줄 미리보기는 앞머리 아래가 아니라 **제목 바로 아래**에서 시작한다.
- **판** `새로 들어온 것`과 같은 따뜻한 카드(`--warm`, `--warm-line`, 20px, `--soft`; 줄 hover `--warm-hover`). 처음엔 흰 카드였는데 그러면 본문이 흰(반응 필요)-노랑(새로 들어온 것)-흰(오늘 할 일)으로 튀었다 — 둘 다 "바깥에서 들어와 내가 손대야 할 것"이라 같은 색으로 묶어 위 두 카드/아래 흰 카드의 두 덩어리로 위계를 세운다(사용자 선택 A안).
- **줄(`.d-atrow`)** 두 줄, 최소 54px, 14px 모서리, 배경은 업무 줄과 같은 `--rowbg`(hover/focus `--hover`).
  - 첫 줄: 프로젝트 색 점(`uiProjectDot('jira:KEY')`, 이 티켓 번호로 고른 색이라 다른 자리의 프로젝트 색과 반드시 같지는 않다 — 정확한 프로젝트는 `열기`로 확인) + **요약**(16/500, 한 줄 말줄임 — 키는 `title` 툴팁과 `열기` 링크에만). 지라 상태는 여기 적지 않는다(반응 필요는 "누가 나를 불렀나"만 보는 자리라 상태까지 적으면 정보가 두 겹이었다). 오른쪽 끝에 `테스터A 외 1명 · 2시간 전`(12.5 `--dim`) + 부름이면 `@멘션`(12.5/600 `--warn` **글자**, 바탕을 채우지 않는다 — 배지는 급한 말 셋뿐이다).
  - 둘째 줄: 미리보기(13 `--dim`, 한 줄 말줄임) + `· 댓글 N개`(2개 이상일 때만).
  - 동작은 업무 줄과 **같은 방식**(`.d-atacts`) — hover·focus에서 오른쪽 끝에 겹쳐 뜨고, 그때 `누가 · 언제`는 같은 속도로 물러난다. `열기`(조용한 글자 링크) · `했어요` · `할 일로`.
  - `할 일로`를 누르면 그 줄 아래에 확인 대기의 `후속 할 일`과 **같은 입력 줄**(`.nx.is-ed`)이 펴진다(`오늘`·`나중에`). 그동안 그 줄의 동작 묶음은 감춘다.
- **여덟 줄**까지 보이고 나머지는 조용한 글자 버튼 `외 N개 보기`(`.d-atmore`). 다시 접는 길은 없다.
- **≤520px** 세 줄로 쌓인다: 제목 / `누가 · 언제` + 미리보기 / 동작(늘 보인다, 32px). 레일(296px)에는 그리지 않는다.

### 사용설명서 카드

`새로 들어온 것` 자리에 **닫기 전까지는 늘**(기록이 있든 없든) 서는 **같은 따뜻한 카드**(`.d-start.d-guidecard` — `--warm`, 20px, `--soft`)다. 머리는 `사용설명서`(15/700) + 오른쪽 조용한 `닫기`(`.d-btn.sm`, 바탕 없음 → hover `--warm-hover`). 줄 넷(`.d-guide`, 최소 44px, `이름 112px 14/600 | 설명 13.5 --muted`, 굵은 글자 `--text`, 14px 모서리): `Dock에 두기`(버튼이 아니다 — 아래 작은 Dock 그림 `.d-guidedock`(`--neutral-bg` 판, 22px 회색 칸 사이에 28px 지금 아이콘 `/app-icon.png`, `aria-hidden`) + 밑줄 글자 `앱이 어디 있는지 모르겠으면 → 설정 › 앱 › 앱 위치`) · `할 일 적기` · `연동은 나중에` · `슬랙에서 보내는 법`(이 셋은 버튼 — 이름이 `--accent-text`, hover `--warm-hover`). 닫으면 브라우저에 기억하고(설정 파일이 아니다) 같은 줄이 **도움말 탭 맨 위** `.d-manual`(`--warm` 판, 12px 모서리, 제목 14/700)로 남는다. **≤520px에서는 이름과 설명이 두 줄로** 쌓인다.

### 새로 들어온 것

아주 옅은 따뜻한 카드(`--warm`, 20px, `--soft`). 줄은 최소 54px, `minmax(0,1fr) auto`: 제목(16/500, 인라인 수정 가능) + `원문` | `오늘` `나중에` `완료` + ⋯. 프로젝트 지정·기한·우선순위·삭제는 ⋯ 안에 있다. 제목 오른쪽 끝의 `오늘 신규 N`은 누르면 ⌘K 팔레트를 오늘 들어온 것만으로 연다. `NEW`는 버튼이 아니라 표시다(DECISIONS) — 화면에 잠깐 머물면 조용히 사라진다.

### 오늘 할 일 머리줄 · 진행 표시

머리줄: 제목(19/700) + 파란 개수 칩 + `19개 중 5개 끝냈어요` + (제안이 있으면) 글자 링크 `2개 미룰까요?` / 오른쪽 `나중에 할 일 13` + ⋯(보기 전환 `프로젝트별`·`마감·중요도순`, `여러 개 선택`). 프로젝트로 가는 길은 탭과 목록의 프로젝트 이름이다.

진행은 별도 띠가 아니라 **오늘 목록 카드 윗변을 따라 흐르는 3px 파란 선**이다. 카드를 덮는 투명한 판(`inset: 0`, 20px 모서리, `overflow: hidden`, `pointer-events: none`) 안에 두어 모서리에 맞게 잘린다. 폭은 `--p`, 전환 320ms.

### 레일 카드 셋

- **오늘 미팅** — 줄 격자 `44px minmax(0,1fr) auto`, 높이 자동(최소 44px). 시각 14/700 `--text`, 제목은 **두 줄까지**(말줄임 없음), 프로젝트는 뒤에 작은 회색 글자, 오른쪽에 `초안 N` 배지.
  - 진행 중인 회의: **배지 없이 색으로** — 줄에 `--accent-soft` 바탕, 시각·제목이 `--accent-text`.
  - 끝난 회의: 전부 `--dim`으로 흐리게. 앞으로 있을 회의: 그대로.
- **리마인드** — 배포가 코앞인 프로젝트, 답변이 해결된 업무, 다시 확인할 것이 이 차례(급한 순)로 모인다.
  - **배포 임박 줄**은 확인 대기와 **같은 두 줄 부품**(`.d-wrow.is-two`)이다: 첫 줄 프로젝트 이름(눌러서 프로젝트 탭 — `openProjectTab`), 둘째 줄 `v2.70.0 배포 3일 전`(`오늘 배포`/`내일 배포`/`배포일 2일 지남`) + `열린 업무 2`(`.nx`). 색은 **글자색**이다(배지가 아니다) — 3일 안 `--warn`, 지남 `--urgent`. 아이콘은 붙이지 않는다(이 카드의 다른 줄과 같다). 넷까지 적고 나머지는 조용한 한 줄 `외 N개`(`.d-rempty`)로만 알리며, 카드 머리의 개수 칩은 안 보이는 줄까지 센다. 줄에 닫기·미루기는 없다.
  - 안 가져온 미팅 노트는 이 카드에 오르지 않는다(회의 탭이 알린다).
- **확인 대기** — 카드 머리 오른쪽에 조용한 ⋯(보기 전환 `프로젝트별`(기본)·`급한 순`, 고른 값은 localStorage에 기억). `프로젝트별`은 프로젝트 소제목(색 점 + 이름 + 개수 — 오늘 목록의 그룹 제목과 같은 가족, 레일 폭에 맞춰 여백만 줄인다) 아래로 묶고, 묶음 차례는 가장 급한 줄이 있는 묶음이 위(각 묶음의 첫 줄 기준), 프로젝트 없는 것은 `프로젝트 없음`으로 맨 아래. 소제목의 이름을 누르면 프로젝트 탭으로(`openProjectTab`). 줄은 두 줄 구성이다: 첫 줄 제목(두 줄까지), 둘째 줄 `결제팀 · 2일째`(3일째부터 `--warn`) + 답변 받을 날이 지났거나 오늘이면 배지 + `9월 24일 다시 확인` / `오늘 요청함` — `급한 순`에서는 이 둘째 줄 맨 앞에 조용한 프로젝트 표기(색 점 + 이름, 말줄임)가 붙는다. 다시 확인할 항목은 목록(또는 급한 순 전체) 맨 위로 올라온다. 입력칸은 카드 맨 아래 `+ 확인 대기 추가`.
  - **체크한 뒤의 `다음은?` 줄**(`.d-wnext`) — 확인 대기를 체크하면 그 자리에 제안 줄이 남는다(알림은 `확인 완료로 표시했어요`만 알린다). 목록이 미완료만 보여 주는 자리(레일·프로젝트 탭)에서는 체크한 줄을 목록 맨 위에 한 번 더 그리고(`.d-wnextwrap`, 제목은 취소선) 그 아래에 붙이고, 줄이 남는 자리(회의의 `이 회의에서 나온 것`)에서는 그 줄 바로 아래에 붙인다. 판은 고른 줄과 같은 `--sel`에 12px 모서리이고, 새 색은 쓰지 않는다.
    - **넓은 자리(프로젝트 탭·회의)** — 줄은 `다음은?`(12.5/600 `--muted`) + 3차 버튼 셋(`후속 할 일` · `결정으로 남기기` · `답변 한 줄 남기기`, 연파랑 판 위에서 묻히지 않게 바탕만 `--surface`) + 오른쪽 끝 `닫기`. 액션을 고르면 버튼 줄이 그 자리에서 입력 줄로 바뀐다(입력칸 + `오늘`·`나중에` / `저장`, Enter는 첫 버튼, Esc는 입력만 닫는다). 아래에 `이 답을 기다리던 업무`가 최대 세 줄(제목 + `오늘로`·`열기`, 더 있으면 `외 N개`) 선다.
    - **좁은 레일(296px)은 한 줄(`.d-wnext.is-one`)** — 레일은 훑어보는 자리라 선택지를 다 펼치면 카드가 화면을 다 먹는다. 줄은 `답을 받았어요 ·`(12.5/500 `--muted`) + **제안 하나**(`.d-link` 파란 글자, 12.5px, 누르면 바로 실행) + `자세히`(조용한 3차 버튼) + `✕`(28px 누름 영역, aria-label `… — 다음은? 닫기`). 줄바꿈하지 않고, 제안 글자가 넘치면 말줄임하며 `자세히`·`✕`는 오른쪽에 고정된다. 넷을 다 펴려면 ~343px가 드는데 레일의 실제 자리는 222px라, `container-type: inline-size` + `@container (max-width: 339px)`로 **앞머리를 접어** 제안 글자를 온전히 보인다(전문은 `title`·aria-label에 남는다).
    - **상세 카드의 `다음은?` 구역(`.d-wnext.is-panel`)** — 완료한 확인 대기의 `답변 한 줄` 칸 바로 아래. 판 배경 없이 카드 위에 그대로 앉고, 구역 제목이 이미 `다음은?`이라 앞머리 글자는 빼고 `답변 한 줄 남기기`(바로 위 칸이 맡는다)와 `닫기`(붙박이라 걷지 않는다)도 뺀다. `waitingNextClose()`는 `.d-wnext:not(.is-panel)`만 걷는다.

더보기(⋯)는 hover·focus에서만 오른쪽 끝에 겹쳐 뜨고, 그 자리에 있던 조용한 정보는 같은 속도로 물러난다(글자가 반쯤 잘려 보이지 않게).

### 상세 (줄 옆 카드 — 업무 · 확인 대기 · 회의 정리 공용)

상세는 부품이 하나뿐이고 폭만 둘이다: **업무·확인 대기 360px, 회의 정리 460px**(최대 `100vw - 48px`). 떠 있는 카드이므로 `--surface` + 20px 모서리 + `--pop`이고, `position: fixed`로 화면 좌표에 선다(z 50 — 서랍 40·선택 막대 46 위, 팔레트 60·더보기 메뉴 75 아래).

**자리.** 가로는 누른 줄이 어디 있느냐로 정한다 — 본문 줄은 줄 오른쪽 끝에서 12px 안쪽, 레일 줄은 줄 **오른쪽 옆**, 서랍 줄은 서랍 **왼쪽 옆**. 세로는 줄 윗변에 맞추되, 화면에 다 들어오도록 필요한 만큼만 위로 끌어올린다(헤더 아래 12px·화면 아래 12px 보장). 어느 쪽이든 화면 안에 갇힌다. **붙을 줄이 없으면**(⌘K 팔레트에서 연 항목의 줄이 화면에 없을 때 등) 화면 가운데 위, 팔레트와 같은 자리에 선다.

**길어질 때.** `max-height: 100vh - 60 - 24`. 머리(제목 + ⋯ + ✕)와 발(동작 줄 / 담기 바)은 카드 안에 붙박이고 **가운데 본문만 스크롤**한다 — 스크롤 중일 때만 머리·발 경계에 `--hair` 한 줄과 아주 옅은 그림자가 생긴다. 긴 제목은 머리에서 3줄까지(누르면 그 자리에서 textarea), `결과 한 줄`(완료한 확인 대기는 같은 부품·같은 칸의 `답변 한 줄`)은 쓰는 만큼 자라다 5줄에서 멈추고 칸 안에서 스크롤, `확인 요청 기록`은 최신 3개 + `이전 기록 N개 더 보기`. 내용이 자라거나 창이 바뀌면 자리를 다시 잡는다.

**닫힘.** 업무·확인 대기 카드는 바깥 클릭 · Esc · ✕ · 같은 줄 다시 누르기로 닫히고, 다른 줄을 누르면 그 줄로 옮겨 간다. **회의 정리 카드만 빈 자리를 눌러도 닫히지 않는다** — 한참 붙들고 검토하는 면이라 자리를 잃으면 안 된다(✕ · Esc · 같은 회의 줄 다시 누르기 · 다른 줄 열기로 닫는다). 목록을 스크롤하면 카드가 줄을 따라가고, 누른 줄이 화면 밖으로 완전히 나가면 닫힌다. 열면 카드 제목으로 포커스, 닫으면 연 줄로 돌아간다. 등장은 140ms 페이드 + 4px(reduced-motion이면 바로).

**차례.** (넘어왔으면) 뒤로 링크 → 제목(카드에서 17/700, 누르면 그 자리에서 textarea) + ⋯ + 닫기 → `슬랙 원문 열기` → 필드 격자 `<dl>` `auto minmax(0,1fr)`(라벨 12.5 `--muted` / 값 14 `--text`) → 구역(위 1px `--hair`) → `고치면 바로 저장돼요`, 맨 아래 붙박이 동작 줄. 저장 버튼은 없다.

**값은 눌러서 고친다.** 상세의 필드 값은 읽기만 하는 글자가 아니다 — `언제 할지`·`우선순위`는 값 + 작은 꺾쇠(`.d-dpick`, 28px 이상, hover `--hover`, `aria-haspopup`)이고 누르면 **줄의 ⋯와 똑같은 고르개**가 붙는다. `기한`·`답변 받을 날`·`다시 확인할 날짜`는 날짜 칸, `누구에게`는 글자 칸, `프로젝트`는 선택 칸이라 그 자리에서 바로 고친다.

**동작 줄은 한 줄.** 1차 `완료로 표시` + 3차 `내일` `나중에`(나중 업무는 `오늘로`). `프로젝트 보기`처럼 가끔 쓰는 이동은 카드의 ⋯ 메뉴로 내린다.

**회의 정리는 자리가 둘, 내용은 한 벌이다.** 같은 함수(`panelMeeting`)가 (가) 줄 옆 카드(460px)와 (나) `회의` 탭의 오른쪽 자리에 그린다 — 내용을 두 벌 만들지 않는다. 다른 것은 **호스트**뿐이다: 탭에는 ✕이 없고, 담기 바가 카드 맨 아래에 붙으며, 담은 결과 카드는 탭의 상태에 머물고(줄 옆 카드는 `panelState`), 항목을 열면 `← 회의로` 없이 **그 줄 옆에** 상세 카드가 뜬다(회의 정리 화면이 뒤에 그대로 있으니 돌아갈 링크가 필요 없다). 담기 → 결과 카드 → `실행 취소` → 고쳐 둔 초안 문구 보존은 두 자리에서 똑같이 동작한다(고친 문구는 회의 키로 기억하는 한 저장소를 나눠 쓴다).

**회의 정리 카드**(460px)는 같은 부품을 쓴다. 머리는 제목 + `오늘 16:00–17:00 · 프로젝트` + ⋯ + ✕(업무 상세 카드 머리 `panelHead`와 같은 자리·같은 모양), 발은 **담기 바**(초안이 있을 때만): 왼쪽에 `할 일 1 · 확인 대기 1 · 결정 1`, 오른쪽에 1차 `N개 담기`. 미팅 노트 가져오기·가져온 표시는 머리 아래 별도 줄(`.d-dget` — 위 `회의 탭` 행 참고)이다. 머리의 ⋯는 레일의 오늘 미팅 줄 ⋯가 여는 메뉴(`회의 정리 열기` · `프로젝트 연결`)와 같은 함수(`meetingMenuSections`)를 그대로 연다 — 회의용 메뉴를 새로 만들지 않는다. 프로젝트를 바꾼 뒤에도 카드는 열린 채로 머리의 `오늘 16:00–17:00 · 프로젝트` 글자가 갱신된다. 결과 카드·초안·`이 회의에서 나온 것`·`이전 회차의 미해결 항목`·`직접 적어 담기`·`기존 항목 연결`은 가운데에서 스크롤한다. `이 회의에서 나온 것`·`이전 회차의 미해결 항목`은 구역 안을 **종류 소제목**(`.d-mgrp` — 13/600 `--muted` + 회색 숫자, 그룹 제목 부품이 아니라 조용한 한 줄)으로 나눈다: `할 일 3`·`확인 대기 1`·`결정 2`(버그는 할 일). 소제목이 서면 줄의 종류 칸(56px)은 통째로 빠지고(`.d-mrow2.no-tg`), 종류가 하나뿐이면 소제목을 세우지 않는다(구역 제목이 이미 개수를 말한다). 한 종류 안에 프로젝트가 둘 이상일 때만 그 아래를 프로젝트 소제목(`.d-mgrp.is-pj` — `· ● 이름`, 12.5/500 `--dim`)으로 한 번 더 나눈다(`이전 회차`는 나누지 않는다). 이 회의의 프로젝트가 아닌 줄은 제목과 한 칸을 나눠 쓰는 `.tw` 안에서 제목 바로 뒤에 `· ● 이름`(`uiInlineProject`)을 달고, 프로젝트 소제목으로 나눈 자리에서는 달지 않는다. 머리의 `오늘 16:00–17:00 · 프로젝트`에서 프로젝트 이름은 **회의 탭에서만** 버튼(`.pjlink` — 글자 모양 그대로, hover `--accent-text`)이고 누르면 왼쪽이 `프로젝트별`로 바뀌며 그 프로젝트만 펼쳐진다. 초안 묶음: 문구가 주인공인 회색 채움 입력(`.d-dtxt`) + 종류 세그먼트 + (할 일이면) `나중에 할 일 / 오늘 할 일` 세그먼트 + 날짜 칸, 오른쪽 위에 ✕. 회의 카드 안에서 항목을 열면 같은 카드 안에서 그 항목 상세로 바뀌고 맨 위에 `← 회의로`가 붙는다.

**`이 회의에서 나온 것` 줄**(`.d-mrow2`, `30px 56px minmax(0,1fr) auto 28px` = 체크 | 종류 | 문구 | 상태 | ⋯)**도 그 자리에서 고친다.** 맨 앞 체크 칸은 종류에 따라 목록이 쓰는 체크박스를 그대로 줄여 쓴다 — 할 일·버그는 완료 체크(`uiCheckCell`), 확인 대기는 확인 완료(`.d-wcb`), 결정은 `PRD 반영함`(`.d-check`+`.d-cb`); `.d-check`의 평소 높이(54px)는 `height: auto`로 눌러 촘촘한 줄에 맞춘다. 아이디어는 체크가 없고 자리만 비워 다른 줄과 제목 시작을 맞춘다. 체크하면 목록과 같은 완료 순간(체크 → 줄 긋기 → 옅어짐)을 거쳐 저장되고, 회의 카드/탭은 열린 채로 그 줄이 완료 모양으로 남는다(`이전 회차의 미해결 항목`은 미해결만 보여 주므로 완료하면 그 구역에서 빠진다 — 구역이 비면 통째로 사라진다). 체크박스는 클릭되면 초점을 받는데, `isTyping()`이 이를 "문구를 고치는 중"으로 오인해 새로고침을 건너뛰지 않도록 체크한 뒤 바로 초점을 내린다. 문구를 고치는 동안은(`.d-din`이 열려 있는 동안) 그 줄의 체크박스를 잠가 누를 수 없다. 이 줄에서 할 수 있는 다른 일은 더보기뿐이라 ⋯은 늘 보인다(평소 흐리게, 손이 닿으면 진하게, 28px). 메뉴도 그 종류의 목록 줄이 쓰는 메뉴 그대로에 맨 위 `문구 고치기` 한 줄만 얹는다 — 회의용 메뉴를 따로 만들지 않는다. `문구 고치기`는 제목을 줄 높이의 채움 입력(`.d-din`, 28px)으로 바꾸고 Enter로 저장·Esc로 입력만 되돌린다(회의 카드는 닫히지 않는다). 할 일·확인 대기의 제목은 눌러서 상세를 열고, 상세가 없는 결정은 제목을 누르는 것이 곧 `문구 고치기`다. 메뉴는 카드 밖으로 넘칠 수 있지만 화면 안에 갇힌다.

**결과 카드**는 담은 직후에 뜨는 **연초록 면**(`--success-bg`, 12px): `✓ 3개 담음` + 종류별 개수 + `실행 취소`, 나중에 담긴 할 일을 그 자리에서 `오늘로`, 맨 아래 다음 검토할 회의. 이 면 위의 3차 버튼은 회색 대신 흰 알맹이가 된다.

### 더보기 메뉴 (한 벌)

모든 줄·상세의 ⋯가 같은 컴포넌트를 쓴다(`aria-haspopup`, `aria-expanded`, `role="menu"` / `role="menuitem"`).
**줄에서 할 수 있는 일이 더보기뿐인 곳은 ⋯을 항상 보인다**(아이디어·결정·반영 완료 줄 — 28px 영역, 평소 `--dim`, hover·focus에서 `--text`). hover에서야 뜨는 동작 버튼이 함께 있는 줄(오늘 목록·새로 들어온 것)은 지금대로 ⋯도 그때 나타난다.
`position: fixed`, 폭 304px(최대 `100vw - 24px`), `--surface`, 18px 모서리, `--pop`, 안쪽 6px. 항목 38px / `0 12px` / 14px 500 / 10px 모서리, hover `--hover`. 구분선 1px `--hair`. `삭제`는 맨 아래 구분선 뒤이고 평소엔 `--muted`, hover에서만 `--urgent`.
**필드 줄**: 라벨(12.5 `--muted`, 최소 52px) + 오른쪽 정렬 컨트롤(칩 30px 999px — 현재 값만 `--accent-soft`; 글자·선택·날짜 칸 30px 12px 모서리).
버튼 오른쪽 끝에 맞춰 4px 아래에 열리고, 자리가 없으면 위로 뒤집힌다(가장자리 8px 유지). 열리면 첫 항목에 포커스, ↑↓ 이동, Esc는 닫고 ⋯로 포커스를 돌려준다. 한 번에 하나만 열린다. 등장 120ms.

### ⌘K 검색 팔레트

가림막(`--scrim`) 위, 위에서 84px, 폭 `min(620px, 100vw - 32px)`, 높이는 내용만큼(`max-height: calc(100dvh - 100px)`), 20px 모서리, `--pop`, 등장 140ms. **찾는 창이다** — 훑어보며 몰아서 정리하는 일은 `회의` 탭이 맡는다(DECISIONS 2026-09-22).
- **입력 줄** 60px, 여백 `0 20px`, 17px 글자 + 17px 돋보기.
- **필터 줄** 회색 트랙 위 종류 칩(`전체 할 일 확인 대기 결정 아이디어 회의`) + 트랙 밖 토글 칩(`완료 제외`). 회의 전용 토글(`초안 있음`·`미완료만`)은 회의 탭에 있다. `오늘 신규` 필터가 걸리면 지울 수 있는 칩이 붙는다.
- **결과 줄** 최소 46px, 12px 모서리. 항목은 `28px 62px minmax(0,1fr) auto` = 체크박스 | 종류(12.5 `--dim`) | 제목(15px, 일치 부분 `--mark`) + `· ● 프로젝트` | 상태. 맨 앞 28px은 완료 체크 칸이다(할 일·버그·확인 대기만 — 목록과 같은 `.d-check`+`taskCompletionCheckbox`, 결정·아이디어는 같은 크기의 빈 칸으로 자리만 지킨다). 회의는 이 칸이 없고 예전 그대로(앞에 날짜·시각 열이 하나 더 붙는다). 상태 칸은 오늘 목록과 같은 규칙(급한 것만 배지)에 더해, 완료가 아니고 오늘이 아닌 할 일·버그에는 조용한 글자 버튼 `오늘로`(`.d-headnum`)가 옆에 붙는다 — 체크·`오늘로` 모두 클릭이 줄 열기로 번지지 않고, 처리해도 팔레트는 닫히지 않는다. 좁은 화면(≤520px)에서도 체크박스 칸은 줄지 않고 제목이 먼저 준다. 고른 줄 `--sel` + 제목 600.
- **바닥** `↑↓ 이동 · Enter 열기 · Esc 닫기`(12px `--dim`). `회의` 칩일 때만 오른쪽 끝에 글자 링크 `회의 탭에서 모두 보기`.
- 회의 결과를 열면 `회의` 탭에서 그 회의를 고른 채로 열린다(팔레트는 닫히고 돌아오지 않는다).
- 닫으면 찾던 자리(검색어·필터·스크롤·활성 줄)로 그대로 돌아온다.

### 여러 개 선택 막대 · 문장 모으기 막대

화면 아래에 떠 있는 흰 둥근 막대(20px, `--pop`, 최대 1120px, 여백 `10px 14px`). 왼쪽부터 **`2개 선택`(15/700 — 가장 먼저 읽힌다)** → `전체 선택` 글자 링크 → 동작들 → (오른쪽) `선택 끝내기`. 동작 중 `완료로 표시`만 2차(연파랑)이고 `삭제`는 빨간 글자, 나머지는 3차다. 아무것도 고르지 않았으면 바꾸는 버튼은 눌리지 않는다.

### 나중에 할 일 서랍

`--bg` 판(404px) + `--drawer-sh`, 그 안에 흰 카드 목록 하나. 머리 60px(제목 17/700 + 회색 개수 칩 + 닫기), 회색 알약 빠른 추가, 프로젝트 그룹. 좁은 면이라 줄은 두 줄형이다: `30px minmax(0,1fr) max-content`, 최소 58px, 제목 최대 2줄, **프로젝트 열은 접고 상태 열은 내용만큼만** 잡는다(그러지 않으면 좁은 서랍에서 제목 폭이 0이 된다).

### 새 프로젝트 화면 (프로젝트 탭 오른쪽 면)

왼쪽 목록 머리의 조용한 `+`(`.d-pnewgo`, 28px 정사각, `--dim` 17px, hover에서 `--hover` + `--accent-text`)가 여는 **한 장짜리 폼**이다. 새 판·새 색을 만들지 않는다 — 입력칸은 `.d-din`, 세그먼트·버튼 세 등급·확인 줄(`.d-jconfirm`)·연결 입력칸(`.d-jlink`)을 모두 있는 것 그대로 쓴다.

- **줄 구성**(`.d-pnew .row`)은 `92px | 나머지` 격자다. 라벨은 13.5/600 `--muted`, 값 아래에 조용한 안내 한 줄(12.5 `--dim`). **≤640px에서는 라벨이 값 위로 쌓인다.**
- **이름 감지 줄**(BMOVE ①, `.d-pnewmove`)은 이름 칸 바로 아래, 이름(또는 고른 에픽 요약)과 같은 이름의 그룹 프로젝트가 있을 때만 서는 조용한 한 줄(`.d-quiet`)이다: `결제 리뉴얼 프로젝트가 이미 있어요(항목 12)` + `.sep` + 체크박스(`.rl` 줄, `.d-wcb` — 위 직군 체크와 같은 부품) `만든 뒤 이 에픽으로 옮기기`(기본 켜짐). 새 판·새 색은 없다.
- **지라 세 모드**는 값을 고르는 세그먼트라 `aria-checked`다(`role="radiogroup"`). `있는 에픽에 붙이기`는 **프로젝트 탭의 `지라 티켓 연결` 입력칸을 그대로 빌려 쓰고**(버튼 글자만 `이 에픽에 붙이기`) 고르고 나면 `--sel` 판의 한 줄(`.d-pnewepic`: 요약 굵게 → 상태 → 조용한 키 → 오른쪽 `다시 고르기` 글자 링크)로 바뀐다.
- **직군**은 앱의 체크박스 가족 중 `.d-wcb`(20px, 28px 누름 영역)를 쓴다. 이미 그 접두어의 하위가 있는 줄은 통째로 `--dim`이고 뒤에 12px `이미 있어요 IO-48401`이 붙는다(배지가 아니다). 그 자리에서 적은 직군은 `✕`가 달린 조용한 칩 한 줄이다.
- **미리 보기**(`.d-pnewpv`)는 흰 카드 한 장이다. 첫 줄은 `에픽`(32px 고정 폭 라벨) + 요약 + 조용한 `새로 만듦`/`IO-48394에 붙임`, 아래는 `├`·`└` + **제목 입력칸**(`.d-din`, 32px) + 종류 + `담당 없음`. ≤640px에서는 제목 칸이 한 줄을 통째로 쓴다.
- **주 버튼은 이 면에 하나**(1차 파란 채움 `지라에 N개 만들기`)이고 옆의 `취소`는 3차다. 누르면 곧바로 나가지 않고 **지라 값 바꾸기와 같은 확인 줄**(`.d-jconfirm`, `--sel` 판 · 묻는 말 굵게 · 오른쪽 `취소`/`만들기`)이 그 자리에 선다.
- **결과**(`.d-pnewres`)도 흰 카드 한 장이다. 머리 15/700, 줄마다 `에픽`/`만듦`/`실패` 라벨(12/600 `--dim`) + 요약 + 조용한 키 또는 **위험색 이유 글자**. 실패가 있을 때만 2차 `실패한 것 다시 시도`가 맨 앞에 서고, 가는 선 아래에 `첫 할 일`(선택) 입력 한 줄이 붙는다. **에픽은 만들어졌는데 나에게 배정만 실패했으면**(`assignError`) 같은 줄(`.kid`) 끝에 `.note`(12.5 `--dim`, 기존 미리 보기·안내 문구와 같은 톤)로 `담당자 지정은 안 됐어요 — 지라에서 직접 정해 주세요`가 붙는다 — 만들기 자체는 성공이라 `.er`(위험색)이 아니라 조용한 회색이다. 하위 줄에는 이 문구가 붙지 않는다(하위는 배정을 아예 시도하지 않는다). **옮긴 결과**(BMOVE ①) — 감지 줄 체크가 켜진 채 만들었으면 에픽 줄 아래에 한 줄이 더 선다: 성공이면 `.d-quiet` `항목 12개를 옮겼어요` + `.sep` + `되돌리기`(`.d-link`), 실패하면 `.note`(조용한 회색) `옮기지는 못했어요 — 프로젝트에서 직접 옮길 수 있어요`.

### 오늘 정리 (모달)

설정과 **같은 판**(`.d-modal` — `<dialog>` 20px 흰 카드, `--pop`, 머리 `.d-mhd` + 본문 `.d-mbody`)이고 폭만 `min(720px, 100vw - 32px)`다(줄마다 세그먼트를 들고 있어 620px로는 좁다). **≤900px에서는 설정 창과 같은 전체 폭 판**(`100vw - 16px`)이 된다.

- **머리**는 제목 18/700 + 그 뒤 조용한 날짜(`9월 22일 (화)`, 13 `--dim`) + 오른쪽 닫기다. 본문 첫 줄은 조용한 요약 한 줄(`8개 중 3개 끝냈어요 · 남은 5개`, 13 `--dim`).
- **구역 제목**은 목록의 그룹 제목(`.d-grp`)을 그대로 쓴다: 접히는 `끝낸 것 N`(0개면 세우지 않는다) + 붙박이 `남은 것 N`. 펼친 완료 줄(`.d-wrapdone`)은 13 `--dim` 한 줄 말줄임이고 체크도 동작도 없다.
- **줄**(`.d-wraprow`, 최소 44px, 줄 사이 `inset 0 1px 0 --hair`)은 `제목 · ● 프로젝트 | 상태말 | 세그먼트`다. 상태말은 업무 줄과 **같은 칸**(`.d-meta`의 `m-carry`·`m-doing`·`m-due`)이라 배지 규칙도 같고, 프로젝트는 `.d-inproj` 그대로다(지라는 요약만).
- **세그먼트**는 값을 고르는 자리라 `.d-seg` + `aria-checked`(`role="radiogroup"`, ←→로 옮긴다)다. 칸은 넷: `그대로 | 내일 | 나중에 | 완료`, **기본은 늘 `그대로`**다.
- **아래 줄**(`.d-wrapfoot`)은 본문 바닥에 붙박이다(상세 카드의 `.d-dbar`와 같은 방식 — 줄이 많아도 주 버튼이 보인다): 1차 파란 채움 하나(`바꿀 게 없어요`(비활성) ↔ `정리 끝 — 내일 2 · 나중에 1 · 완료 1`) + 3차 `취소`.
- **≤520px에서는 줄이 두 줄로 쌓인다**: 제목·프로젝트 줄 → 상태말 → 세그먼트가 줄 폭을 통째로 쓰고 넷이 고르게 나눠 선다.

### 설정 (모달)

`<dialog>` 20px 흰 카드, 폭 `min(620px, 100vw - 32px)`, `--pop`. 머리(여백 `14px 14px 14px 20px`, 아래 `--hair`): 제목 18/700 + 세그먼트 `연동`/`앱`/`꾸미기`/`도움말`/`삭제한 항목 N`(개수는 0이면 붙지 않는다, 처음 열면 `연동`) + 닫기. 본문 여백 `14px 16px 18px`. **≤520px에서는 세그먼트가 제목 줄 아래로 통째로 내려오고 다섯 칸이 폭을 고르게 나눈다**(한 줄에 들어가지 않는다).
**앱 탭**은 꾸미기 탭과 같은 줄 넷(`.d-pset` — `이름 | 값`, 이름 칸 위 여백 2px): `버전`(작은 설명 `워크스페이스`) · `데이터 백업`(`매일 19:30`) · `앱 위치`(`파일을 찾을 때`) · `문제 보고`. 연결 상태는 이 탭에 없다(연동 탭 하나).
- **버전** 값은 14px `--text` 한 줄(`.d-abline`): `v1.2.0 · 배포된 버전만 받기`(`main`이면 `만드는 중인 것까지 받기(main)`, 개발용이면 `· 개발용`) + (고친 파일이 있으면) 밑줄 글자 버튼 `· 수정된 파일 3개`(누르면 그 아래 파일 이름 목록 12 `--dim` — 경고 색이 아니다). 새 버전이 있으면 그 아래 `.d-abupd`(`--sel` 판, 12px 14px, `--r-sm`) — `새 버전 v1.3.0이 있어요`(14/600 `--accent-text`, 한 줄 가득) 다음 줄에 1차 `업데이트 받기`(`.d-btn.sm.pri`)와 밑줄 글자 `무엇이 바뀌었나요 ↗`(`--accent-text`, 28px), 상자 아래 조용한 `.d-ismall` 한 줄. 누르면 그 자리가 진행 목록 `.d-abprog`(줄 30px 13.5, 아이콘 18px 칸 — `✓` `--success` · `⟳` `--accent` 회전(동작 줄이기면 멈춤) · `·` `--dim`, 도는 줄은 600) + 위 `v1.0.0 → v1.1.0`(12.5 `--dim`) + (재시작 중) `.d-quiet` `다시 켜는 중…`이 된다. 성공은 같은 `.d-abupd` 판에 `v1.1.0으로 바꿨어요` + 1차 `새로고침`, 실패는 `.d-abfail`(`--urgent-bg` 판, 13.5/600 `--urgent`) + `.d-btn.sm.dng`(판 `--surface`) `이전 버전으로 되돌리기` → 해제 확인 줄과 같은 `.d-iconfirm`. 막힌 이유(폴더 이동·처음 한 번)는 `.d-abupd.is-plain`(13.5/500 `--text`) + 경로 `.d-icode` `복사`.
- **데이터 백업**은 `.d-bkline`(14 `--text`, tabular-nums) 한 줄 `● 이 맥에 매일 백업 · 어제 19:30 · 7일치`(점만 `--success`) + (GitHub을 켠 사람만) `.d-ismall` `GitHub 비공개 저장소에도 올려요 · 어제 19:30` + 경로 `.d-icode` `~/workspace-data-backup/daily` `복사` + `.d-ismall` `Finder에서 ⇧⌘G(폴더로 이동)에 붙여 넣으면 바로 가요`. 멈추면 `.d-bkline.k-neg`(`--urgent` 600, 점 `--urgent`) `● 이 맥 백업이 멈췄어요 · 오늘 19:30` + 이유 한 줄 `.d-ismall.k-neg`(`--urgent`). 아직 한 번도 안 돌았으면 점 없이 `오늘 19:30에 처음 백업해요`.
- **앱 위치**(`.d-pplace`, 이 탭에서는 위 여백 없음) — `Dock 앱`/`업데이트 파일` 이름(12/600 `--muted`) 아래 경로 `.d-icode` + `복사`, 그리고 `Finder에서 ⇧⌘G(폴더로 이동)에 붙여 넣으면 바로 가요`. 사용설명서에서 찾아오면 이 줄을 `--sel` 판으로 밝힌다.
- **문제 보고** — 2차 `진단 내용 복사`(`.d-btn`, 왼쪽 정렬) + `.d-ismall` `버전·연동 상태·최근 오류만 복사해요(업무 내용은 들어가지 않아요).`
톱니바퀴 점의 차례는 **빨강(지금 멈춘 연동) > 주황(낡음) > 파랑(새 버전, `--accent`)**이다. 빨간 점이면 연동 탭으로 열고 멈춘 카드를 잠깐 붉힌다(아래), 파란 점만 있으면 앱 탭으로 연다.
**연동 탭**은 연결이 잘 되는지 보는 **유일한 곳**이다. 맨 위 한 줄 요약(`.d-intghead`, 13.5 `--muted` — 멈춘 게 없으면 `.lead` `● 연결 N개 모두 잘 읽고 있어요`(점만 `--success`), 있으면 `.lead.k-neg` `● N개가 멈췄어요`(`--urgent` 600), 연결이 하나도 없으면 `연동은 선택이에요. 필요할 때 하나씩 켜요.` + 오른쪽 `.d-quiet` `연결됨 N · 남은 것 M`) 아래 **카드 넷**(`.d-intg`, 안쪽 `12px 0`, 카드 사이 `inset 0 1px 0 --hair`)이 서고 맨 아래 `.d-intgfoot`(위 `--hair` 선, 12.5 `--dim`: `다른 도구를 쓰고 있어요 → 요청하기 · 각자 붙이는 법 ↗` 밑줄 글자 + 자동화 등록 안내 한 줄)으로 끝난다. 카드 머리(`.d-intgtop`, 최소 38px, 줄바꿈 허용)는 `이름 14.5/600 · 필요한 것 칩 | (연결됨) 상태 줄 · ⋯ | 버튼`이다.
- **필요한 것 칩**(`.d-itag`)은 **회색 글자 칩 하나로 통일**한다 — 22px, 6px 모서리, `--neutral-bg` 위 12/600 `--muted`(`누구나` / `누구나 · Claude` / `Claude Code 필요`). 주황 칩은 쓰지 않는다.
- 머리 아래 한 줄 효용(`.d-intguse`, 13.5 `--muted`)과 둘째 줄(`.d-intgneed`, 12.5 `--dim`, tabular-nums — 연결 전엔 준비물 `5분 · 팀 슬랙 앱 토큰 하나`, 연결된 뒤엔 주기·오늘 수치: `매일 9–19시, 5분마다 · 오늘 새 항목 6개` · `내 티켓 12개 · 반응 필요 댓글 2개 · 1분 전 확인` · `30분마다` · `회의가 끝나면 회의 탭에서 가져오기`; 반응 필요를 다시 읽지 못하면 이 줄이 `k-warn`). 슬랙에서 사라진 채널은 그 아래 같은 부품의 `k-warn` 줄(주의색 글자, 배지 아님) `#이름을 찾을 수 없어요 — 슬랙에서 지웠거나 보관했어요 · 채널 고르기`(밑줄 글자).
- **멈춘 카드**(시안 B): 상태 줄이 빨간 글자 `● 읽지 못했어요 · 12분 전`(할 일 채널이 사라졌으면 `● #my-todo을 찾을 수 없어요`)이 되고 바로 아래 이유 한 줄 `.d-intgwhy`(12.5 `--urgent`, 안의 밑줄 글자도 `--urgent`) — `슬랙이 응답하지 않았어요 — 다시 시도해도 안 되면 ⋯ › 최근 기록`, 토큰 문제면 버튼이 `다시 연결`이고 이유 줄도 그 말. 빨간 점을 누르고 들어오면 그 카드가 약 2초 `.is-flash`(`--urgent-bg` 판, `--r-sm`, 좌우 -10px 내어 쓰기, 바탕 전환 400ms — 동작 줄이기면 전환 없이 바탕만)로 보인다.
- **⋯ › 최근 기록**(시안 C)은 펼치는 자리(`.d-intgbody`) 안의 목록 `.d-ilog`(13 `--muted`, tabular-nums, 줄 간격 4px): 줄마다 시각(`.t` 최소 44px `--dim`, 오늘이면 `10:05`, 아니면 `9/23 10:05`) + 결과 한 줄(실패는 `.bad` `--urgent`), 최근 10개. 맨 아래 밑줄 글자 `오류 전문 복사`(28px). 슬랙은 목록 위에 처리 대장 `.d-autonote`(12.5 `--dim`): `최근 수집 · 본 메시지 12개 → 등록 3 · 중복 7 · 건너뜀 0 · 시스템 2` — 넷 중 0인 항목만 한 단계 더 흐리게(`.is-zero`, opacity .55), 합이 안 맞으면 `합이 안 맞습니다 …` 문장 그대로에 `k-warn`, 그 아래 `건너뛴 것: …`(최대 3개)·`⚠️ …`(최대 2개) 한 줄씩. 기록이 없으면 메뉴에 그 칸이 없다.
- **연결 안 된 카드**는 오른쪽에 2차 `연결하기`(`.d-btn.acc`, 회의록만 `.d-btn.sm` `바꾸기`), **연결된 카드**(티로로 받는 회의록 포함 — 그 ⋯에 `바꾸기`)는 상태 줄(13 `--dim`, 앞에 `--success` 점 `●`, 말줄임)과 ⋯(`uiMoreButton`)만 둔다. 펼치면 버튼이 `접기`(`.d-btn.sm`)로 바뀌고, 한 카드를 펼치면 나머지는 접힌다.
- **위저드**(`.d-intgbody`)는 `--field` 판(12px 모서리, 안쪽 14px, 세로 12px 간격, 안의 입력칸은 `--surface`) 위에 **한 번에 한 단계만** 보인다. 맨 위 단계 줄 `.d-isteps`(12/600 `--dim`, 16px 가는 선 `.ln`): 지금 단계 `--accent-text`, 끝낸 단계는 `--success` + `✓`로 접힌다. 안내 문장 `.d-ihow`(13 `--muted`, 1.7, 굵은 글자 `--text`), 조용한 한 줄 `.d-ismall`(12.5 `--dim`), 아래 줄 `.d-ifoot`(왼쪽 `← 이전` `.d-btn.sm`, 오른쪽 1차 `다음 →`/`연결`). 링크형 동작(`토큰 받는 곳 열기 ↗`·`토큰 만들기 ↗`)은 새 탭 `<a class="d-btn acc">`다. 작은 동작(`만든 사람에게 요청 문구 복사`·`복사`)은 `.d-btn.xs`(28px, 12.5).
- **채널 줄**(`.d-ich`, `20px | 1fr | 180px` 격자, `--surface` 판, 고른 줄은 `--sel`): 체크 · 이름(600)과 `필수`(11.5/600 `--accent-text`) · 들어가는 곳(12.5 `--dim`) · 이름 칸(32px `.d-din`, 고르지 않은 줄은 흐리게 꺼짐). 위저드 ②에서 만든 줄·이미 연결된 줄은 이름 칸 대신 `✓ #my-todo 만들었어요` / `#my-todo 연결됨`(12.5/600 `--success`). 줄 오류는 그 줄 안 `.d-derr`. **≤520px에서는 이름 칸이 둘째 줄로** 내려온다.
- **⋯ › 채널 고르기**(시안 E·F)는 같은 줄 부품으로 **더하기·빼기**를 한 화면에서 한다(단계 줄 없음): 연결된 채널은 이름 칸 대신 고정 글자 `.lock`(13/600 `--text` `#my-todo` + 아래 작은 `연결됨` 11.5 `--dim`), 새 줄은 `.nmwrap`(칸 위 작은 라벨 `새 채널 이름` 11.5 `--dim` + 32px 칸). `할 일`은 `필수 — 뺄 수 없어요`에 체크 칸이 꺼져 있다. 연결된 줄의 체크를 풀면 그 줄이 `.is-off`(`--urgent-bg` 판, 이름에 가운데 줄 `--dim`, 들어가는 곳 자리에 `빼요` `--urgent`), 사라진 채널(다시 만들기)은 `.is-gone`(`--warn-bg` 판). 목록 아래 안내 `.d-inote`(`--neutral-bg` 판, 13 `--muted`, `ⓘ` + 굵은 `이름은 여기서 바꿀 수 없어요.`), 빼는 줄이 있으면 누르기 전에 확인 줄 `.d-iconfirm`(문장만, 버튼 없음) `#이름을 빼면 앱이 이 채널을 더 이상 읽지 않아요. 슬랙 채널과 이미 들어온 항목은 그대로예요.`, 다시 켜는 줄이 있으면 `.d-ismall` `#이름을 다시 연결해요 — 뺀 동안 온 메시지는 가져오지 않아요`. 아래 줄 `.d-ifoot`: 왼쪽 `취소`(`.d-btn.sm`), 오른쪽 1차 — 문구가 고른 대로 `1개 만들기` / `1개 빼기` / `1개 만들고 1개 빼기`(다시 켜기는 `1개 다시 연결하기`), 바꾼 게 없으면 꺼진 `바꾼 것이 없어요`. 체크를 바꿔 다시 그려도 초점은 그 체크 칸에 남는다.
- **③ 확인** 줄 `.d-iok`(`--success-bg` 판, 14/600 `--success`) + **보내는 법 상자** `.d-isend`(`--warm` 판 — 사용설명서 카드와 같은 따뜻한 색, 제목 13.5/700 + 번호 목록 13 `--muted`). 붙여 넣을 명령은 `.d-icode`(`--surface` 판, 고정폭 12 + `복사`), 번호 단계는 `.d-inum`(번호 18px `--dim`).
- **캘린더 갈래**는 `.d-ichoice` 둘(`--surface` 판, 1.5px `--border`, 240px 이상이면 나란히): 누구나 되는 `비밀 주소 붙이기`가 먼저(번호 세 줄 `.d-inum` + 가려진 칸 + 1차 `연결` + `.d-ismall` 한 줄), `Claude Code로`가 다음. 연결된 카드의 상태 줄은 `비밀 주소로 읽는 중 · 오늘 3개 · 10분 전`이고, 못 읽고 있으면 점이 `--urgent`인 `비밀 주소를 읽지 못했어요`다. `⋯ › 다시 연결(주소 바꾸기)`은 비밀 주소 갈래 하나만 편다.
- **해제**는 큰 버튼 없이 ⋯의 `해제…`(`.d-mitem.dng`) → 카드 안 **확인 줄** `.d-iconfirm`(`--urgent-bg` 판: 문장 13 `--text` + 2차 `취소` + 위험 `해제`(`--surface` 바탕 `.d-btn.dng`)).
- 토큰 칸은 늘 `type="password"`, Enter는 다음 단계(한글 조합 중 무시), 펼친 뒤 커서는 그 단계의 첫 칸으로 간다. **≤520px에서는 상태 줄이 머리 아래 한 줄로** 내려오고 ⋯·버튼은 오른쪽에 남는다.
**꾸미기 탭**(이 맥에만)은 줄 셋(`.d-pset` — `이름 130px 14.5/600(+작은 설명 12 --dim) | 값`, 줄 사이 `inset 0 1px 0 --hair`): `Dock 아이콘`(56px 미리보기 `.d-piconimg` 14px 모서리 `--shadow` + 2차 `내 그림 고르기`(`.d-btn.acc`) + 내 그림이 있을 때만 밑줄 글자 `기본으로 되돌리기`, 아래 `PNG·JPG — 정사각형으로 잘라요` `.d-ismall`; 고르면 미리보기가 가운데를 자른 캔버스로 바뀐다) · `Dock 이름` · `워크스페이스 제목`(입력칸 `.d-din` 폭 가득). 아래 `.d-pfoot`: 조용한 `이 맥에만 적용돼요 — 다른 사람 앱에는 영향이 없어요(업데이트해도 남아요)` + 1차 `저장`. 저장 뒤 `.d-psaved`(`--success-bg` 판, 13/600 `--success`) `✓ 바뀌었어요` + (Dock이 바뀌었으면) 400 `--muted` ` · Dock은 앱을 닫고 다시 열면 보여요`. `앱 위치`는 앱 탭으로 옮겼다. **≤520px에서는 이름이 값 위로** 올라간다(앱 탭도 같다).
**도움말 탭**은 (사용설명서 카드를 닫았으면 맨 위에 `.d-manual`) 첫 문단 한 줄(`.d-faqintro` 13 `--muted`) → 개념 한 줄 사전(`.d-words`, `--neutral-bg` 판 위 12.5 `--muted` 9줄, 말은 600/`--text`) → 묶음 제목(`.grp` 12/600 `--dim`, 위 `--hair`) → 문답(질문 14/600, `필요한 것: …` 11.5 `--dim`, 답 13 `--muted` 1.65, 최대 64ch) 차례다.
**삭제한 항목 탭**(`.d-trow`)은 자동화 줄과 같은 리듬의 한 줄이다(안쪽 `8px 0`, 줄 사이 `inset 0 1px 0 --hair`): 왼쪽에 종류 글자(12/600 `--muted`) + 문구(14.5, 한 줄 말줄임 + `title`) + (있으면) 프로젝트 `· ● 이름`(`.d-inproj` 그대로), 그 아래 조용한 `9월 21일 22:10에 삭제`(12.5 `--dim`, tabular-nums). 오른쪽은 2차 `되살리기` + ⋯(28px, 평소 `--dim`·손이 닿으면 `--text`)이고, ⋯의 `완전히 지우기`만 위험색(`.d-mitem.dng`)이다. 누르면 그 자리에 확인 줄이 선다 — `되살릴 수 없어요`(12.5 `--muted`) + 위험색 `지우기` + 2차 `취소`, 취소하면 동작 묶음이 그대로 돌아온다. 비어 있으면 빈 화면 한 줄(`삭제한 항목이 없어요.`). **모달 안에서 연 ⋯ 메뉴는 그 `<dialog>` 안에 붙인다** — 맨 위 층 밖이면 보이기만 하고 눌리지 않는다(자리는 `position: fixed`라 그대로다).

### 주간요약 문서

세 개의 흰 카드다. 가운데 문서 카드가 주인공이고, 그 안은 **흰 카드 위의 문서**처럼 읽힌다(안쪽 28/32px, 본문 최대 720px).

- **머리줄** 제목 `이번 주`(22/700) + 기간 + 세그먼트(`보고`/`전체 업무 기록`) + (있으면) `되돌리기` + (이번 주만) 3차 `다음 주 계획 쓰기` + 오른쪽 끝 **1차 `슬랙용으로 복사`**. 복사 버튼은 여기 한 곳에만 둔다. `다음 주 계획 쓰기`는 계획 구역으로 스크롤하고 맨 아래 입력칸에 초점을 준다 — 금요일에 가장 먼저 하는 일이라 긴 문서를 훑지 않게.
- **조용한 한 줄** `보고 8문장 · 근거 업무 8개`(13 `--dim`), 확인이 필요한 문장이 있으면 그 자리로 데려가는 글자 링크.
- **위계** 상태 제목(12.5/600 `--dim`, 위 1px `--hair`) → 프로젝트 소제목(16/700 `--text`) → **문장(15.5 / 1.65 — 이 화면에서 가장 큰 본문)**.
- **문장 줄** `22px minmax(0,1fr) auto` = 글머리 | 문장 | `수정` `제외` (아래로 들어간 문장은 `따로 빼기`) ⋯ — hover·focus에서만(≤520px는 항상). 손으로 고친 문장에만 `직접 수정`, 자동 초안에는 아무 표시도 하지 않는다.
- **아래로 들어간 문장**(`.rp-s.is-sub`)은 왼쪽 18px 들여 쓴 `◦` 줄이고 글자는 한 단계 조용하다(13.5 / 1.6 `--muted` — 여러 줄 글자의 부연과 같은 가족). 프로젝트가 부모와 다르면 글 뒤에 `· 이름`(12 `--dim`)을 조용히 적는다(문서에서만).
- **수정**은 그 자리에서 textarea(15.5 / 1.65, 회색 채움 + 파란 테) + `저장`(1차) `취소`.
- **근거 업무**는 문장 아래 왼쪽 1px `--border` 선으로 들여쓴 목록.
- **모으기 모드**(문장 ⋯ → `이 아래로 문장 모으기`)는 아래 고정 막대를 쓴다 — 일괄 선택 막대와 **같은 부품**(`.d-selbar`): `「기준 문장 앞부분…」 아래로` + `넣을 문장을 눌러 주세요` + 1차 `완료`. 기준 문장은 `--sel` 바탕 + 연파랑 알약 `여기 아래로`, 넣을 수 있는 문장만 밝게 서서 줄 전체가 과녁(누르면 바로 들어간다, Tab·Enter로도), 나머지는 45%로 흐리고 눌리지 않는다. 모드 중에는 줄의 동작 묶음을 그리지 않는다. 옛 합치기 문장 줄의 ⋯에는 `묶음 풀기`가 하나 더 붙는다(서버가 묶기 전 문장을 들고 있는 행에만).
- **한 줄로 모으기 고르기 모드**(문장 ⋯ → `한 줄로 모으기…`)는 모으기 모드와 **같은 막대**(`#reportNestBarEl`, `.d-selbar`)를 나눠 쓴다(둘은 함께 열리지 않는다) — 막대 글자 `한 줄로 모을 문장을 골라요 · N개` + 1차 `한 줄로 모으기`(2개 미만이면 비활성) + `취소`. 시작한 문장은 이미 골라진 상태로 `--sel` 톤이고(모으기 모드의 기준 문장과 같은 톤을 빌려 쓴다 — 새 색 없음), 같은 소제목의 최상위·아래에 문장이 없는 문장만 밝게 서서 눌러 고르고 뺄 수 있다. 확인하면 그 자리에서 새 요약 문장이 인라인 `수정` 칸으로 열리고 글 전체가 선택된다.
- **접힌 요약 문장**(`한 줄로 모으기`로 만든 부모, 기본이 접힘)은 문장 뒤에 조용한 글자 버튼 `· N건 ▸`(28px 누름 영역, `--dim`, hover에 `--hover` 배경)이 붙는다. 누르면 아래 문장이 화면에서만(저장 안 함) `.rp-s.is-sub`와 같은 흐린 모양으로 펼쳐 보이고 버튼이 `· N건 ▾`로 바뀐다 — 새로고침하면 다시 접힌다. 펼쳐진 부모(`folded:false`, `setFolded`로 펼친 것 포함)는 지금 nest 묶음과 똑같이 들여쓴 항목으로 늘 보인다.
- **자리를 옮긴 결과**는 알림으로 말한다: `문장을 아래로 넣었어요` · `따로 뺐어요` · `묶음을 풀었어요` · `한 줄로 모았어요 · N건` + 알림 안의 `되돌리기`(그 밖의 접힘·펼침·풀기 등은 예전대로 `보고 내용을 저장했어요`). 이 탭에서는 입력칸 밖의 **⌘Z**도 같은 되돌리기이고, 머리줄의 `되돌리기` 버튼은 그대로 남는다 — 되돌릴 길이 셋이어도 부르는 곳은 하나다.
- **다음 주 계획**은 문서의 마지막 구역이고 빠른 추가 부품을 쓴다(48px 맨 줄, 초점에서만 채움 + 파란 테). 사람이 직접 쓴 문장만 들어간다(DECISIONS) — 앱은 후보를 보여 줄 뿐 담는 것은 사람이 누른 것만이다. 차례는 **담은 문장 → `다음 주 후보` → 맨 아래 입력줄 + 토글**이다.
  - **프로젝트 소제목**(`.rp-pj`)에 hover·focus하면 이름 오른쪽에 조용한 `+ 추가`가 뜨고(≤520px는 항상), 누르면 그 묶음 아래에 목록의 `+ 이 그룹에 추가`와 같은 입력줄(`.rp-add.is-group`, 42px)이 열린다. 저장 뒤 문서를 다시 그려도 그 줄은 열린 채로 초점이 돌아온다.
  - **`다음 주 후보`**(`.rp-cands`) — 조용한 소제목(12.5/600 `--dim`) + 오른쪽 3차 `나중에 할 일에서 고르기`. 줄(`.rp-cand`, 34px)은 `minmax(0,1fr) auto auto` = 제목(13.5, 말줄임) \| 기한(12.5 `--dim`) \| 3차 `+ 담기`. 이미 담은 줄은 45% 흐리고 `담음`이 그 자리에 선다. `나중에 할 일에서 고르기`를 켜면 같은 자리가 나중 목록이 되고 위에 32px 거르기 칸이, 목록에는 `max-height: 320px` 스크롤이 붙는다. 후보 글자는 **문서 화면에만** 있고 슬랙 복사 모델(`reportSlackModel`)은 후보를 모른다.
  - **`나중에 할 일에도 추가`**(`.rp-also`, 26px 토글 칩)는 입력줄 바로 아래의 조용한 한 줄이다. 기본 켜짐, localStorage에 기억(try/catch). 이 셋(후보·소제목 `+ 추가`·토글)은 **이번 주 보고에서만** 그린다.
- **전체 업무 기록**(세그먼트의 다른 쪽)은 같은 문서 폭 안의 조용한 목록이다. **프로젝트로 묶고**(보고 문서와 같은 프로젝트 차례, 같은 그룹 제목 부품, `프로젝트 없음`은 맨 아래), 줄은 제목 + `원문` \| 오른쪽에 기본값이 아닌 것만(`완료`, `보고에서 제외됨` + 3차 `복원`) \| 늘 보이는 ⋯. `미완료`·`보고에 포함`은 찍지 않는다. 줄을 누르면 업무 상세가 열린다. ⋯ 메뉴는 맨 위 `보고에서 제외`(이미 제외됐으면 `보고에 복원`) + 그 업무 종류의 줄 메뉴(할 일·확인 대기·결정) 그대로다.
- **아래로 들어간 문장**은 슬랙 글에서 부모 항목의 `◦` 부연이 된다(문서에서 보이는 그대로). 문서의 프로젝트 소제목·슬랙의 프로젝트 머리는 **부모의 프로젝트**다. 옛 합치기 문장이 모이는 `여러 프로젝트`는 문서·슬랙 모두 그 구역의 맨 끝(`프로젝트 없음`/`기타` 앞)에 선다.
- **슬랙 미리보기 카드**는 오른쪽 흰 카드: `슬랙에 붙이면`(13/700) → **넣을 구역 칩 + 조용한 토글 칩 `지라 정보`**(같은 줄, 같은 `.d-chip` 부품. 구역 칩만 `role="group"`인 `.rp-secg`로 묶이고 그 판은 `display: contents`라 칩들이 한 줄로 그대로 흐른다) → 붙였을 때의 모습(13.5 / 1.7). 아래 "슬랙 복사" 절이 형식을 정한다. **복사될 글자와 한 글자도 다르면 안 된다**(테스트가 형식을 고정한다).

### 슬랙 복사

`슬랙용으로 복사`는 **서식 있는 복사(`text/html`)와 일반 글자(`text/plain`)를 함께** 클립보드에 넣는다(`ClipboardItem`이 없거나 막히면 일반 글자만, 그것도 막히면 미리보기 글자를 잡아 주고 직접 복사하라고 알린다). 셋(일반 글자·HTML·미리보기)은 **하나의 구조 함수**(`reportSlackModel`)에서 나오므로 어긋날 수 없다.

- **형식** 첫 줄 제목 `9월 3주차 (9/21~9/27)`(슬랙 글에는 `이번 주` 같은 상대 표현을 쓰지 않는다) → 빈 줄 → 구역. 구역 차례는 `완료 · 진행 중 · 결정 · 확인 대기 · 예정` 하나로 정해져 있다.
- **구역 안** 프로젝트 이름 한 줄 → 그 프로젝트의 문장을 `• ` 글머리로. 같은 구역·같은 프로젝트는 머리 하나 아래로 모으고, **프로젝트 없는 문장은 `기타`로 구역 끝**. 문장이 여러 줄이면 둘째 줄부터 `    ◦ ` 부연(공백 4칸)이다.
- **빈 줄**은 구역 사이, **구역 이름 줄 뒤**, **프로젝트 묶음 사이**에 하나씩이다(프로젝트 없는 메모 줄들도 한 묶음이라 그 앞에 하나). 빈 줄은 묶음 **앞**에만 넣으므로 구역 끝 묶음 뒤에는 붙지 않고, 구역 사이의 빈 줄과 겹쳐 **잇따라 두 개가 되는 곳은 없다**. 묶음을 가르는 곳은 한 함수(`reportSlackBlocks`)에서만 정해 세 형식이 어긋날 수 없다.
- **`확인 완료`는 `완료` 안**으로 들어가고, `예정`에서 프로젝트를 고르지 않은 문장은 구역 끝 `* 문장` 메모 줄이 된다. 제외한 문장과 내용이 없는 구역은 빠진다.
- **접힌 요약 문장**(한 줄로 모으기)은 부모 한 줄만 나가고 아래 문장·`◦` 부연은 붙지 않는다. 펼친 요약 문장은 지금 아래로 넣은 문장과 같은 모양(부모 + `◦` 부연)이다. 접힌 부모만 있는 프로젝트 블록은 근거(`sourceIds`)가 없어 `지라 정보`를 켜도 괄호가 붙지 않는다(같은 이름이 다른 곳에서 근거와 함께 나오면 거기에는 붙는다).
- **HTML** 제목·구역·프로젝트는 `<p><b>…</b></p>`, 항목은 `<ul><li>…<ul><li>부연</li></ul></li></ul>` — 슬랙 입력창에 붙으면 굵은 글씨와 글머리 목록이 된다. 빈 줄 자리에는 빈 단락 `<p><br></p>`를 같은 수만큼 넣어 일반 글자와 간격이 같게 한다(속이 완전히 빈 `<p></p>`는 붙는 곳에서 지워지는 일이 있다). 사용자 문구는 전부 escape한다.
- **미리보기**도 같은 간격이다 — 줄마다 `div`이고 빈 줄만 진짜 줄바꿈 글자(`\n`)라, 직접 긁어 복사해도 일반 글자와 한 글자도 다르지 않다.
- **`지라 정보` 토글 칩**(기본 꺼짐, localStorage에 기억)은 **슬랙 글의 프로젝트 줄에만** 괄호 한 마디를 붙인다: `결제 정산 주기 정책 변경 (v2.70.0 · 9/30 배포 · QA 대기)`(버전이 없으면 `(QA 대기)`). 같은 프로젝트가 여러 구역에 나오면 **처음 나오는 곳에만** 붙고, 값은 이미 받아 둔 지라 목록(`jiraIssuesByKey`)에서만 읽는다 — 주간요약을 열었다고 지라를 새로 부르지 않는다. 세 형식은 그대로 `reportSlackModel` 하나에서 나온다(`options.jira`). 문서(가운데)는 영향을 받지 않고 지라 키도 나가지 않는다.
- **넣을 구역 칩**은 미리보기 카드 머리의 토글 칩 한 줄(28px, ⌘K 토글 칩과 같은 부품). 기본값은 `완료 · 진행 중 · 예정` 켜짐, 내용이 없는 구역은 흐리게 잠긴다. 고른 값은 주차와 상관없이 하나로 기억한다(localStorage). **문서(가운데)는 영향을 받지 않는다** — 문서에는 늘 전부 보인다.
- **다음 주 계획**의 빠른 추가 줄에는 왼쪽에 조용한 프로젝트 선택(기본 `프로젝트 없음`)이 있다. 고른 프로젝트는 문서에서도 소제목이 되고, 고르지 않은 문장은 구역 끝에 선다. 마지막으로 고른 값은 다음 입력에 그대로 남는다.
- **문장 수정 칸**에서 Enter는 줄바꿈이다. 칸 아래에 `둘째 줄부터는 슬랙에서 들여 쓴 부연으로 들어가요`를 조용히 적고, 문서에서도 둘째 줄부터는 1px 선으로 들여 쓴 13.5px `--muted` 글자로 보인다.

### 알림(toast) · 복구 배너 · 빈 화면

- **알림** 왼쪽 아래 24px 고정, `--surface`, 16px 모서리, `--pop`, 여백 `12px 12px 12px 18px`, 14px. 문구 + (해당되면) `⌘Z로 되돌리기` 안내 · `삭제 실행 취소` · `다시 시도` · 후속 동작 · `닫기`. 4.5초 뒤 사라짐(동작이 붙으면 8초), 등장 140ms(아래 6px에서), `role="status"`. **오류일 때만** 빨간 테두리가 생기고 스스로 사라지지 않는다.
- **복구 필요 배너** 헤더 아래 전체 폭 띠, `--urgent-bg` + `--urgent` 14/600, 여백 `12px 24px`. **스스로 사라지지 않는다.** 원인은 뒤에 `--muted` 12.5px로 덧붙인다.
- **빈 화면** 15/500 `--muted`, 여백 `34px 20px`. 한 줄 친근한 문구 하나만 적는다: `오늘 할 일이 비었어요. 맨 위 줄에서 바로 추가할 수 있어요.` · `오늘 할 일을 모두 끝냈어요.` · `오늘은 미팅이 없어요.` · `찾는 항목이 없어요.`

## 말투 (해요체)

**말을 거는 문장은 해요체.** 알림·안내·빈 화면·오류·도움말·`title` 툴팁·`aria-live` 문구, 그리고 서버가 화면에 돌려주는 오류 문구까지 전부.
**버튼·메뉴·열 이름·구역 제목은 짧은 명사형.** `완료로 표시` · `내일` · `나중에` · `오늘로` · `삭제` · `프로젝트 없음` · `기록 없음`.

규칙 셋: ① 짧고 구체적으로 ② 사과·과장 없이 ③ **무엇이 일어났는지 / 어떻게 하면 되는지**를 적는다.

| 상황 | 이렇게 |
|---|---|
| 잘된 일 | `내일로 미뤘어요` · `완료했어요` · `삭제했어요` · `되돌렸어요` · `저장했어요` · `오늘 할 일로 옮겼어요` |
| 단서를 붙일 때 | `나중에 할 일로 옮겼어요 · 기한은 그대로예요` |
| 저장이 불확실할 때 | `저장됐는지 확인하지 못했어요. 입력한 내용은 그대로 있어요` |
| 막혔을 때 | `복구가 필요해서 저장을 멈췄어요.` · `같은 상태의 문장만 묶을 수 있어요.` · `최근 작업부터 순서대로 실행 취소해 주세요` |
| 빈 화면 | `아이디어가 아직 없어요. 맨 위 줄에서 바로 적어 둘 수 있어요.` |

바꾸지 않는 것: 로그·주석·테스트 설명. 문구를 바꾸면 그 문구를 고정한 테스트도 같이 고친다.

### 용어 (그대로 쓴다)

`오늘로` · `내일` · `나중에` · `완료로 표시` · `결정` · `아이디어` · `확인 대기` · `언제 할지` · `기한`(할 일) · `답변 받을 날`(확인 대기, 줄의 배지는 짧게 `N일 늦음`·`오늘 답변 예정`) · `다시 확인할 날짜` · `프로젝트` · `원문` · `결과 한 줄` · `초안`.
업무를 끝내는 일이 아니면 `완료`라고 쓰지 않는다 — 결정은 `PRD 반영`, 확인 대기는 `확인됨으로 표시`. 날짜는 `9월 22일 (화)`, 좁은 자리에서는 `9월 22일`. 연도는 붙이지 않는다.

**찍지 않는 것.** 목록·줄·개수에 `보통` · `낮음` · `없음` · `0`을 찍지 않는다 — 값이 의미 없으면 자리를 비운다. 예외는 값을 고르거나 확인하는 자리뿐이다(상세 필드의 `없음`, 메뉴의 우선순위 칩).

## 모션

`--ease: cubic-bezier(.22, .8, .3, 1)`.

| 움직임 | 길이 |
|---|---|
| 줄·버튼 hover 색, 입력 초점 | 170ms |
| 겹쳐 뜨는 동작 묶음, 꺾쇠 회전 | 130ms |
| 메뉴 등장(위 6px에서) | 120ms |
| 팔레트·알림 등장(위/아래 6px에서) | 140ms |
| 서랍 열고 닫기, 본문 밀림 | 160ms |
| 체크 커졌다 돌아오기 | 320ms |
| 진행 선 채우기 | 320ms |
| 완료 순간의 줄 긋기 + 옅어짐 | 520ms |
| 새로고침 아이콘 한 바퀴 | 400ms |
| 팔레트에서 연 줄 밝히기 | 1.6s |

등장은 6px 이동 + 투명도뿐이다. 장식적인 움직임은 없다. `prefers-reduced-motion: reduce`에서는 모든 transition·animation을 `.01ms`로 줄이고, 완료 순간의 두 애니메이션과 진행 선 전환은 아예 끈다.

## 접근성 바닥

**대비 (계산값, 4.5:1 기준)**

라이트 — `--text`/흰 **16.6** · `--text`/바탕 **15.0** · `--muted`/흰 **7.11** · `--muted`/바탕 **6.45** · `--dim`/흰 **5.32** · `--dim`/바탕 **4.83** · `--accent-text`/흰 **5.41** · `--accent-text`/바탕 **4.91** · `--accent-text`/`--accent-soft` **4.82** · 흰 글자/`--accent-strong` **5.41**.
배지 — urgent `#c9252d`/`#ffeceb` **4.87** · warn `#b0560a`/`#fff4e5` **4.62** · success `#0d7a4a`/`#e6f6ee` **4.82** · 중립 `#4e5968`/`#eef1f4` **6.27**.

다크 — `--text`/면 **13.6** · `--muted`/면 **7.35** · `--muted`/바탕 **8.20** · `--dim`/면 **5.25** · `--accent-text`/면 **6.79** · `--accent-text`/`--accent-soft` **6.12** · `--on-fill`/`--accent` **5.97**.
배지 — urgent **6.79** · warn **8.09** · success **7.55** · 중립 **6.20**.

> 참고 색에서 일부러 벗어난 곳: 토스 계열의 `#8b95a1`는 흰 바탕에서 3.04:1이라 글자로 쓸 수 없다 → 라이트의 `--dim`을 `#636c78`로 한 단계 어둡게 했다(다크에서는 `#8b95a1` 그대로, 5.25 ✓).

**그 밖의 바닥**
- 포커스: `:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; border-radius: 10px }`. 입력칸은 2px 파란 테나 `outline`으로 대신한다. hover에만 보이는 동작은 `:focus-within`에서도 보여야 한다.
- 누르는 영역: 줄 안의 컨트롤 28px 이상(체크박스는 투명한 `::after`로 확보), 줄 자체 54px, 520px 이하에서는 줄 60px 이상.
- 컨트롤 대비 3:1 이상(꺼진 체크박스 테두리 `--check-line`이 흰 면 위 3.04 ✓).
- 키보드: ⌘K 검색 · ⌘Z 되돌리기 / ⌘⇧Z 다시 실행 · Esc는 **가장 위에 열린 것부터** 하나씩 닫는다(설정 → 메뉴 → 검색 → 회의 → 상세 → 서랍). 맨 위에 `오늘 할 일로 건너뛰기` 링크(초점을 받으면 나타나고, 흰 글자를 얹으므로 바탕은 `--accent-strong`이다).
- 탭은 `role="tablist"`/`aria-selected`, 아이콘 버튼은 `aria-label`, 알림은 `role="status"` + `aria-live="polite"`, 팔레트는 `role="dialog"` + `combobox`/`listbox`/`option`.
- 한글 조합 중(`event.isComposing`)의 Enter는 글자를 확정하는 것이지 실행이 아니다. 입력 중에는 자동 갱신으로 화면을 다시 그리지 않는다.

## Do's and Don'ts

### Do
- **Do** 색·모서리·그림자·간격은 토큰(`--accent`, `--r`, `--soft`, `--pad` …)만 쓴다. 컴포넌트 규칙에 색 값을 직접 적지 않는다.
- **Do** 목록은 테두리 없는 20px 흰 카드 하나에 담고, 그 안은 여백과 그룹 제목으로만 나눈다.
- **Do** 줄은 54px 한 줄, 열 순서 `체크박스 | 제목 | 상태(밀림 · 진행 · 우선순위 · 기한)`를 지킨다. 기한이 항상 맨 오른쪽이다.
- **Do** 줄 위 동작은 상태 열 위에 겹쳐 띄우고(`:hover`, `:focus-within`, 선택됨), 520px 이하에서는 항상 보이게 한다.
- **Do** 상태말은 아이콘 + 회색 글자로 적고, 급한 말만 6px 배지로 세운다.
- **Do** 완료는 둥근 네모 체크박스로 한다. 완료하는 순간을 짧게 보여 준다.
- **Do** 한 패널에 1차 버튼은 하나만 둔다. 나머지는 2·3차, `삭제`는 빨간 글자.
- **Do** 새 더보기 메뉴가 필요하면 한 벌뿐인 메뉴 컴포넌트에 항목을 더한다.
- **Do** 기존 부품(`.d-jlink` 같은)을 다른 화면이 **감싸 쓸 때**, 감싸는 컨테이너의 셀렉터는 직계 자식(`.d-pnew > .row`)으로 좁힌다. `row`·`in`·`note`처럼 부품 안에서도 쓰는 범용 클래스명을 조상 셀렉터로 걸면 같은 특이도의 뒷 규칙이 부품 내부를 덮어쓴다(새 프로젝트 화면에서 지라 입력칸이 92px로 찌그러진 사고).
- **Do** 입력칸의 `blur`·`input`에서 화면을 통째로 다시 그릴 때는 초점을 되돌린다(`projectNewPaint`처럼 이름표를 적어 두고 새 트리에서 찾아 `focus()`) — Tab으로 나가는 순간 트리를 갈아엎으면 초점이 `<body>`로 떨어져 키보드 흐름이 끊긴다.
- **Do** 말을 거는 문장은 해요체로, 버튼 이름은 명사형으로 적는다.
- **Do** 화면을 고치면 라이트·다크, 1440/1280/1120/900/640/520/390, `prefers-reduced-motion`에서 확인한다.

### Don't
- **Don't** 정보를 버튼처럼 생긴 **파란 알약**으로 그리지 않는다. 배경이 칠해진 둥근 것은 전부 누를 수 있어야 한다.
- **Don't** 상태 배지를 알약(999px)으로 만들지 않는다 — 6px이다. 그리고 급하지 않은 말에는 배지를 쓰지 않는다.
- **Don't** 파랑을 선택·행동 밖에 쓰지 않는다. `--accent` 위에 흰 글자를 얹지 않는다(`--accent-strong`을 쓴다).
- **Don't** 카드 안에 카드를 넣지 않는다. 붙어 있는 카드에 `--pop`을 주지 않는다.
- **Don't** 목록 줄을 실선으로 자르지 않는다.
- **Don't** 부가 정보에 굵기 700을 쓰지 않는다. 700은 제목의 것이다.
- **Don't** `보통` · `낮음` · `없음` · `0개` 같은 빈 정보를 목록에 찍지 않는다.
- **Don't** 상세를 위해 목록 옆에 열을 비워 두지 않는다 — 상세는 줄 옆에 떠서 열린다.
- **Don't** 상세의 값을 읽기만 하는 글자로 두지 않는다 — 보이는 자리에서 바로 고칠 수 있어야 한다.
- **Don't** 이모지·글리프 아이콘, 1px을 넘는 색 띠, 제목 위의 작은 머리글(eyebrow), 큰 숫자 통계 카드를 쓰지 않는다.
- **Don't** 계획 모드·저장 버튼·삭제 확인창을 되살리지 않는다. 옮기기는 그 자리에서, 저장은 자동, 삭제는 되돌리기로.
- **Don't** 웹폰트 CDN·라이브러리·이미지·빌드 단계를 들이지 않는다. 글꼴은 앱에 넣어 둔 파일 하나를 서버가 직접 준다.
- **Don't** 업무를 끝내는 일이 아닌 것에 `완료`라는 말을 붙이지 않는다.
