# BL2 말투 전환 — 바꾼 문구 전부 (전 → 후)

화면이 사람에게 말을 거는 문장(알림·안내·빈 화면·오류·도움말·`title` 툴팁, 그리고 서버가 화면에 돌려주는 오류 문구)을 해요체로 바꿨습니다.
버튼·메뉴·열 이름·구역 제목 같은 **짧은 명사형은 그대로** 뒀습니다 — `완료로 표시` · `내일` · `나중에` · `오늘로` · `삭제` · `프로젝트 없음` · `기록 없음` · `모두 처리함` · `연결 없음` · `오늘 확인 요청함`.
로그·주석·README는 이번 범위가 아닙니다. 줄 번호는 바꾼 뒤 기준이고, 같은 문구가 여러 줄에 있으면 전부 적었습니다.

**총 149개 문구 · 180곳**

## `app.js` — 화면 전체(알림·빈 화면·안내·도움말·툴팁)

| 줄 | 전 | 후 |
|---|---|---|
| 604, 606, 1080, 1728, 2037, 2407, 2841, 3884 | `'오늘 할 일로 확정함'` | `'오늘 할 일로 옮겼어요'` |
| 607, 1727, 2407, 2846 | `'내일로 미룸'` | `'내일로 미뤘어요'` |
| 608, 1080, 2409, 2850 | `'나중에 할 일로 옮김 · 기한은 그대로입니다.'` | `'나중에 할 일로 옮겼어요 · 기한은 그대로예요'` |
| 604, 606, 1080, 1728, 2037, 2407, 2841, 3884 | `'오늘 할 일로 옮김'` | `'오늘 할 일로 옮겼어요'` |
| 1727, 3885 | `'나중에 할 일로 옮김'` | `'나중에 할 일로 옮겼어요'` |
| 2044 | `'할 일로 옮김'` | `'할 일로 옮겼어요'` |
| 2047, 2467, 3887 | `'완료로 표시함'` | `'완료했어요'` |
| 2394 | `'미완료로 되돌림'` | `'미완료로 되돌렸어요'` |
| 2299 | `'대기중으로 되돌림'` | `'대기중으로 되돌렸어요'` |
| 2123, 2812, 3916 | `'삭제함'` | `'삭제했어요'` |
| 1979 | `'PRD 미반영으로 되돌림' : 'PRD 반영함으로 표시함'` | `'PRD 미반영으로 되돌렸어요' : 'PRD 반영으로 표시했어요'` |
| 2102 | `showNotice('확인 완료로 표시함'` | `showNotice('확인 완료로 표시했어요'` |
| 2076 | `announce(project ? '프로젝트 지정함' : '프로젝트 해제함');` | `announce(project ? '프로젝트를 지정했어요' : '프로젝트를 지웠어요');` |
| 2118 | `announce(who ? '누구에게 수정함' : '누구에게 해제함');` | `announce(who ? '누구에게를 적었어요' : '누구에게를 지웠어요');` |
| 2480, 2823, 3900 | `` `기한 ${uiKoDate(value)}로 지정함` : '기한 해제함' `` | `` `기한을 ${uiKoDate(value)}로 정했어요` : '기한을 지웠어요' `` |
| 2158, 2974 | `` `회신 기한 ${uiKoDate(value)}로 지정함` : '회신 기한 해제함' `` | `` `회신 기한을 ${uiKoDate(value)}로 정했어요` : '회신 기한을 지웠어요' `` |
| 2146, 2978 | `` `${uiKoDate(value)}에 다시 확인` : '다시 확인할 날짜 해제함' `` | `` `${uiKoDate(value)}에 다시 확인할게요` : '다시 확인할 날짜를 지웠어요' `` |
| 4172 | `announce('그룹 해제함');` | `announce('그룹을 지웠어요');` |
| 4194, 4209 | `announce('그룹 지정함');` | `announce('그룹을 지정했어요');` |
| 4216 | `announce('지라 이슈 연결함');` | `announce('지라 이슈를 연결했어요');` |
| 4034 | `announce('내용 수정함');` | `announce('내용을 고쳤어요');` |
| 3968, 4333 | `'오늘 할 일 추가함'` | `'오늘 할 일에 추가했어요'` |
| 2316, 4334 | `'나중에 할 일 추가함'` | `'나중에 할 일에 추가했어요'` |
| 4335 | `'확인 대기 추가함'` | `'확인 대기에 추가했어요'` |
| 4336 | `'아이디어 추가함'` | `'아이디어에 추가했어요'` |
| 4337 | `'결정 추가함'` | `'결정에 추가했어요'` |
| 2063 | `announce('가능성을 바꿨습니다.');` | `announce('가능성을 바꿨어요');` |
| 2445 | `announce('우선순위를 바꿨습니다.');` | `announce('우선순위를 바꿨어요');` |
| 2113 | `announce('확인 요청을 기록했습니다.');` | `announce('오늘 확인을 요청한 것으로 적었어요');` |
| 2097 | `announce('정책/얼라인에 남겼습니다. 아이디어·결정 탭에서 내용을 다듬을 수 있어요.');` | `announce('결정으로 남겼어요 · 아이디어·결정 탭에서 다듬을 수 있어요');` |
| 1256 | `announce('삭제한 항목을 복원했습니다.');` | `announce('삭제한 항목을 되살렸어요');` |
| 4476, 4806 | `announce('새로고침했습니다.');` | `announce('새로고침했어요');` |
| 1743 | `announce('오늘은 제안을 접어둡니다.');` | `announce('오늘은 제안을 접어 둘게요');` |
| 2902 | `announce('원본 링크를 복사했습니다.');` | `announce('원본 링크를 복사했어요');` |
| 2905, 2907 | `'복사하지 못했습니다. 링크를 길게 눌러 복사해 주세요.'` | `'복사하지 못했어요. 링크를 길게 눌러 복사해 주세요'` |
| 3132 | `announce('담은 것을 되돌렸습니다.');` | `announce('담은 것을 되돌렸어요');` |
| 1401 | `'되돌릴 작업이 없습니다.' : '다시 실행할 작업이 없습니다.'` | `'되돌릴 작업이 없어요' : '다시 실행할 작업이 없어요'` |
| 1409 | `` `${direction === 'undo' ? '되돌림' : '다시 실행'}${entry.label ? `: ${entry.label}` : ''}` `` | `` `${direction === 'undo' ? '되돌렸어요' : '다시 실행했어요'}${entry.label ? ` · ${entry.label}` : ''}` `` |
| 1339 | `showNotice('저장했습니다.');` | `showNotice('저장했어요');` |
| 1332 | `` (response.ok ? '저장하지 못했습니다.' : `요청 실패 (${response.status})`) `` | `` (response.ok ? '저장하지 못했어요' : `요청이 실패했어요 (${response.status})`) `` |
| 1346 | `showNotice(writing ? '저장을 확인하지 못했습니다. 입력은 유지됩니다. 연결을 확인한 뒤 다시 시도해 주세요.' : '목록을 불러오지 못했습니다.'` | `showNotice(writing ? '저장됐는지 확인하지 못했어요. 입력한 내용은 그대로 있어요' : '목록을 불러오지 못했어요'` |
| 1287 | `const STORAGE_BANNER_TEXT = '데이터를 지키기 위해 저장을 멈췄습니다. 지금까지의 기록은 그대로 있습니다. README의 "복구 필요 상태"를 따라 정리한 뒤 서버를 다시 시작해 주세요.';` | `const STORAGE_BANNER_TEXT = '복구가 필요해서 저장을 멈췄어요. 지금까지의 기록은 그대로 있어요. README의 "복구 필요 상태"를 따라 정리한 뒤 서버를 다시 시작해 주세요.';` |
| 880 | `` showNotice(`${result.count}개 업무를 변경했습니다.` `` | `` showNotice(`${result.count}개를 바꿨어요` `` |
| 881, 925 | `showNotice('이후 작업부터 순서대로 실행 취소해 주세요.', true)` | `showNotice('최근 작업부터 순서대로 실행 취소해 주세요', true)` |
| 922 | `` stopped ? `${removed.length}개까지 삭제하고 멈췄습니다. 나머지는 그대로 있습니다.` : `${removed.length}개를 삭제했습니다.`, `` | `` stopped ? `${removed.length}개까지 삭제하고 멈췄어요 · 나머지는 그대로 있어요` : `${removed.length}개를 삭제했어요`, `` |
| 1143 | `'<div class="d-empty">아직 프로젝트가 없습니다. 업무에 프로젝트를 지정하면 여기에 모입니다.</div>'` | `'<div class="d-empty">아직 프로젝트가 없어요. 업무에 프로젝트를 지정하면 여기 모여요.</div>'` |
| 1565 | `'오늘 일정을 확인할 수 없습니다.' : '오늘 미팅 없음'` | `'오늘 일정을 가져오지 못했어요.' : '오늘은 미팅이 없어요.'` |
| 1935 | `'정해진 내용이 아직 없습니다. 위 첫 줄에서 바로 추가하세요.'` | `'정해진 내용이 아직 없어요. 맨 위 줄에서 바로 적을 수 있어요.'` |
| 1941 | `'아이디어가 아직 없습니다. 위 첫 줄에서 바로 적어 두세요.'` | `'아이디어가 아직 없어요. 맨 위 줄에서 바로 적어 둘 수 있어요.'` |
| 1957 | `${decisionArchiveQuery ? '일치하는 결정 없음' : '반영 완료한 결정 없음'}` | `${decisionArchiveQuery ? '찾는 결정이 없어요.' : '아직 반영 완료한 결정이 없어요.'}` |
| 2311 | `'<div class="d-empty">나중에 할 일이 없습니다.</div>'` | `'<div class="d-empty">나중에 할 일이 없어요.</div>'` |
| 1881 | `empty.textContent = '주간 요약 없음';` | `empty.textContent = '아직 주간요약이 없어요.';` |
| 4544 | `'<div class="d-empty">상태를 불러오지 못했습니다.</div>'` | `'<div class="d-empty">상태를 불러오지 못했어요.</div>'` |
| 3660 | `? '찾는 항목이 없습니다.' ⏎       : '찾을 내용을 입력하세요. 완료한 업무와 회의 초안 문구까지 함께 찾습니다.';` | `? '찾는 항목이 없어요.' ⏎       : '찾을 내용을 적어 보세요. 완료한 업무와 회의 초안 문구까지 함께 찾아요.';` |
| 3982 | `'<div class="d-empty">오늘 할 일이 비었어요. 위 첫 줄에서 바로 추가하세요.</div>'` | `'<div class="d-empty">오늘 할 일이 비었어요. 맨 위 줄에서 바로 추가할 수 있어요.</div>'` |
| 2999 | `` `${uiKoDate(detail.contacted)} 요청함` : '아직 없습니다.' `` | `` `${uiKoDate(detail.contacted)} 요청함` : '아직 요청한 기록이 없어요.' `` |
| 2939 | `gone.textContent = '연결했던 확인 대기가 삭제되었습니다.';` | `gone.textContent = '연결했던 확인 대기가 삭제됐어요.';` |
| 3092 | `note.textContent = '아직 기록되지 않은 회의입니다. 여기서 적은 것은 회의에 연결되지 않고 바로 담깁니다.';` | `note.textContent = '아직 기록되지 않은 회의예요. 여기서 적은 것은 회의에 연결되지 않고 바로 담겨요.';` |
| 3172 | `all.textContent = '오늘 검토할 초안을 모두 처리했습니다.';` | `all.textContent = '오늘 검토할 초안을 모두 처리했어요.';` |
| 3276 | `panelSetError('비어 있는 문구가 있습니다. 채우거나 ✕로 빼 주세요.')` | `panelSetError('비어 있는 문구가 있어요. 채우거나 ✕로 빼 주세요')` |
| 3126 | `throw new Error(undo.error \|\| '되돌리지 못했습니다.');` | `throw new Error(undo.error \|\| '되돌리지 못했어요.');` |
| 3401 | `throw new Error(result.error \|\| '담지 못했습니다.');` | `throw new Error(result.error \|\| '담지 못했어요.');` |
| 3406 | `panelSetError('추가하지 못했습니다. 입력 내용은 유지됩니다.');` | `panelSetError('추가하지 못했어요. 적은 내용은 그대로 있어요.');` |
| 3045 | `\|\| '저장하지 못했습니다. 입력 내용을 확인하고 다시 시도해 주세요.');` | `\|\| '저장하지 못했어요. 내용을 확인한 뒤 다시 시도해 주세요.');` |
| 2760 | `note.textContent = '자동으로 저장됩니다';` | `note.textContent = '고치면 바로 저장돼요';` |
| 2950 | `area.placeholder = '끝나고 한 줄로 남기면 주간요약에 그대로 올라갑니다';` | `area.placeholder = '끝나고 한 줄로 남기면 주간요약에 그대로 올라가요';` |
| 4540 | `'<div class="d-empty">불러오는 중…</div>'` | `'<div class="d-empty">불러오는 중이에요…</div>'` |
| 4612 | `translateFailureText(a.lastSummary \|\| '실패했습니다.')` | `translateFailureText(a.lastSummary \|\| '실패했어요.')` |
| 292 | `link.title = '슬랙 원문을 새 탭에서 엽니다';` | `link.title = '슬랙 원문을 새 탭에서 열어요';` |
| 3247 | `whenSeg.title = '나중: 나중에 할 일로 담김 · 오늘: 오늘 할 일로 담김';` | `whenSeg.title = '나중에: 나중에 할 일로 담겨요 · 오늘: 오늘 할 일로 담겨요';` |
| 1539 | `` ? `${source.name} 자동 갱신이 ${source.lastSync} 이후 멈춰 있습니다. 목록이 최신이 아닐 수 있어요. 누르면 자세한 상태를 봅니다.` ⏎       : `${source.name}를 아직 한 번도 가져오지 못했습니다. 누르면 자세한 상태를 봅니다.`; `` | `` ? `${source.name} 자동 갱신이 ${source.lastSync} 이후 멈춰 있어요. 목록이 최신이 아닐 수 있어요. 누르면 자세한 상태를 볼 수 있어요.` ⏎       : `${source.name}를 아직 한 번도 가져오지 못했어요. 누르면 자세한 상태를 볼 수 있어요.`; `` |
| 4628 | `'업무 줄에 마우스를 올리면 <b>내일</b>·<b>나중에</b>가 나옵니다. 나중에로 보낸 업무는 머리줄의 <b>나중에 할 일</b> 서랍에 모이고, 거기서 <b>오늘로</b> 다시 가져옵니다. 따로 "계획 모드"로 들어갈 필요가 없습니다.'` | `'업무 줄에 마우스를 올리면 <b>내일</b>·<b>나중에</b>가 나와요. 나중에로 보낸 업무는 머리줄의 <b>나중에 할 일</b> 서랍에 모이고, 거기서 <b>오늘로</b> 다시 가져와요. 따로 "계획 모드"로 들어갈 필요가 없어요.'` |
| 4630 | `'슬랙·회의에서 자동으로 모인 항목이 먼저 쌓이는 곳입니다. AI가 오늘 할지 나중에 할지 정하지 않습니다. 프로젝트만 지정하고 <b>오늘</b> 또는 <b>나중에</b>로 보내면 정리가 끝나고 인박스에서 사라집니다.'` | `'슬랙·회의에서 자동으로 모인 항목이 먼저 쌓이는 곳이에요. AI는 오늘 할지 나중에 할지 정하지 않아요. 프로젝트만 지정하고 <b>오늘</b> 또는 <b>나중에</b>로 보내면 정리가 끝나고 여기서 사라져요.'` |
| 4632 | `'줄의 <b>⋯</b> 더보기 → <b>프로젝트</b>에서 지라 이슈나 그룹을 고릅니다. 모아 보려면 위쪽 <b>프로젝트</b> 탭으로 갑니다. 목록의 그룹 제목을 눌러도 그 프로젝트로 넘어갑니다.'` | `'줄의 <b>⋯</b> 더보기 → <b>프로젝트</b>에서 지라 이슈나 그룹을 고르면 돼요. 모아 보려면 위쪽 <b>프로젝트</b> 탭으로 가요. 목록의 그룹 제목을 눌러도 그 프로젝트로 넘어가요.'` |
| 4634 | `'오늘 할 일 머리줄의 <b>여러 개 선택</b>을 누르면 줄마다 선택 칸이 하나 더 생깁니다(완료 체크는 그대로 씁니다). 목록 아래 막대에서 <b>오늘로</b>·<b>내일</b>·<b>나중에</b>·<b>날짜</b>·<b>프로젝트</b>·<b>완료로 표시</b>·<b>삭제</b>를 한 번에 적용합니다.'` | `'오늘 할 일 머리줄의 <b>⋯</b> → <b>여러 개 선택</b>을 누르면 줄마다 선택 칸이 하나 더 생겨요(완료 체크는 그대로 써요). 목록 아래 막대에서 <b>오늘로</b>·<b>내일</b>·<b>나중에</b>·<b>날짜</b>·<b>프로젝트</b>·<b>완료로 표시</b>·<b>삭제</b>를 한 번에 적용해요.'` |
| 4636 | `'<b>⌘K</b>(윈도는 Ctrl+K)로 검색을 엽니다. 할 일·확인 대기·결정·아이디어·회의를 한 자리에서 찾고, 위 칩으로 종류를 좁힙니다. 검색에서 연 항목을 닫으면 찾던 자리로 그대로 돌아옵니다.'` | `'<b>⌘K</b>(윈도는 Ctrl+K)로 검색을 열어요. 할 일·확인 대기·결정·아이디어·회의를 한 자리에서 찾고, 위 칩으로 종류를 좁힐 수 있어요. 검색에서 연 항목을 닫으면 찾던 자리로 그대로 돌아와요.'` |
| 4638 | `'왼쪽 <b>오늘 미팅</b>의 회의를 누르면 오른쪽에 회의 정리 패널이 열립니다. 초안을 고쳐 담고, 담은 뒤 뜨는 결과 카드의 <b>실행 취소</b>로 되돌릴 수 있습니다.'` | `'왼쪽 <b>오늘 미팅</b>의 회의를 누르면 오른쪽에 회의 정리 패널이 열려요. 초안을 고쳐 담고, 담은 뒤 뜨는 결과 카드의 <b>실행 취소</b>로 되돌릴 수 있어요.'` |
| 4640 | `'완료·삭제·보고 제외는 아래 알림의 <b>되돌리기</b>로 바로 취소됩니다. <b>⌘Z</b>도 같은 일을 하고, <b>⌘⇧Z</b>로 다시 실행합니다.'` | `'완료·삭제·보고 제외는 아래 알림의 <b>되돌리기</b>로 바로 취소할 수 있어요. <b>⌘Z</b>도 같은 일을 하고, <b>⌘⇧Z</b>로 다시 실행해요.'` |
| 4642 | `'완료한 업무에 적은 한 줄이 주간요약 문장으로 그대로 올라갑니다. 금요일에 다시 쓰지 않아도 됩니다.'` | `'완료한 업무에 적은 한 줄이 주간요약 문장으로 그대로 올라가요. 금요일에 다시 쓰지 않아도 돼요.'` |
| 4644 | `'아니요. 보고 문장과 원본 기록은 따로 보존됩니다. 원본이 바뀌면 수정 제안으로만 알려 주고, 직접 적용하기 전에는 편집한 문장을 바꾸지 않습니다.'` | `'아니요. 보고 문장과 원본 기록은 따로 남아요. 원본이 바뀌면 수정 제안으로만 알려 주고, 직접 적용하기 전에는 고쳐 둔 문장을 바꾸지 않아요.'` |
| 4646 | `'주간요약에서 <b>이번 보고에서 제외</b>를 누르면 복사할 내용에서 빠집니다. 원본은 업무 기록에 남고 언제든 보고에 되돌릴 수 있습니다.'` | `'주간요약에서 문장의 <b>제외</b>를 누르면 복사할 내용에서 빠져요. 원본은 업무 기록에 남고 언제든 보고로 되돌릴 수 있어요.'` |
| 4648 | `'다른 사람의 답을 기다리는 항목입니다. 언제까지 답을 받아야 하는지는 <b>회신 기한</b>으로 적습니다. 할 일 쪽에서 "이 답변을 기다리는 중"으로 연결해 두면 답이 오는 순간 알려 줍니다.'` | `'다른 사람의 답을 기다리는 항목이에요. 언제까지 답을 받아야 하는지는 <b>회신 기한</b>에 적어요. 할 일 쪽에서 "이 답변을 기다리는 중"으로 연결해 두면 답이 오는 순간 알려 줘요.'` |
| 4650 | `'슬랙·캘린더·지라 자동 동기화가 최근에 못 돌았다는 뜻입니다. 그 글자를 누르면 이 창의 <b>상태</b>에서 해당 자동화 줄이 바로 보입니다.'` | `'슬랙·캘린더·지라 자동 동기화가 최근에 못 돌았다는 뜻이에요. 그 글자를 누르면 이 창의 <b>상태</b>에서 그 자동화 줄이 바로 보여요.'` |
| 4685 | `showNotice('다른 기기 접속이 설정되지 않았습니다.');` | `showNotice('다른 기기 접속이 아직 설정되지 않았어요');` |
| 4687 | `showNotice('암호를 복사했습니다. 다른 기기에서 사용자 이름은 workspace를 입력해 주세요.');` | `showNotice('암호를 복사했어요 · 다른 기기에서 사용자 이름은 workspace를 넣어 주세요');` |
| 4688 | `showNotice('암호를 복사하지 못했습니다.', true);` | `showNotice('암호를 복사하지 못했어요', true);` |
| 4692 | `hint.textContent = '같은 와이파이·Tailscale에서 이 주소를 열고, 사용자 이름은 workspace를 입력합니다.';` | `hint.textContent = '같은 와이파이·Tailscale에서 이 주소를 열고, 사용자 이름은 workspace를 넣으면 돼요.';` |
| 2430 | `` `${uiKoDate(input.value)}로 옮김` `` | `` `${uiKoDate(input.value)}로 옮겼어요` `` |

## `workflows.js` — 회의 검토 저장

| 줄 | 전 | 후 |
|---|---|---|
| 123 | `'검토 결과를 저장하지 못했습니다.'` | `'검토 결과를 저장하지 못했어요.'` |
| 127 | `showNotice('완료로 표시했습니다.'` | `showNotice('완료했어요'` |

## `report-ui.js` — 주간요약 문서

| 줄 | 전 | 후 |
|---|---|---|
| 39 | `'저장하지 못했습니다. 입력은 보존됩니다.'` | `'저장하지 못했어요. 적은 내용은 그대로 있어요'` |
| 143, 415 | `'저장을 확인하지 못했습니다. 입력은 보존됩니다.'` | `'저장됐는지 확인하지 못했어요. 적은 내용은 그대로 있어요'` |
| 155 | `announce('보고 내용을 저장했습니다.');` | `announce('보고 내용을 저장했어요');` |
| 187 | `` `${reportMergeHeading} 안에서만 고를 수 있습니다` `` | `` `${reportMergeHeading} 안에서만 고를 수 있어요` `` |
| 224 | `announce('보고 내용을 복사했습니다.');` | `announce('보고 내용을 복사했어요');` |
| 275 | `'연결된 원본을 찾을 수 없습니다.'` | `'연결된 원본을 찾지 못했어요.'` |
| 283 | `? '원본 상태가 다르거나 삭제된 업무가 있습니다. 문장을 직접 확인해 주세요.' ⏎     : '현재 문장은 그대로 보존됩니다.'` | `? '원본 상태가 다르거나 삭제된 업무가 있어요. 문장을 직접 확인해 주세요.' ⏎     : '지금 문장은 그대로 둬요.'` |
| 430 | `'이번 주에 연결된 업무 기록이 없습니다.'` | `'이번 주에 연결된 업무 기록이 없어요.'` |
| 457 | `'보고에 담긴 문장이 없습니다.'` | `'보고에 담긴 문장이 없어요.'` |
| 393 | `'직접 쓴 문장만 들어갑니다'` | `'직접 쓴 문장만 들어가요'` |
| 507 | `'이번 주 기록이 생기면 문서에 나타납니다.'` | `'이번 주 기록이 생기면 여기에 나타나요.'` |

## `server.js` — 서버가 화면에 돌려주는 오류

| 줄 | 전 | 후 |
|---|---|---|
| 55 | `'다른 내용으로 같은 요청을 재사용할 수 없습니다.'` | `'다른 내용으로 같은 요청을 다시 쓸 수 없어요.'` |
| 1120 | `'지원하지 않는 가져오기입니다.'` | `'지원하지 않는 가져오기예요.'` |
| 1052 | `'요청이 너무 큽니다.'` | `'요청이 너무 커요.'` |
| 1054 | `'요청이 중단되었습니다.'` | `'요청이 중간에 끊겼어요.'` |
| 1526 | `'기록을 읽지 못했습니다. 파일을 덮어쓰지 않았습니다. 백업을 확인해 주세요.'` | `'기록을 읽지 못했어요. 파일은 덮어쓰지 않았어요. 백업을 확인해 주세요.'` |

## `task-batch.js` — 여러 개 선택 일괄 저장

| 줄 | 전 | 후 |
|---|---|---|
| 16 | `'실행 취소 기록이 만료되었습니다.'` | `'실행 취소 기록이 만료됐어요.'` |
| 34, 36 | `'지원하지 않는 일괄 변경입니다.'` | `'지원하지 않는 일괄 변경이에요.'` |
| 46 | `'중복된 업무 ID가 있습니다.'` | `'중복된 업무 ID가 있어요.'` |
| 47 | `'미완료 업무만 선택할 수 있습니다.'` | `'미완료 업무만 고를 수 있어요.'` |
| 50 | `'이후 변경된 업무가 있어 실행 취소할 수 없습니다.'` | `'그 뒤에 바뀐 업무가 있어 실행 취소할 수 없어요.'` |
| 58 | `'일부 업무가 삭제되었거나 찾을 수 없습니다. 새로고침해 주세요.'` | `'일부 업무가 삭제됐거나 찾을 수 없어요. 새로고침해 주세요.'` |

## `report-drafts.js` — 주간보고 저장

| 줄 | 전 | 후 |
|---|---|---|
| 20 | `'보고 기록 형식을 확인해 주세요. 원본은 보존되어 있습니다.'` | `'보고 기록 형식을 확인해 주세요. 원본은 그대로 있어요.'` |
| 82 | `'새 기록이나 다른 창의 변경이 있습니다. 입력은 보존했습니다. 최신 내용을 확인한 뒤 다시 저장해 주세요.'` | `'새 기록이나 다른 창의 변경이 있어요. 적은 내용은 그대로 있어요. 최신 내용을 확인한 뒤 다시 저장해 주세요.'` |
| 85 | `'되돌리기 기록이 만료되었습니다.'` | `'되돌리기 기록이 만료됐어요.'` |
| 86 | `'이후 다른 변경이 있어 되돌릴 수 없습니다. 최신 보고를 확인해 주세요.'` | `'그 뒤에 다른 변경이 있어 되돌릴 수 없어요. 최신 보고를 확인해 주세요.'` |
| 94 | `'같은 상태의 보고 항목만 묶을 수 있습니다.'` | `'같은 상태의 문장만 묶을 수 있어요.'` |
| 99 | `'보고 항목을 찾을 수 없습니다.'` | `'보고 항목을 찾을 수 없어요.'` |
| 104 | `'지원하지 않는 보고 변경입니다.'` | `'지원하지 않는 보고 변경이에요.'` |

## `workflow-store.js` — 흐름 기록 저장

| 줄 | 전 | 후 |
|---|---|---|
| 12 | `'업무 연결 기록을 읽을 수 없습니다.'` | `'업무 연결 기록을 읽지 못했어요.'` |
| 87, 187 | `'항목을 찾을 수 없습니다.'` | `'항목을 찾을 수 없어요.'` |
| 89 | `'지원하지 않는 변경입니다.'` | `'지원하지 않는 변경이에요.'` |
| 90 | `'확인 대기에서만 지정할 수 있습니다.'` | `'확인 대기에서만 지정할 수 있어요.'` |
| 91 | `'확인 요청 날짜가 올바르지 않습니다.'` | `'확인 요청 날짜가 올바르지 않아요.'` |
| 92 | `'연결할 확인 대기를 찾을 수 없습니다.'` | `'연결할 확인 대기를 찾을 수 없어요.'` |
| 101 | `'회의를 찾을 수 없습니다.'` | `'회의를 찾을 수 없어요.'` |
| 125, 157 | `'결정에는 마감일을 넣을 수 없습니다.'` | `'결정에는 마감일을 넣을 수 없어요.'` |
| 136 | `'항목을 저장하지 못했습니다.'` | `'항목을 저장하지 못했어요.'` |
| 151 | `'이미 처리했거나 찾을 수 없는 항목입니다.'` | `'이미 처리했거나 찾을 수 없는 항목이에요.'` |
| 155 | `'오늘은 할 일만 고를 수 있습니다.'` | `'오늘은 할 일만 고를 수 있어요.'` |
| 175 | `'되돌릴 항목이 없습니다.'` | `'되돌릴 항목이 없어요.'` |
| 178 | `'이미 되돌렸거나 되돌릴 수 없는 항목입니다.'` | `'이미 되돌렸거나 되돌릴 수 없는 항목이에요.'` |
| 190 | `'이미 다른 회의에 연결된 항목입니다.'` | `'이미 다른 회의에 연결된 항목이에요.'` |

## `mutation-store.js` — 저장 잠금·복구 필요

| 줄 | 전 | 후 |
|---|---|---|
| 7 | `'데이터를 지키기 위해 저장을 멈췄습니다. 기존 기록은 그대로 있습니다. README의 "복구 필요 상태"를 따라 정리한 뒤 서버를 다시 시작해 주세요.'` | `'복구가 필요해서 저장을 멈췄어요. 기존 기록은 그대로 있어요. README의 "복구 필요 상태"를 따라 정리한 뒤 서버를 다시 시작해 주세요.'` |
| 21 | `'외부에서 변경된 파일이 있습니다. 복구 저널과 현재 파일을 보존했습니다.'` | `'밖에서 바뀐 파일이 있어요. 복구 저널과 지금 파일은 그대로 뒀어요.'` |
| 32 | `'다른 저장 프로세스가 실행 중입니다.'` | `'다른 저장 프로세스가 돌고 있어요.'` |
| 43 | `'다른 저장 작업이 진행 중입니다. 잠시 후 다시 시도해 주세요.'` | `'다른 저장 작업이 돌고 있어요. 잠시 뒤 다시 시도해 주세요.'` |

## `client.test.js` — 테스트가 고정한 문구

| 줄 | 전 | 후 |
|---|---|---|
| 57 | `assert.match(app.nodes.get('liveRegion').textContent, /저장을 확인하지 못했습니다/);` | `assert.match(app.nodes.get('liveRegion').textContent, /저장됐는지 확인하지 못했어요/);` |
| 104 | `error: '데이터를 지키기 위해 저장을 멈췄습니다.'` | `error: '복구가 필요해서 저장을 멈췄어요.'` |
| 108 | `assert.match(app.nodes.get('liveRegion').textContent, /저장을 멈췄습니다/);` | `assert.match(app.nodes.get('liveRegion').textContent, /저장을 멈췄어요/);` |
| 385 | `assert.match(notice(normal), /저장했습니다/);` | `assert.match(notice(normal), /저장했어요/);` |
| 391 | `"이미 처리했거나 찾을 수 없는 항목입니다."` | `"이미 처리했거나 찾을 수 없는 항목이에요."` |
| 393 | `assert.match(notice(failing), /저장을 확인하지 못했습니다\|이미 처리/);` | `assert.match(notice(failing), /저장됐는지 확인하지 못했어요\|이미 처리/);` |
| 396 | `assert.match(notice(item), /저장했습니다/, 'other workflow saves (item details) keep their notice');` | `assert.match(notice(item), /저장했어요/, 'other workflow saves (item details) keep their notice');` |
| 426 | `assert.match(notice(loud), /저장했습니다/);` | `assert.match(notice(loud), /저장했어요/);` |

## `server.test.js` — 테스트가 고정한 문구

| 줄 | 전 | 후 |
|---|---|---|
| 372 | `assert.match(body.error, /저장을 멈췄습니다/);` | `assert.match(body.error, /저장을 멈췄어요/);` |
| 367 | `assert.match(status.reason, /외부에서 변경된/);` | `assert.match(status.reason, /밖에서 바뀐 파일/);` |

## `report-drafts.test.js` — 테스트가 고정한 문구

| 줄 | 전 | 후 |
|---|---|---|
| 64 | `/저장을 멈췄습니다/.test(error.message)` | `/저장을 멈췄어요/.test(error.message)` |
| 62 | `/저장 실패.*외부에서 변경된/` | `/저장 실패.*밖에서 바뀐 파일/` |
