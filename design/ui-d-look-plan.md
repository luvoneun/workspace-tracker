# 룩 반영 계획 (BL) — 사용자 확정 사항 (2026-09-21)

구조·동작(B1~B9a)은 그대로 두고 **겉모습과 몇 가지 배치**를 사용자가 고른 방향으로 바꾼다. 사용자는 PM이며 매일 종일 띄워 두는 앱이다. 사용자의 말: "동글동글하고, 색이 좀 있고, 글씨 간의 여유가 있는 것이 좋다", "토스/flex 느낌이 나면 좋겠다", "전체적으로 괜찮은데 약간 복잡스럽다", "완료 표시는 체크박스가 맞다".

## 기준 자료
- 눈으로 볼 기준: 검수 복사본 `…/scratchpad/ui-d-review/tracker/inbox-app/`의 **룩 5 「정돈」**(`index.html` 머리 스크립트가 `data-look="5"`를 준다. CSS는 `ui.css` 끝의 룩 블록들 — 룩 1·2·4·5 규칙이 겹쳐서 최종 모양이 된다. 도우미는 `app.js`의 `look*` 함수). 스크린샷: `…/scratchpad/look-5-*.png`. **이 복사본은 읽기만 하고 고치지 않는다.**
- 값 명세: `…/scratchpad/look4-spec.md`(토큰 라이트/다크·대비 계산·부품 규칙) + `…/scratchpad/look5-spec.md`(룩 4에서 덜어낸 것). 두 파일을 합친 것이 확정 룩이다. `ui-d`에는 `html[data-look]` 분기 없이 **정식 토큰·부품 규칙으로** 옮긴다(전환기·`look*` 분기 코드는 가져오지 않는다).
- 글꼴 파일: `…/scratchpad/fonts/PretendardVariable.woff2`(v1.3.9, sha256 `9599f12fd42fc0bce1cd50b47a0c022e108d7aa64dd0d1bb0ed44f3282d900b4`) + `LICENSE.txt`(SIL OFL 1.1).

## 확정 룩 요약 (자세한 값은 명세 파일)
연회색 바탕(`#f2f4f6` / 다크 `#15181e`) 위 **테두리 없는 큰 둥근 흰 카드**(20px, 아주 옅은 그림자; 다크는 hairline 허용) · 푸른 기 회색 글자(`#191f28`/`#4e5968`/`#636c78`) · 파랑은 채움 `#3182f6` / 흰 글자 올리는 버튼·파란 글자 `#1b64da`(다크 `#4b91f1`/`#7aa9ff`) · 구역 제목 굵고 크게(18–20/700) + 둥근 개수 칩(구역·카드 제목에만, 그룹 제목은 회색 숫자) · 줄 54px, 줄 사이 선 없이 여백+둥근 회색 hover(14px) · 버튼 3단 둥근 사각(파란 채움 / 연파랑 / 연회색, `:active{scale(.98)}`) · 입력칸은 테두리 없는 회색 채움 + 초점 파란 테(빠른 추가는 평소 맨 줄, 초점에서만 채움) · 세그먼트는 회색 트랙+흰 손잡이 · **체크박스는 둥근 네모(22px, 6px, 켜지면 파란 채움+흰 체크, 누르는 영역 28px)** + 완료 순간(체크 그려짐 → 제목에 줄 → 옅어짐) · 상태 배지는 6px 모서리의 작은 연한 배지로 **급한 것만**(`긴급`·`기한 N일 지남`·`오늘까지`·회의의 `초안 N`), 나머지는 아이콘+회색 글자 · 프로젝트 색 점(6색 토큰)은 오늘 목록 그룹 제목(과 아래 10번의 제목 뒤 표기)에만 · 새로 들어온 것은 아주 옅은 따뜻한 카드 · 진행 표시는 오늘 할 일 카드 윗변 3px 파란 선 · 레일 296px, 카드 3장(미팅 / 리마인드 / 확인 대기), 요약 줄 없음 · 카드 사이 16px·안쪽 10px 통일 · 글꼴 `"Pretendard Variable", Pretendard, -apple-system, …`(앱에 내장) · 모션 150–220ms, reduced-motion 존중 · 글자 대비 4.5:1(배지 글자/틴트 포함), 컨트롤 3:1, 누르는 영역 28px.

## 사용자 확정 사항 14건 (2026-09-21, 전부 반영)
1. **오늘 미팅의 "지금"**: 배지 없이 색으로 — 진행 중인 회의는 시각·제목 파란 글자 + 줄에 연파랑 바탕, 끝난 회의는 흐리게, 앞으로 있을 회의는 그대로.
2. 회의 모아보기 = ⌘K 팔레트의 회의 필터(이미 구현) — 새 룩만 입힌다. 회의 정리 패널도.
3. **설정**: 아이콘을 톱니바퀴로, 명칭 `환경설정` → `설정`(창 제목·aria·사용법 문구·README).
4. **상세 패널은 항상 화면 위쪽(헤더 바로 아래)에 고정** — 어느 줄을 눌러도 같은 자리, 길면 패널 안에서 스크롤. 누른 줄은 선택 강조로 표시. (줄 높이에 맞춰 열던 `anchorTop` 방식 폐기. 업무·확인 대기·회의 패널 모두, 오늘·프로젝트 탭 모두.)
5. **새로 들어온 것**: 줄에 `오늘 · 나중에 · 완료` 세 개 상시 노출(평소 연회색, 줄 hover/focus에서 연파랑), 프로젝트 지정·기한·우선순위·삭제는 ⋯ 안. 슬랙 원문 링크는 제목 뒤.
6. **결정 검토 숫자**: 첫 화면 요약 줄은 없애고, 탭 이름 옆 작은 숫자(`아이디어·결정 3` = PRD 미반영 결정 수; 새 기록 점은 숫자가 대신함) + 결정 구역 제목을 `결정` + 조용한 `PRD 미반영 3`으로.
7·8. 프로젝트 탭은 이미 구현 — 아티팩트 시안(`/Users/luvon/personal/design/mockup/workspace-d.html`의 프로젝트 탭)의 구성·비율을 기준으로 새 룩을 입힌다.
9. **확인 대기 카드**: 줄을 두 줄 구성으로 — 첫 줄 제목 전체(두 줄까지, 말줄임 최소화), 둘째 줄 `결제팀 · 2일째`(3일째부터 주의색 글자) + 회신 기한이 지났거나 오늘이면 배지 + `9월 24일 다시 확인`/`오늘 요청함` 조용한 글자. "다시 확인할 항목"은 따로 나열하지 않고 해당 줄을 목록 맨 위로 올려 `오늘 다시 확인` 표시. "답변이 해결된 업무"는 리마인드 카드로. 입력란은 카드 맨 아래 `+ 확인 대기 추가`.
10. **프로젝트 이름 위치**: 그룹 제목이 프로젝트가 아닌 곳(`진행 중` 그룹, `마감·중요도순` 보기, 검색 결과, 리마인드)에서는 프로젝트를 **제목 바로 뒤**에 작은 색 점 + 회색 글자로(`… 확인하기 · ● 알림센터`). 줄 가운데 떠 있는 프로젝트 열은 없앤다.
11. **태그 말과 아이콘**(종류마다 작은 아이콘 + title 툴팁):
   - 기한: 달력 아이콘 + `오늘까지` / `내일까지` / `9월 27일까지` / **`기한 2일 지남`** — 배지는 `오늘까지`·`기한 N일 지남`만.
   - 밀림: 시계 아이콘 + **`어제에서 밀림`** / **`5일째 밀림`**(회색 글자). 툴팁: "오늘 하려다 넘어온 업무예요".
   - 우선순위: 깃발 아이콘 + `긴급`(배지) / **`중요`**(주황 글자, 배지 아님). 데이터 값(critical/high)은 그대로, 표시 말만. 상세·메뉴의 선택지는 `긴급 / 중요 / 보통 / 낮음`.
   - 답변 기다림: 말풍선 아이콘 + **`답변 기다리는 중`**. 답변 옴: **`답변 왔어요`**(초록 글자).
   - 확인 대기 쪽 `회신 기한`도 같은 규칙(`회신 기한 1일 지남`).
12. 주간요약은 아티팩트 시안 구성(B9a가 구조 구현) — 새 룩에서 문서 타이포를 시안 비율로.
13. **슬랙 원문**: 원문이 있는 업무·확인 대기·결정 줄은 제목 바로 뒤에 조용한 `원문` 링크를 **항상** 표시(새 탭), 상세 패널에서는 제목 바로 아래 `슬랙 원문 열기`.
14. **미루기 제안**: 진행 문장에 붙인다 — `19개 중 5개 끝냈어요 · 2개 미룰까요?`(뒤쪽이 글자 링크, 누르면 머리줄 아래로 펼침). 제안이 없으면 안 보임.
+ 오늘 할 일 머리줄: 제목 + 개수 칩 + 위 문장 / 오른쪽 `나중에 할 일 13` + ⋯(보기 `프로젝트별/마감·중요도순`, `여러 개 선택`). `오늘 신규 14`는 새로 들어온 것 제목 오른쪽 끝의 조용한 글자 버튼(누르면 팔레트의 오늘 신규 필터 — 이미 구현).
+ 헤더의 새로고침 아이콘은 유지.

## 말투 — 해요체 (사용자 결정)
말을 거는 문장(알림·안내·빈 화면·오류·도움말·서버 오류 문구)은 해요체. 버튼·메뉴·열 이름은 짧은 명사형 유지(`완료로 표시`, `내일`, `나중에`, `오늘로`, `삭제`). 예: `완료했어요` · `내일로 미뤘어요` · `나중에 할 일로 옮겼어요` · `삭제했어요` · `되돌렸어요` · `저장했어요` · `저장됐는지 확인하지 못했어요. 입력한 내용은 그대로 있어요` · `오늘 할 일을 모두 끝냈어요` · `복구가 필요해서 저장을 멈췄어요`. 테스트가 고정한 문구는 테스트와 함께 바꾼다. 바꾼 문구 전체를 전→후 표로 보고(사용자가 검토한다).

## 나누기
- **BL1**: 글꼴 내장(서버 허용 `/fonts/*.woff2` + MIME + 테스트, 인증 예외에는 넣지 않음) · `ui.css` 토큰·공용 부품을 확정 룩으로 전면 교체 · 네모 체크박스·완료 순간 · 오늘 탭 전체(위 1, 4, 5, 9, 10, 11, 13, 14, 머리줄 ⋯, 진행 윗선, 프로젝트 색 점, 레일 카드) · 3(설정 명칭·아이콘) · 6(탭 숫자).
- **BL2**: 나머지 화면을 새 룩에 맞춰 다듬기(⌘K 팔레트, 회의 패널, 프로젝트 탭, 아이디어·결정, 주간요약, 설정, 여러 개 선택 막대, 알림·배너) · 해요체 전체 + 테스트 · `/api/track/seen`의 성공 알림 없애기(NEW는 조용히 사라져야 함 — `request` 호출에 기존 `quiet` 옵션 사용, `request` 정의는 그대로) · `DESIGN.md`를 확정 룩으로 다시 쓰기.
- 그 뒤 **B9b 마감**: README·DECISIONS(덧붙이기)·PRODUCT.md 말투 항목·스킬 설명·`workflows.css`/`<style>` 잔여 삭제·alias 토큰 삭제·반응형(1280/1120/900/640/520)·다크 전수 점검.
